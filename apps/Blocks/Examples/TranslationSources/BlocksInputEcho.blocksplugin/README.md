# Blocks Input Echo

This schema v3 example source returns a stable JSON description of the
translation invocation. It has no network or secret permissions. Screenshot
bytes are never included in the result; only attachment metadata and SHA-256
are echoed.

The package is a development fixture. It is not installed or enabled by
default.
