# 源码安装与独立发布

现行方向：MIT 开源；不计划 App Store。本文描述入口与安全边界，实际可用状态以
项目管理库的最新验收记录和 GitHub Releases 为准。没有正式 Release 时，安装脚本
会拒绝安装，不会拿未签名源码产物充当官方发行包。

## 贡献者：Blocks 源码版

需要 Apple Silicon Mac、完整 Xcode 26+ 及其支持的构建主机系统。发行包的 macOS 14+
最低版本不代表 Xcode 可以运行在 macOS 14。无需 Apple 开发证书或 provisioning profile。

### Xcode 27 工具链兼容性（#46）

旧版 CLI 曾在 Xcode 27.0（27A266a）/ macOS 27 编译时触发 `swift-frontend` 的 SimplifyCFG 崩溃。2026-09-26 已在相同版本的 Xcode 27.0（27A266a）、Swift 6.4 与 macOS 27.0（26A428）上完成实机工具链复验。

`0.1.0-beta.4` 起的结构规避保留 `-O`：泛型动作返回普通输出值，进程退出与文件生命周期分离，并设置不内联边界。当前源码在独立构建目录完成完整 CLI 的显式 `-O` 构建，统一源码入口的主 App、CLI、Helper 构建也通过；23项隔离执行测试和9个真实CLI dry-run通过，含原报告的 scrolling.cancel。不需要关闭优化、删减CLI功能或调整系统权限。详见 [Xcode27复验记录](../项目管理库/018_阶段性公开发布/Issue-46-Xcode27复验-2026-09-26.md)。此结论只涵盖上述工具链，不代替其他版本和全部运行时功能验收。

多版本并存时，可仅对当前命令指定工具链，不必改变全局 `xcode-select`：

```sh
DEVELOPER_DIR=/Applications/Xcode-27.app/Contents/Developer ./script/dev.sh build
```

在检出目录执行：

```sh
./script/dev.sh doctor
./script/dev.sh run
./script/dev.sh test
./script/dev.sh update
```

`run` 编译主 App、CLI 和 Helper，固定安装到
`~/Applications/Blocks.app`，成功后启动。`build` 是仅构建、不安装的附加入口。
`update` 只允许干净工作树的快进拉取，不自动 stash、覆盖修改或重置分支。

Blocks 源码版保持 `app.blocks.dev`、原有独立 Application Support/Keychain/IPC 命名空间。
私有 `peers.json` 登记当前四个可执行文件的身份与 CDHash；重新编译应通过统一入口安装，
不要只替换其中一个二进制。开发 CLI 导出的是 `blocks-dev` 包装入口，调用固定安装内的已登记 CLI。
正式版不会信任开发版清单，开发版也不会接入官方 Sparkle 更新。

### 源码升级自动编排（Beta 9 起）

已安装支持此协议的源码版后，直接执行 `./script/dev.sh run` 或 `update`。安装器先验证新包，再调用旧包内已登记的 CLI，经本地双向身份校验请求旧 App 安全暂停并退出；仍使用旧 Broker 的版本先认证排空并注销它。确认旧进程/job 消失后才原子替换包与 peers.json。新源码版由主 App 承载业务 CLI，不再注册独立 Action Broker；保留总开关意图和模块选择，不再日常手动关闭/重开 CLI。

管理通道只编译进 LocalDevelopment，独立于日常 CLI 开关；固定私有 Unix socket，校验内核 audit token、当前安装清单、可执行文件路径和代码签名。它只提供版本探测、准备、提交、取消，不接受任意路径或业务命令。正式沙盒发行版不启用此通道，仍走现有 Sparkle 更新事务。

忙碌、身份失败、断连或超时都不会强杀 App/Broker，也不会替换未经安全准备的安装。请等待工作完成后重试。准备中断会恢复；准备成功后换包失败，安装器只在原身份与清单仍匹配时尝试重新打开旧 App，备份继续保留。

**首次迁移边界：beta.8 及更早版本没有管理入口。** 若它仍运行或 Broker 仍注册，首次安装 beta.9 源码版需最后一次：等待任务完成 → 关闭总 CLI 集成开关并确认停止 → 退出 App/Helper → 重装 → 重新开启。安装器明确提示此兼容路径，不会冒充旧 App 发认证命令；后续支持协议版本之间才自动编排。已完全退出且无注册的旧安装可直接升级。

若旧二进制已经被替换、Broker 身份握手无法建立，程序不能证明没有写任务，因此仍不会自动强杀或重置注册。请保留诊断与旧安装备份；不要把 `broker_unavailable` 当成模块授权已关闭的证据，也不要盲目重复重装。

内置助手现名为 `blocksHelper.app`，与主 App 使用同一图标。其 Bundle ID、签名身份与配对命名空间不变；旧独立助手仍按既有迁移规则处理，不静默删除。主 App 启动只恢复已配对且可信的助手，不自动开启登录项、不弹权限申请、不清除配对。真实跨路径更名后的系统授权保留仍应在原安装环境确认。

### CLI 入口与模块授权（Beta 5 起）

在“本地自动化”开启所需模块即可开始命令配置。截图、剪贴板、翻译源与插件分别授权，默认均关闭；从旧版本升级也不会把旧全局开关隐式迁移成全部授权。总开关暂停所有 Broker 动作但保留模块选择；模块关闭后请求返回 `module_disabled`。`blocks list` 实时查询已认证 Broker/App，不再离线展示全部静态动作，旧接口会要求升级，不静默回退。

开发版自动管理真实用户目录下的 `~/.local/bin/blocks-dev`；官方沙盒版首次使用系统目录选择面板授权命令目录，推荐 `~/.local/bin`，不关闭沙盒或经 Helper 绕过访问控制。已拥有的命令在 App 启动时自动检查并安全更新；陌生命令、被更改的文件或不安全目录不自动覆盖。需要另选位置或重新授权时使用高级位置入口。

App 不修改 `.zshrc` 等 shell 文件，也不执行 shell 探测终端环境。GUI App 的 PATH 不等于终端 PATH；若终端找不到命令，需将命令目录加入自己的终端 PATH。可消除提示，但不将“提示已消除”当作 PATH 已正确设置。

卸载 CLI 命令独立于模块授权：不会关闭模块，但会阻止后续启动自动重装；再次明确开启模块或配置命令才重新安装。更新与卸载沿用身份校验、事务恢复和应用更新门禁。

离线反馈与隐私 CLI 保留原独立路径，不受这四个 Broker 模块开关控制。反馈提交仍需自身预览与明确确认；剪贴板全文 `show/export` 仍需在 App 中逐次批准范围。模块开关不是全文读取授权，也不是对任意本机工具的系统级隔离。

替换前需正常退出 Blocks（包括旧路径的 Blocks Dev）与其内置 Helper。脚本不杀进程；安装互斥、身份或权限不满足时会停止。
旧路径 `~/Applications/BlocksDev/Local/Blocks Dev.app` 会作为一次性迁移源；事务中临时保留旧包，安装和 peer 登记成功提交后清理旧包，只保留 `~/Applications/Blocks.app`。失败时还原旧包及 peer 登记，无法安全恢复时保留恢复材料并报告。存在两份源安装或其他 Bundle ID 占用目标路径时停止，不猜测覆盖对象。

本仓库的 Codex Run 按钮也使用 `./script/dev.sh run`，不再另装 Debug 副本。构建与验收产物不作为日常安装入口；临时界面预览完成后应注销并清理预览 App，不保留可被启动器发现的多份安装。应用数据、钥匙串及配对不属于旧安装包清理范围。

### 固定本机签名，避免每次编译重授权

可在 `~/.config/blocks/development-signing.json` 配置公钥身份信息（文件权限 0600，目录归当前用户且不可被其他用户写入）：

```json
{"schemaVersion":1,"mode":"certificate","identity":"填写 security find-identity -v -p codesigning 列出的40位证书指纹","teamIdentifier":"填写证书的10位Team ID"}
```

私钥仍留在系统钥匙串，不写入配置或仓库。统一安装入口使用该证书重签主 App、内置 Helper/CLI/Broker 及嵌套代码，并保留各组件权限边界。升级前比较旧新 designated requirement，已使用证书签名的安装不允许变回临时签名或改变身份。固定证书丢失时明确失败，不自动切换其他证书。

自 `0.1.0-beta.3` 对应源码起，没有 Team ID 的固定本机自签证书也受支持，可省略 `teamIdentifier` 或将其设为 `""`。不要编造 Team ID；它不是 GitHub 用户名或随意填写的组织名称。无 Team ID 的配置示例：

```json
{"schemaVersion":1,"mode":"certificate","identity":"填写有效代码签名证书的40位大写SHA-1指纹"}
```

两种证书模式都校验实际签名的叶证书指纹；指定 Team ID 时还需一致，省略时只允许无 Team ID 的签名元数据。空 Team ID 本身不授予信任。证书必须出现在 `security find-identity -v -p codesigning` 的有效身份列表中；仅有证书文件、缺私钥或用途不适合代码签名时不能使用。此能力只用于本机开发安装，官方 Developer ID 分发策略不变。

已有无 Team ID 的固定证书安装应继续使用原证书，不必为升级而创建新证书。本次不开放静默换证书：旧新 designated requirement 不一致仍拒绝；原证书遗失时保留旧包和数据，恢复原签名材料或单独规划显式迁移，不能靠清空配置绕过。签名身份有效不等于系统接受 SMAppService 启动；新源码版 CLI 因此改由主 App 承载，不依赖独立 Action Broker。macOS27 自签环境仍需原机复验，详见 [CLI 启动排查](CLI服务启动失败排查.md)。

缺少配置时保留无需证书的 ad-hoc 构建路径，`doctor` 会提示重编译的授权限制。首次从临时签名迁移到固定证书可能需要一次重新授权；后续同身份升级应保留授权，但系统策略或用户撤销仍可能要求确认，不能承诺永不弹窗。不修改 TCC 数据库、不关闭 Gatekeeper、不用明文密钥替代钥匙串。

自 `0.1.0-beta.2` 对应源码起，Helper 新配对的登录钥匙串条目显式只信任已登记、签名和 CDHash 校验通过的主 App 与内置 Helper，避免首次跨进程读取时要求授权。配对相关读写统一采用无弹窗访问；旧条目无法读取时保留密钥并报告错误，刷新不会自动重置配对。登录钥匙串锁定时请先解锁；若旧条目的 ACL 属于不同签名，应恢复原签名安装，或由用户在“钥匙串访问”中明确修复访问权限。此改动不把所有应用加入信任列表，也不改变其他翻译服务凭据的访问策略。ad-hoc 重编译会改变身份，仍应优先使用固定本机签名。

### 权限开关已开但运行仍拒绝时的手动恢复

这是一种现象，不是对 TCC 内部记录或根因的诊断。旧 ad-hoc 签名迁移到固定证书后，旧的
权限记录可能无法匹配当前签名；也可能是路径、系统策略或其他环境因素。Blocks 不读取或写入
TCC 数据库，绝不自动执行重置。

只有在当前安装包的权限页仍显示“未授权”，且用户已经确认在系统设置中开启了该权限时，才可由
用户自行决定在终端定向清除**当前 Bundle ID** 的单项记录：

```sh
tccutil reset ScreenCapture app.blocks.dev
tccutil reset Accessibility app.blocks.dev
```

这两条命令分别只移除本 Bundle ID 对应服务的记录，不会授予权限；下一次请求必须重新在系统设置
中授权。授权真正被 macOS 识别后，相关系统提示通常会自行关闭；若不关闭，说明权限仍未生效，
不应通过重复点击请求来堆叠提示。不要执行全局 reset，也不要对其他 Bundle ID 执行这两条命令。

## 普通用户：官方包

只有通过 Developer ID 签名、公证和 stapling 的 DMG 才是官方安装包。安装不需要 Xcode。
可以从 GitHub Releases 下载后拖入应用程序，或使用 `./script/install.sh`。
脚本会核验版本、架构、固定签名身份、Gatekeeper 和完整性校验；SHA-256 不替代身份验证。
已有用户目录安装会原地更新；不会为绕过系统目录权限另造重复安装。

### 旧 Debug / 独立 Helper 迁移

此路径必须在真实迁移验收后才能标为完成。目前不自动把未知 Debug 包改签或删除。

1. 记录旧 App 和 Helper 的实际位置、Bundle ID、Team ID、数据目录与配对状态，不能只按显示名称猜测。
2. 正常退出旧进程；存在写入或退出失败时停止迁移，不 force quit。关闭相关登录启动项，防止旧包重新启动。
3. 备份旧 App、Helper 和退出后的完整应用数据目录（包括数据库附属文件）。备份放在归档目录，
   不再作为可启动的第二份 App 留在 Applications 中。钥匙串和配对项保留，不清空、不导出私钥。
4. 确认官方包使用兼容的 Bundle ID、Team ID、Keychain group 后安装；不把 `app.blocks.dev` 数据并入正式版。
5. 验证历史、收藏、设置、凭据重启读取、Helper 配对和单实例，再启用新包的登录/CLI 集成。
   任一项失败保留备份和诊断，不能通过清库或重新配对来假装迁移成功。

已知旧开发入口可能位于 `~/Applications/BlocksDev/Debug/Blocks.app`；不得因它不在常规
Applications 根目录就忽略它。各用户路径和数据位置需现场核实，本文不是自动删除指令。

## 维护者：本机发布

首次配置包括：固定的 `install-release-policy.json`（见同目录 schema）、忽略跟踪的
`apps/Blocks/Config/ReleaseIdentity.local.xcconfig`、两份独立 Developer ID profile，
以及本机公证 profile。将 `script/release/release.local.example.json` 的非秘密配置填入
`~/.config/blocks/release.json` 并设置为 `0600`。其中仅存公证 profile 名称、公开证书指纹、
公钥和本机路径，不存密码、API 私钥或 Sparkle 私钥。

Sparkle 使用独立钥匙串 account `app.blocks.app.sparkle`；私钥不进入 GitHub CI。
公钥随官方 App 编译，不能发布后随意更换而中断信任链。

为版本提交 `release-notes/<版本号>.md`，从干净检出运行：

```sh
./script/release.sh prepare 0.1.0-beta.1
./script/release.sh publish 0.1.0-beta.1
```

`prepare` 锁定已提交的源码到临时独立 worktree，核验身份/profile/公证配置后构建、打包、
公证并创建草稿。默认 build number 高于已验证 appcast 与本机状态记录；缺少远端证据时不猜测。
`publish` 复验草稿实际字节、签名和 tag 后公开，再更新对应频道的 appcast。
已公开但 feed 更新失败时可重试 `publish`；不会重新覆盖发行包。中断发生在外部操作和本地
记账之间时必须先核对状态，不手工删锁后盲目重发。

安装包托管于 GitHub Releases；GitHub Pages 仅托管 stable/beta 的静态 appcast，现有官网不迁移。
所有包默认稳定频道，Beta 由用户主动选择；无合适的更高版本时不会自动降级。
首次 Beta、下一版本真实升级和现有发布门禁全部通过后，才具备正式交付条件。
