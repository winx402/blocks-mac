# 覆盖矩阵（2026-07-24）

## 验收口径

- “问题”包含功能 Bug、体验问题、交互问题、UI 问题、可访问性问题和运行时质量风险。
- 自动化测试通过不能替代真实 UI、外部 App 和运行中进程证据。
- 对会写入凭据、发起真实网络调用、改变系统权限或泄露屏幕隐私的动作保持低敏边界。

## 覆盖结果

| 区域 | 深度覆盖 | 结果 | 证据或限制 |
| --- | --- | --- | --- |
| 版本与启动 | 重新构建并启动、唯一进程、冷启动、版本/签名/哈希/UUID | 通过 | `0.1.0 (1)`；冷启动后仍为唯一稳定路径进程 |
| 设置导航 | 10 个顶层页面、边栏隐藏/恢复、最小化/恢复、默认与最大窗口 | 通过 | 默认窗口和内容上限已调整，多行说明不再强制两行截断；最大窗口见截图 25 |
| 全局快捷键 | 截图 `Option+A`、剪贴板 `Option+V`、翻译 `Option+D`；重新注册；真实事件派发 | 通过 | 外部 TextEdit 中三项均真实触发；注册接受和事件派发分层显示，见截图 22 和 shortcut 日志 |
| 截图入口 | 菜单入口、选择层、取消、3 秒倒计时、比例约束 | 通过 | 菜单启动/取消连续 10/10；倒计时与 Escape 取消通过 |
| 截图编辑器 | 线/箭头、矩形、文字、气泡、步骤、像素化、椭圆、画笔、模糊、编号、聚光、隐私遮盖、放大镜、水印、OCR、高亮、撤销、关闭确认 | 修复后自动化通过/真实气泡待复核 | OCR 既有证据有效；2026-07-25 补充修复气泡语义文字颜色和二维 `.fixedBox`，单测通过，真实鼠标气泡缩放与导出仍待人工闭环 |
| 剪贴板设置与浮窗 | 清理策略、保留时长、数量模式、字体、面板方位、清除确认、标签输入、隐私搜索、浮窗详情、自动粘贴、历史大图下的普通线程 ID 文本捕获、统一焦点状态机、原生单击／双击识别 | 修复后部分通过 | 纯文本安装版复制只读取声明文本表示且无冻结；详情双任务通道、当前记录缓存失效及主/子 Panel 焦点状态机自动化通过；条目已统一采用 AppKit 原生单击／双击识别且完整 AppTests 0 failure；TextEdit、浏览器、Electron 的连续物理鼠标双击仍待人工闭环 |
| 剪贴板隐私 | 79 个 App、2 条限制；搜索 Screen Sharing | 通过 | 仅查看，无策略修改 |
| 翻译设置与浮窗 | 默认目标语言、记住上次语言、引擎选择、运行时开关、Provider 路由检查、浮窗输入/结果/清空 | 通过 | 安装版切换未配置 LLM 引擎后显示缺失配置和下一步；恢复本地引擎后立即可用；未读 Keychain、未发网络 |
| Provider | 空配置、低敏本地预览、路由解析、审计生成和就地反馈 | 通过/有限 | 预览显示审计 ID、未发网络及下一步；未使用真实 Key、未发真实网络 |
| 数据与审计 | 查看记录、清空取消、清空确认、删除结果 | 通过 | 取消保留 2 条，确认删除至 0；文案明确永久且不可撤销 |
| Agent 与 CLI | 临时启用、列表、历史查询、搜索 dry-run、隐私主体列表、禁用后的结构化错误 | 通过 | 已恢复关闭；禁用时返回 `broker_unavailable` 和 `explicit_enable_required:true` |
| 权限与身份 | 屏幕录制、辅助功能、输入监控、稳定路径/Bundle/Team 诊断 | 通过 | 三项权限均显示已授权；未修改系统设置 |
| 外观与语言 | 浅色、深色、跟随系统；语言选项检查 | 通过/有限 | 外观均实测并恢复跟随系统；未重启切换语言，避免持久化用户偏好 |
| 关于与品牌 | About、菜单栏、设置页标题 | 通过 | 可见品牌统一为“积木工具” |
| 自动化触发 | 生命周期状态、能力边界、下一步入口 | 通过 | 安装版明确显示规则已定义、运行能力未开放；“前往本地自动化”真实跳转成功，见截图 28 |
| 运行时质量 | 稳定 App 进程 faults/errors、最小 SwiftUI 对照、测试宿主日志、剪贴板主线程采样和重复复制资源曲线 | 修复后通过/外部覆盖单列 | 纯文本读取不再请求未声明表示；详情预览不在 SwiftUI body 解码；Screen Sharing 运行期间的新 changeCount 按系统真实变化记录，不用排序启发式掩盖 |
| 自动化回归 | 截图核心、App 测试、OCR/翻译专项、两个近上限资源用例、Release、Analyze | 通过 | 2026-07-25：ScreenshotCore 220、AppTests 434（2 个资源用例默认跳过、0 failure）；两个 58.5MP 用例另行显式 2/2 成功 |

## 明确未覆盖

- 未使用真实 API Key，未执行 Keychain 凭据写入/删除，未发起真实 Provider 网络调用。
- 未修改 macOS 权限、登录项或系统全键盘控制设置。
- OCR 未覆盖手写体、复杂表格和极低分辨率文档；已覆盖真实 Vision 中英日/混排、小字号 UI 与安装版编辑器链路。
- 未使用真实外部翻译 Provider；缺失配置分支和本地引擎恢复已在安装版验证。
- 剪贴板和翻译浮窗已完成低敏核心交互，但未读取或记录用户真实剪贴板正文。
- 未保存可能包含其他 Codex 任务或工作区内容的截图；发现一张可能泄露隐私的临时证据后已立即永久删除。
- 2026-07-25 的界面控制通道不能稳定暴露独立详情子 Panel 和截图 Canvas；详情保存后的连续真实点击、气泡真实鼠标二维缩放与导出仍需人工复核。
