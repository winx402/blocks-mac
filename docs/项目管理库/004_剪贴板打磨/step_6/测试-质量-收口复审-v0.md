# 004_剪贴板打磨 Step 6 测试/质量收口复审 v0

日期：2026-07-07
角色：测试/质量
对象：Step 6 收口覆盖回扫
范围：只读复审 `004_剪贴板打磨` R1-R13、C1-C17、P1-P5 覆盖闭环、P2 residual 与最低验证矩阵；不修改业务代码，不触发真实 App、真实剪贴板、provider、Keychain、TCC、System Settings、Finder、App launch、真实系统枚举或真实系统状态变更。

## 1. 结论

结论：`approve-with-changes`。

P0/P1：`0`。

产品经理 Step 6 覆盖回扫文档可以作为收口复审输入。基于 Step 1-5 最终验收文档，本轮未发现 R1-R13、C1-C17、P1-P5 存在 P0/P1 级需求遗漏，也未发现产品经理把 P2 residual 写成已实测通过。

但 Step 6 最终接受前，仍需要补一组低敏集成证据。原因是若只继承 Step 1-5 的单阶段验收，搜索 + 标签 + 编辑后索引、旧事实源退出、真实内容可访问但输出可控、隐私策略 capture bridge 等跨阶段路径仍可能缺少最终 pass/fail 证据。该要求是收口验证补证，不是新功能开发或重开 Step 1-5。

## 2. R / C / P 覆盖判断

### 原始需求 R1-R13

未发现 P0/P1 级遗漏。

需要 Step 6 低敏证据支撑最终接受的重点：

- R2：搜索底座、标签搜索、详情编辑后索引更新需要组合回扫。
- R7 / R8：标签 / 收藏模型与旧 pinned / pinboard 退出需要最终旧事实源回扫。
- R9：隐私 UI + CLI 单一策略事实源、P13E capture bridge 与低敏输出需要最终 evidence 汇总。
- R12 / R13：详情编辑、dirty guard、metadata 组织与面板继续动作需要 P13D 证据作为最终收口输入。

可作为 P2 residual 接受、不建议在 Step 6 真实触发的部分：

- R3-R6 / R10-R11 的真实 UI 手感、真实鼠标 hover / click / double-click、真实录屏证据。
- R9 的真实三目录枚举、真实 icon 截图、真实 hidden / unreadable / damaged app 枚举。
- R12 的真实系统剪贴板读写、真实 paste command 端到端。

### 澄清需求 C1-C17

未发现 P0/P1 级遗漏。

需要 Step 6 低敏证据支撑最终接受的重点：

- C1 / C12：真实内容可访问与输出可控必须同时成立。Step 6 需要最终确认 UI / CLI / audit / verification / 日志没有默认输出完整 payload、完整 URL、完整路径、secret 或图标 binary。
- C2：正文、标签、来源 App、URL、时间、富文本纯文本、OCR、文件名、类型同义词的搜索字段需要跨 Step 1 / 2 / 4 回扫。
- C7：旧 pinboard / pinned 不得作为 active UI、Store、filter、search、CLI 或 verifier ok 事实源。
- C8：UI AppInstance、CLI PolicySubject、PolicyRule 与 capture policy 必须共享同一策略事实源；dangerous action hard-block 仍需最终低敏证据。
- C9 / C14 / C15：详情编辑保存事务、OCR late completion、dirty navigation guard、系统剪贴板不更新需要最终 P13D 证据汇总。
- C16 / C17：最终文档需要区分已完成能力、P2 residual、后续专项，避免把发布前或后续专项反写为 004 内新增范围。

可作为 P2 residual 接受的部分：

- 真实 OCR 识别质量、语言效果、长图片性能。
- 真实 VoiceOver / accessibility inspector。
- 真实系统剪贴板、真实用户数据库 mutation、真实系统 App 枚举。

### 流程需求 P1-P5

未发现 P0/P1 级遗漏。

- P1-P3 已由 step 目录、阶段 PRD / 技术方案 / 复审 / 验收文档闭合。
- P4 / P5 仍处于 Step 6 收口流程中；产品经理覆盖回扫已完成，但最终闭合必须等待角色复审、最低验证矩阵和项目负责人最终接受。

## 3. 需要 Step 6 补低敏证据的 P2 residual

以下不要求真实 App / 真实剪贴板 / 真实系统动作，但需要低敏 verifier、静态检查、fixture 或 evidence 汇总来支撑最终接受。

| 路径 | 最低证据 | Pass/fail 门槛 |
| --- | --- | --- |
| 搜索 + 标签 + 编辑后索引 | P13A + P13B + P13D + P9A/P9B 组合证据，覆盖标签字段、编辑后 search document rebuild、旧内容不误命中 | 任一门禁失败、缺 Step 4 编辑后索引证据、缺 Step 2 tag rebuild 证据则 fail |
| 真实内容可访问 + 输出可控 | P11E + P13A + P13E sanitizer evidence | 输出命中完整 payload、完整 URL、完整本机路径、home path、secret、Authorization、Bearer、base64 / image bytes、provider route 则 fail |
| 旧事实源退出 | P13B + P11E + P13A + P13E + P9B 静态 / fixture evidence | `redacted`、`pinned`、`pinboard`、旧 `excludedBundleIdentifiers` 任一作为 active fact source 或 verifier ok 输入则 fail |
| OCR 生命周期 + 编辑冲突 | P13A + P13D evidence，覆盖 pending/running/failed/retry/succeeded、用户编辑后 late completion 不覆盖 | OCR retry / late completion 覆盖用户编辑或缺失败态证据则 fail |
| 详情 dirty guard + 继续动作 | P13D R3 evidence，覆盖 paste、selected paste、retry pending paste、translation panel continuation | dirty 下 Continue Editing 仍继续动作、Save and Continue 保存失败仍继续、Discard 未明确丢弃则 fail |
| 隐私 UI + CLI + capture policy | P13E evidence，覆盖 `ui_interaction`、`capture_bridge`、CLI typed subject、dangerous action hard-block | P13E `ok=false`、缺 required scenario、`ClipboardCapturePolicy` 未消费 snapshot、dangerous action 可被 confirm 解锁则 fail |
| Settings / 产品 polish 回归 | P8 + P8I evidence | 设置页回到旧 hardening 文案、旧 clipboard privacy exclusion list 或 Step 1-5 已移除入口则 fail |
| 构建与 CLI 基线 | Blocks App build、BlocksCLI build、`blocks --help`、`git diff --check` | 任一命令失败，或 CLI help 输出包含真实路径 / secret / dangerous command snippet 则 fail |

## 4. 可接受为最终 residual 的 P2

以下可以作为 004 最终 residual 或发布前人工验收待办接受，不应在 Step 6 里用真实系统动作补测，也不应升级为 P1，除非出现新证据表明核心路径不可用或会造成数据损坏 / 泄漏。

- 真实 App UI 操作、真实鼠标 hover 手感、真实单击 / 双击事件顺序、真实低敏录屏。
- 真实 VoiceOver / accessibility inspector 证据。
- 真实系统剪贴板读写、真实 paste command、真实用户数据库 mutation。
- 真实 Vision OCR 识别质量、语言覆盖、长图片性能。
- 真实 `/Applications`、`~/Applications`、`/System/Applications` 全量枚举、真实 icon 截图、真实 hidden / unreadable / damaged app 枚举。
- Settings 标签管理更贴近触发来源的反馈、右键新建标签完整命名输入。
- P13C `.label` localization verifier 完整性、P13D dispatch 指针清理、P13E temp DB migration harness。
- 既有 `FloatingPanelSupport.swift` actor-isolation warning、AppIntents metadata skipped warning。

这些项必须在最终接受文档中继续写为“未实测 / residual / 发布前或后续专项”，不能写成已通过。

## 5. 建议 Step 6 最低验证矩阵

建议项目负责人最终接受前至少串行运行或汇总最近一次同工作区低敏证据：

```bash
python3 tools/verification/p13a_clipboard_plaintext_search_ocr_checks.py
python3 tools/verification/p13b_clipboard_tags_model_checks.py
python3 tools/verification/p13c_clipboard_panel_interaction_layout_checks.py
python3 tools/verification/p13d_clipboard_detail_edit_checks.py
python3 tools/verification/p13e_clipboard_privacy_policy_checks.py
python3 tools/verification/p11e_clipboard_hardening_checks.py
python3 tools/verification/p9a_clipboard_repository_storage_smoke.py
python3 tools/verification/p9b_clipboard_appstate_repository_integration_checks.py
python3 tools/verification/p9c_no_reset_fixtures_ui_checks.py
python3 tools/verification/p8_clipboard_product_polish_checks.py
python3 tools/verification/p8i_settings_clipboard_system_checks.py
xcodebuild -project apps/Blocks/Blocks.xcodeproj -scheme Blocks -configuration Debug -derivedDataPath DerivedData/Blocks build
xcodebuild -project apps/Blocks/Blocks.xcodeproj -scheme BlocksCLI -configuration Debug -derivedDataPath DerivedData/Blocks build
DerivedData/Blocks/Build/Products/Debug/blocks --help
git diff --check
```

最低 pass/fail：

- 所有命令退出码为 0。
- P13A-E、P11E、P8/P8I、P9A/B/C 输出必须 `ok=true` 或等价 PASS。
- P13E 必须包含 `ui_interaction` 与 `capture_bridge` evidence，且 required scenarios 不缺失。
- P13D 必须包含 Step 4 R3 dirty continuation 证据。
- P13B / P9B 必须证明旧 pinned / pinboard active path 不参与当前 ok。
- P11E / P13A / P13E sanitizer 不得输出真实 payload、完整 URL、完整本机路径、home path、secret、Authorization、Bearer、token、base64、图标 binary、provider route 或危险系统命令。
- `blocks --help` 只证明 CLI 可用和帮助低敏，不得被写成真实 CLI policy mutation 通过。
- 若构建存在既有 warning，最终文档必须标注为既有非阻塞 warning，不能省略。

## 6. 对产品经理 Step 6 文档的判断

产品经理 `coverage-ready-for-review` 结论可以接受。文档已正确做到：

- 不把覆盖矩阵旧状态当最终事实。
- 不把 Step 1-5 P2 residual 写成已实测通过。
- 没有把 Step 6 扩大成新功能阶段。
- 列出了跨阶段集成路径和不建议在 004 内继续返工的内容。

建议补充或由项目负责人收敛时吸收：

1. 将第 7 节“建议进一步验证的集成路径”转成明确的最终验证矩阵，并记录实际 PASS / 未覆盖 / residual。
2. 对 R2、C1/C2/C7/C8/C9/C12/C14/C15、P4/P5 标注“需要 Step 6 低敏证据闭合”，避免最终接受时只引用阶段验收。
3. 最终接受文档必须继续保留真实 UI、真实剪贴板、真实 VoiceOver、真实系统枚举等 P2 residual，不得写成已实测通过。

## 7. 复审边界

本轮读取：

- `step_6/项目负责人-收口派发-Step6-v0.md`
- `step_6/产品经理-收口覆盖回扫-v0.md`
- `step_6/项目负责人-收口预审-v0.md`
- `需求覆盖矩阵-v0.md`
- Step 1-5 项目负责人最终验收文档。

本轮未读取业务代码，未运行 App，未触发真实剪贴板、provider、Keychain、TCC、System Settings、Finder、App launch、真实系统枚举或真实系统状态变更。
