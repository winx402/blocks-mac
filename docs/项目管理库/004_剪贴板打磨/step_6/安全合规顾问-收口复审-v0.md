# Step 6 安全合规顾问收口复审 v0

状态：role-review-complete
日期：2026-07-07
复审角色：安全合规顾问
复审对象：004_剪贴板打磨 Step 6 收口覆盖回扫

## 结论

结论：`approve`

P0：0
P1：0
P2：5

安全合规视角未发现 Step 6 收口覆盖回扫文档存在 P0/P1 级安全、隐私或权限遗漏。产品经理回扫对“真实内容可访问，交互输出可控”的表达基本准确：真实内容可访问被限定在本地 App 内的明确交互、搜索、详情、OCR 和策略管理能力；CLI、日志、audit、verification JSON、开发记录、验收记录、provider 和自动化仍必须低敏、有界、结构化，不等同于默认输出完整 payload、完整路径、真实 App 清单或敏感凭据。

Step 6 应继续禁止真实 App、真实剪贴板、provider、Keychain、TCC、System Settings、Finder、App launch、真实系统枚举、真实用户数据库 mutation 或真实系统状态变更。现有 P2 residual 可以作为最终 residual 或发布前待办接受，但不得写成已实测通过。

## 复审范围

已读取本轮输入：

- `docs/项目管理库/004_剪贴板打磨/step_6/项目负责人-收口派发-Step6-v0.md`
- `docs/项目管理库/004_剪贴板打磨/step_6/产品经理-收口覆盖回扫-v0.md`
- `docs/项目管理库/004_剪贴板打磨/step_6/项目负责人-收口预审-v0.md`

按需对照：

- `docs/项目管理库/004_剪贴板打磨/step_1/项目负责人-Step1验收-v0.md`
- `docs/项目管理库/004_剪贴板打磨/step_1/安全合规顾问-技术方案复审-v0.md`
- `docs/项目管理库/004_剪贴板打磨/step_4/项目负责人-Step4最终验收-v0.md`
- `docs/项目管理库/004_剪贴板打磨/step_4/安全合规顾问-Step4-R1复审-v0.md`
- `docs/项目管理库/004_剪贴板打磨/step_5/项目负责人-Step5最终验收-v0.md`
- `docs/项目管理库/004_剪贴板打磨/step_5/项目负责人-Step5-R3验收-v0.md`
- `docs/项目管理库/004_剪贴板打磨/step_5/安全合规顾问-技术方案复审-v0.md`

本轮未修改业务代码，未触发真实 App、真实剪贴板、provider、Keychain、TCC、System Settings、Finder、App launch、真实系统枚举、真实用户数据库 mutation 或真实系统状态变更。

## P0 Findings

无。

## P1 Findings

无。

## P2 Findings

### P2-1：Step 6 最终低敏证据清单可以再显式化

产品经理文档已经多处要求 synthetic / fixture / 低敏证据，且提到 secret、二维码、完整 URL、真实路径、真实系统枚举等禁止项。建议最终收敛时增加一张统一 low-sensitive evidence checklist，避免不同角色各自解释。

可吸收口径：

```text
Step 6 所有开发记录、验收记录、CLI/audit/verifier JSON、截图说明和失败详情默认不得包含：真实剪贴板正文、完整 URL/query、完整 file path/home path、OCR 原文、图片/base64、真实 App 清单、真实 bundle path、真实 command arguments、Authorization/Bearer/token/password/OTP/private key/cookie/session/JWT/webhook、provider raw request/response、TCC raw requirement/csreq、System Settings/Finder/launchctl/tccutil 可复制命令。
```

### P2-2：跨阶段低敏门禁组合建议写成 Step 6 最低安全复跑矩阵

产品经理文档已建议 P13A / P11E / P13E 组合回扫输出边界，并提到 P13D、P13B、P9B 等路径。建议项目负责人最终收敛时把安全相关 gate 组合写成最低安全矩阵，避免“单个阶段 PASS”被误读为跨阶段已闭合。

可吸收口径：

```text
Step 6 安全最低复跑矩阵建议包括：P13A、P11E、P13D、P13E、P9A/P9B，以及覆盖 tag/search 的 P13B。所有命令必须使用 synthetic / temp DB / fake provider / fake pasteboard / fake icon source，不读取真实剪贴板、不扫描真实系统 App 清单、不执行真实 policy mutation。
```

### P2-3：真实系统动作禁令应保留到最终接受文档

Step 6 输入和产品经理回扫均正确禁止真实系统动作。建议最终接受记录继续原样保留，不要因“集成验收”而放宽。

可吸收口径：

```text
Step 6 最终接受不以真实 TCC、System Settings、Finder、App launch、真实系统 App 枚举、真实剪贴板、真实用户 DB mutation 或 provider call 作为必须证据；如后续发布前要做实物验收，必须单独派发、单独记录、明确人工操作和脱敏要求。
```

### P2-4：Step 5 `canonical_identifier` / sensitive path 残余应继续按 P2 处理

Step 6 文档已把真实 App 清单、真实 icon、真实 DB mutation列为 P2 residual。建议最终收敛继续保留 Step 5 技术方案复审里的约束：`canonical_identifier` 只作为本地显式 policy rule 字段，不默认输出；首版不建议启用 CLI `--include-sensitive-paths`；如果存在该参数，不能用于任何 Step 6 证据。

可吸收口径：

```text
Step 6 不要求使用 `--include-sensitive-paths` 或完整 canonical path 证明 CLI 正确性；CLI 证据默认只允许 subject_ref、path_summary、source_directory、path_hash、path_redacted、counts 和低敏 error code。
```

### P2-5：真实 VoiceOver / 真实 UI / 真实 OCR 质量可以接受为发布前待办，不应被安全复审升级

产品经理文档已准确把真实 UI、VoiceOver、OCR 质量、真实 App icon 和真实三目录扫描列为 P2 residual。安全合规不建议在 Step 6 内为这些 P2 打开真实敏感动作；如果项目负责人需要补证据，应优先选择低敏截图、synthetic fixture 或人工发布前 checklist。

## 已确认的安全边界

- Step 6 没有新增功能需求，没有重开 Step 1-5 已接受范围。
- Step 6 文档没有把 P2 residual 写成已实测通过。
- “真实内容可访问”没有被写成 provider、CLI、日志、audit、automation 默认无限制外发。
- Step 1 已接受的明文展示 / 搜索 / OCR 仍保留低敏输出、无 provider、无真实 OCR / TCC / 系统设置动作边界。
- Step 4 已接受的详情编辑仍保留 fake pasteboard、P13D fail-closed、full value 显式读取和 OCR user-edited guard 边界。
- Step 5 已接受的隐私页 / CLI 仍保留 synthetic roots、fake icon provider、dangerous action hard-block、低敏 CLI JSON、无真实系统枚举、无真实 DB mutation边界。
- 产品经理文档已正确指出真实剪贴板、真实系统 App 清单、真实用户数据库 mutation、真实 VoiceOver、真实 UI 录屏等不能直接写成已验证。

## 可接受的安全 residual

以下 residual 可接受为最终 P2，不阻塞 Step 6 收口：

- 真实系统剪贴板未读取 / 未写入。
- 真实 Vision OCR 质量、多语言效果、长图片性能未实测。
- 真实 App UI、真实键盘 / 点击路径、真实 VoiceOver 未覆盖。
- 真实三目录 App 清单、真实 hidden / unreadable / damaged app、真实 icon 视觉未做实物验收。
- CLI policy set confirm 未对真实用户数据库执行 mutation。
- Step 5 legacy conflict 仍以 deterministic / static evidence 为主，后续可补 temp DB harness。
- 既有构建 warning、verifier 指针清理和 localization verifier 完整性清理不构成安全 P1。

## 需要补低敏证据或 fail-closed 检查的方向

这些不是新增功能，也不是 P1 阻塞；它们是 Step 6 最终验收可选择补强的低敏证据方向：

- 搜索 + 标签 + 详情编辑后的索引一致性：使用 temp DB / synthetic records，证明新 token、标签筛选、旧 token 退出和 FTS/search document 一致。
- 输出可控回扫：复跑 P13A / P11E / P13D / P13E，确认 CLI、audit、verification、开发记录和验收记录不泄露完整 payload、路径、OCR 原文、App 清单或 secret。
- 旧事实源退出：确认 redacted、pinboard/pinned、旧 excluded bundle ids 不再作为当前 UI / search / filter / CLI / policy pass 事实源。
- Dangerous action hard-block：用 P13E synthetic CLI evidence 证明 confirm 不能解锁 TCC reset、System Settings、Finder、App launch、command execution、launchctl 或登录项/helper 生命周期动作。
- OCR 用户编辑冲突：用 synthetic OCR state fixture 证明 retry / late completion 不覆盖 user-edited OCR。

## 是否建议接受产品经理收口输入

建议接受产品经理 Step 6 收口覆盖回扫输入进入后续角色复审与项目负责人收敛。当前 P0/P1 为 0；安全 residual 均可作为 P2 或低敏补证据项处理。
