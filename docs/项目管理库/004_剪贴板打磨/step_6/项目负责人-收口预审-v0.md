# 004_剪贴板打磨 Step 6 收口覆盖回扫预审 v0

日期：2026-07-07
角色：项目负责人
对象：产品经理收口覆盖回扫输入

## 1. 结论

结论：`approve-for-role-review`。

产品经理已产出 [Step 6 收口覆盖回扫 v0](产品经理-收口覆盖回扫-v0.md)，结论为 `coverage-ready-for-review`。项目负责人预审后判断，该文档可以进入 Step 6 角色收口复审。

本结论不代表 004 整体最终完成。Step 6 仍需角色复审、项目负责人收敛、必要验证和最终接受。

## 2. 预审通过点

- 未新增功能需求，保持 Step 6 “集成验收与收口”定位。
- 明确 `需求覆盖矩阵-v0.md` 中旧状态不作为最终事实，改以 Step 1-5 最终验收为准重新判断。
- 覆盖原始需求 R1-R13、澄清需求 C1-C17、流程需求 P1-P5。
- 保留各阶段 P2 residual，没有把真实 UI、真实剪贴板、真实 VoiceOver、真实系统枚举、真实数据库 mutation 等未覆盖项写成已实测通过。
- 提出 Step 6 需要进一步复审的集成路径，包括搜索 + 标签 + 编辑后索引、旧事实源退出、明文可访问但输出可控、OCR 生命周期、隐私 UI + CLI 单一策略事实源和 dangerous action 边界。
- 未发现产品经理文档本身存在 P0/P1 级覆盖遗漏。

## 3. 角色复审重点

### 测试/质量

- 判断 R1-R13、C1-C17、P1-P5 是否仍有 P0/P1 级遗漏。
- 给出 Step 6 最低验证矩阵和 pass/fail 门槛。
- 判断哪些 P2 residual 可以接受为最终 residual，哪些需要补低敏证据。

### UI/交互设计师

- 复审体验类 P2 residual 的分类和口径是否准确。
- 判断真实 UI / VoiceOver / 窄宽度 / 长文本 / hover / selected feedback 是否存在被错误降级的 P1。
- 给出最终体验验收应保留的发布前待办。

### App 架构师

- 复审跨阶段集成路径是否覆盖核心架构边界。
- 判断旧 pinboard / pinned / redacted / hardening fact source 退出、搜索索引、标签、详情编辑、隐私 policy fact source 是否有遗漏。
- 给出是否需要架构级补验或 verifier 清理。

### 安全合规顾问

- 复审“真实内容可访问，交互输出可控”的最终边界。
- 判断 provider、CLI、日志、自动化、系统权限、真实 App 清单、真实数据库 mutation 等 residual 是否有风险误降级。
- 确认 Step 6 是否继续禁止真实系统动作和真实敏感数据证据。

### 代码审查

- 复审 Step 6 收口路径中的旧事实源退出和 verifier evidence 是否足以支撑最终接受。
- 判断是否存在需要阻塞最终接受的代码级 P0/P1。
- 不要求重新做全量代码审查，重点看跨阶段门禁和已知 residual 是否有技术假 PASS 风险。

## 4. 边界

- 只读复审，不修改业务代码。
- 不触发真实 App、真实剪贴板、provider、Keychain、TCC、System Settings、Finder、App launch、真实系统枚举或真实系统状态变更。
- 不重新打开 Step 1-5 已接受的阶段范围，除非发现 P0/P1 级遗漏。
- 不把 P2 residual 写成已实测通过。
- 复审意见应写入 Step 6 目录的角色文档，避免只通过会话回调。

## 5. 下一步

派发测试/质量、UI/交互设计师、App 架构师、安全合规顾问和代码审查做 Step 6 收口复审。角色复审完成后，项目负责人统一收敛并决定是否需要补证据、返工、更新覆盖矩阵或进入最终接受。
