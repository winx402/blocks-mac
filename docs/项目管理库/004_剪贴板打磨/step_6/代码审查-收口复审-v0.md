# 004_剪贴板打磨 Step 6 代码审查收口复审 v0

日期：2026-07-07
角色：代码审查
范围：Step 6 收口覆盖回扫复审；只读审查，不修改业务代码，不触发真实 App / 真实系统剪贴板 / provider / Keychain / TCC / System Settings / Finder / App launch / 真实系统枚举 / 真实系统状态变更。

## 结论

结论：approve

P0/P1：0。

代码审查视角认为 Step 6 收口路径可以带已记录 P2 residual 进入项目负责人最终收敛。未发现 old pinboard / pinned / redacted / hardening、policy migration、search / tag / detail / privacy 集成层面的 P0/P1 级代码或门禁遗漏。最终接受仍由项目负责人判断。

## Findings

### P0

无。

### P1

无。

### P2 residual

1. P13E legacy migration conflict 仍是静态 + synthetic 场景证明，不是真实临时 DB harness。
   - 证据：`tools/verification/p13e_clipboard_privacy_policy_checks.py:95`-`98` 要求 `privacy_policy_legacy_excluded_bundle_conflict_preserves_current_004`；`tools/verification/p13e_clipboard_privacy_policy_checks.py:710`-`725` 验证 existing current rule 保持 allowed、legacy-only rule 变 restricted；`tools/verification/p13e_clipboard_privacy_policy_checks.py:1045`-`1055` 检查 legacy migration body 不含 `policy = excluded.policy`，且存在 `ON CONFLICT(subject_ref) DO NOTHING` / `WHERE NOT EXISTS`。
   - 代码侧：`apps/Blocks/BlocksCore/PrivacyPolicyRepository.swift:77`-`135` 的 `migrateLegacyRestrictedBundleIDs` 使用 `ON CONFLICT(subject_ref) DO NOTHING`；正常 `applyPolicy` 在 `apps/Blocks/BlocksCore/PrivacyPolicyRepository.swift:141`-`188` 仍保留 conflict update。
   - 判断：能覆盖 R3 旧回归，不阻塞 Step 6；如后续准备发布，可补一个低敏临时 DB harness 降低 residual。

2. P13D `current_evidence.dispatch` 仍指向 Step 4 通用开发派发，而非 R3 派发。
   - 证据：`tools/verification/p13d_clipboard_detail_edit_checks.py:95`-`108` 已要求 detail save / dirty guard / AppModel continuation 等关键场景；`tools/verification/p13d_clipboard_detail_edit_checks.py:1169`-`1174` 包含 `detail_appmodel_close_continuation_004`；`tools/verification/p13d_clipboard_detail_edit_checks.py:1596`-`1602` 的 current evidence 仍是 PRD、技术方案、通用 dispatch、development_record。
   - 判断：Step 4 R3 最终验收和 P13D 场景已覆盖同步 close 后继续副作用风险；dispatch 指针不够细属于证据可读性 P2，不构成假 PASS P1。

3. P13C `.label` 本地化覆盖仍非完整键级证明。
   - 判断：Step 3 R1 已关闭 detail activation manifest-only 假 PASS，P13C 现有 coverage 可防主要交互路径回退；`.label` 完整性属于可接受 P2，后续可补键级清单。

4. 真实 UI / VoiceOver / 真实系统剪贴板 / 真实 OCR / 真实 App list 与 icon / 真实 DB mutation 未触发。
   - 判断：这是本轮明确边界内的未覆盖项，不代表代码 P1。建议保留为发布前或更高信任验收的 P2 residual。

5. legacy pinboard / pinned / redacted 支持代码仍存在于 Core 或兼容结构中。
   - 证据：`apps/Blocks/BlocksCore/ClipboardRepository.swift` 仍包含 `clipboard_pinboards` / `clipboard_pinned_metadata` 相关旧仓储 API；但 active App 层过滤与搜索事实源为 tag/favorite：`apps/Blocks/BlocksApp/Support/ClipboardFilters.swift:163`-`167`、`apps/Blocks/BlocksApp/Support/ClipboardFilters.swift:238`-`253`，`apps/Blocks/BlocksApp/Stores/ClipboardController.swift:13`-`20`，`apps/Blocks/BlocksApp/Features/Clipboard/ClipboardSearchCoordinator.swift:60`-`86`。
   - 判断：旧代码未回到 active current fact source；继续作为 legacy/baseline residual 可接受。

## 关键证据

旧事实源退出：

- `ClipboardFilterState` 当前只有 format / time / selectedTagID / sourceBundleIdentifier，clear-all 会清 `selectedTagID`：`apps/Blocks/BlocksApp/Support/ClipboardFilters.swift:238`-`253`。
- active filter count 使用 `selectedTagID`，无 `pinboardID`：`apps/Blocks/BlocksApp/Stores/ClipboardController.swift:13`-`20`。
- 搜索过滤用 `recordTags` + `selectedTagID`，无旧 pinboard fact：`apps/Blocks/BlocksApp/Features/Clipboard/ClipboardSearchCoordinator.swift:60`-`86`。
- `ClipboardStore` 加载 recent + preview snapshots 后清 payload cache、加载 tagStore、刷新 search：`apps/Blocks/BlocksApp/Features/Clipboard/ClipboardStore.swift:56`-`79`。
- `ClipboardStore.repositoryStateSummary()` 返回 `.normal(recordCount:)`，未把 redacted-first state 作为 active repository summary：`apps/Blocks/BlocksApp/Features/Clipboard/ClipboardStore.swift:100`-`108`。
- full payload 读取仍是 explicit purpose path：`apps/Blocks/BlocksApp/Features/Clipboard/ClipboardStore.swift:138`-`161`。
- 收藏与清理路径使用 favorite / tagStore，而非 pinned：`apps/Blocks/BlocksApp/Features/Clipboard/ClipboardStore.swift:329`-`339`、`apps/Blocks/BlocksApp/Features/Clipboard/ClipboardStore.swift:360`-`429`。

搜索 / 标签 / 详情一致性：

- 非空搜索走 repository search document，再由 coordinator 应用当前过滤：`apps/Blocks/BlocksApp/Features/Clipboard/ClipboardStore.swift:216`-`267`。
- detail save 在同一事务中更新 record content metadata、构建含 tag / OCR / contentRevision 的 search document 并 upsert：`apps/Blocks/BlocksCore/ClipboardRepository+DetailEdit.swift:183`-`227`。
- tag search document repair / dirty path 会补建缺失 search document 或替换 tag tokens：`apps/Blocks/BlocksCore/ClipboardRepository+SearchDocuments.swift:353`-`368`。
- OCR late completion / retry 会拒绝 redacted derivation、userEdited 或 locked revision：`apps/Blocks/BlocksCore/ClipboardRepository+SearchDocuments.swift:371`-`409`。

detail / paste / translation dirty guard：

- paste 先通过 `clipboardHistoryPanelPresenter.close(afterClose:)`，continuation 内重新取当前 record 并按 `.paste` purpose 读 payload：`apps/Blocks/BlocksApp/App/AppModel.swift:321`-`371`。
- translation panel 同样通过 `close(afterClose:)` 后进入 continuation：`apps/Blocks/BlocksApp/App/AppModel.swift:518`-`540`。

privacy / capture policy：

- AppModel live capture 使用 `privacyStore.policySnapshot` 生成 `ClipboardCapturePolicy`：`apps/Blocks/BlocksApp/App/AppModel.swift:213`-`217`、`apps/Blocks/BlocksApp/App/AppModel.swift:247`-`254`。
- capture snapshot path hash 优先于 bundle ID，restricted 优先于 allowed 的同类型判断顺序清晰：`apps/Blocks/BlocksCore/PrivacyPolicyModels.swift:171`-`188`。
- legacy excluded bundle migration 只补缺失规则，不覆盖已有 current rule：`apps/Blocks/BlocksCore/PrivacyPolicyRepository.swift:77`-`135`。
- 正常 policy mutation 未被误伤，仍可通过 `applyPolicy` 更新现有 subject policy：`apps/Blocks/BlocksCore/PrivacyPolicyRepository.swift:141`-`188`。

Verifier fail-closed 证据：

- P11E 继续覆盖 denied default sites、`filtered_records_no_pinned_display_name`、repository summary normal state 和 baseline 不参与 ok：`tools/verification/p11e_clipboard_hardening_checks.py:210`-`228`。
- P13B 检查 `ClipboardFilterState` 不含 `pinboardID`，search coordinator 使用 `recordTags` / `selectedTagID`，active UI/store 禁止旧 pinned/pinboard token，并明确旧 storage 仅 baseline allowed：`tools/verification/p13b_clipboard_tags_model_checks.py:237`-`270`、`tools/verification/p13b_clipboard_tags_model_checks.py:330`-`372`。
- P13D 覆盖 detail close continuation 与 detail save/dirty guard 场景，baseline story/archive 不参与 ok：`tools/verification/p13d_clipboard_detail_edit_checks.py:95`-`108`、`tools/verification/p13d_clipboard_detail_edit_checks.py:1169`-`1174`、`tools/verification/p13d_clipboard_detail_edit_checks.py:1596`-`1607`。
- P13E 要求顶层 `ui_interaction` / `capture_bridge` / `performance` schema，implementation evidence 覆盖 Swift store search、capture policy、CLI、legacy migration、icon provider，并要求低敏输出扫描：`tools/verification/p13e_clipboard_privacy_policy_checks.py:106`-`110`、`tools/verification/p13e_clipboard_privacy_policy_checks.py:919`-`927`、`tools/verification/p13e_clipboard_privacy_policy_checks.py:1028`-`1074`、`tools/verification/p13e_clipboard_privacy_policy_checks.py:1120`-`1155`。

## 已运行命令

低敏 verifier / 静态命令：

- `python3 tools/verification/p13a_clipboard_plaintext_search_ocr_checks.py`：通过，`ok=True`，`failures=0`。
- `python3 tools/verification/p13b_clipboard_tags_model_checks.py`：通过，`ok=True`，`failures=0`。
- `python3 tools/verification/p13c_clipboard_panel_interaction_layout_checks.py`：通过，`status=pass`，`failures=0`。
- `python3 tools/verification/p13d_clipboard_detail_edit_checks.py`：通过，`status=pass`，`failures=0`，`scenarios=31`。
- `python3 tools/verification/p13e_clipboard_privacy_policy_checks.py`：通过，`status=pass`，`failures=0`，`scenarios=37`。
- `python3 tools/verification/p11e_clipboard_hardening_checks.py`：通过，`ok=True`，`failures=0`。
- `python3 tools/verification/p8_clipboard_product_polish_checks.py`：通过，`ok=True`，`failures=0`。
- `python3 tools/verification/p8i_settings_clipboard_system_checks.py`：通过，`ok=True`，`failures=0`。
- `python3 tools/verification/p9a_clipboard_repository_storage_smoke.py`：通过，`ok=True`，`failures=0`，`schema_version=5`。
- `python3 tools/verification/p9b_clipboard_appstate_repository_integration_checks.py`：通过，`ok=True`，`failures=0`。
- `python3 tools/verification/p9c_no_reset_fixtures_ui_checks.py`：通过，`ok=True`，`failures=0`。
- `git diff --check`：通过，无输出。
- `git diff --check -- docs/项目管理库/004_剪贴板打磨/step_6/代码审查-收口复审-v0.md`：通过，无输出。
- `rg -n "pinboard|pinned|pinnedCount|Pinned|Pinboard" apps/Blocks tools/verification -g '*.{swift,py}'`：确认旧 token 集中在 Core legacy storage / baseline verifier / 支持结构，未发现 active AppModel / ClipboardStore / filter / search current fact source 回流。
- `rg -n "searchableText|redactedPreview\\(" apps/Blocks/BlocksApp apps/Blocks/BlocksCore -g '*.swift'`：确认旧 redacted preview/search helper 仍存在于支持代码；结合 P13A/P11E 当前 search/read-model gate，未发现其回流为 active search fact source。

## 未覆盖风险

- 未进行真实 App launch、真实系统剪贴板写读、真实 OCR、真实 provider、真实 Keychain / TCC / System Settings / Finder、真实系统 App 枚举或真实系统状态变更。
- 未做真实 VoiceOver / 辅助功能手感验收。
- 未做 P13E legacy conflict 的真实临时 DB mutation harness，仅依赖静态 + synthetic 证据。
- 未做 P13C `.label` 本地化完整键级清单证明。

这些均按当前任务边界判断为 P2 residual，不阻塞 Step 6 代码审查通过。

## 建议口径

- 建议项目负责人接受 Step 6 代码审查结论：approve，P0/P1 为 0。
- 旧 pinboard / pinned / redacted / hardening 只允许继续作为 legacy/baseline residual，不应重新进入 AppModel / Store / filter / search / verifier active current fact source。
- 后续如果进入发布前质量门禁，可优先补：P13E 临时 DB migration conflict harness、P13D current evidence 指针细化、P13C `.label` 键级检查、真实 UI / VoiceOver / 低敏系统交互验收。
