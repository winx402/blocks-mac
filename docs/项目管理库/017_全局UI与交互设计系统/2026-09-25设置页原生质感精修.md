# 设置页原生质感精修

日期：2026-09-25。范围仅限设置窗口；未提交、推送或发布版本。

## 后续确认：所有一级菜单统一大图标概览

同日用户要求所有菜单顶部都显示大图标。现已将所有侧栏一级分类统一接入40pt图标、标题和一句说明组件；插件目录与翻译收藏的独立布局也已补齐，二级详情页仍不重复显示分类概览。此要求替代此前“只有四个一级页有概览”的约定。

通过`./script/dev.sh run`更新本机安装，实机检查快捷键、翻译收藏、插件、权限、AI服务、本地自动化、数据与审计和通用入口；21项导航与侧栏回归测试、设计系统门禁通过。测试结果：`/Users/bot/Library/Developer/Xcode/DerivedData/Blocks-cosqiniawngrfkbaivzhpqaurliz/Logs/Test/Test-BlocksAppTests-2026.09.25_23-36-09-+0800.xcresult`。本次新增概览未重新穷举三语言与辅助显示矩阵；相关基础组件沿用下述已验证实现。

## 实施

- 保留分类顺序、30pt 行高、20pt 图标和10pt无标题分组留白。
- 设置专属图标采用12–14pt光学尺寸、轻微明暗及轮廓；侧栏与概览共用绘制组件，浮窗图标不变。
- macOS 26使用 NavigationSplitView 自带侧栏材质，移除自定义材质叠加；早期系统保留现有兼容背景。
- 表单最大宽度640pt，内容浏览器1120pt和Sheet640pt不变。
- 快捷键和隐私页移除无信息增量的重复正文标题。
- 快捷键视觉显示使用紧凑修饰键符号，辅助功能标签仍使用完整名称；详细统计与验收说明默认折叠，失败仍外显。
- 开发版及测试宿主不支持官方更新属于中性信息；配置错误和运行失败仍为警告。

## 验证结果

最终代码完成后，DebugTesting build-for-testing及33项针对性XCTest通过，0失败。覆盖侧栏几何/滚动、图标及选中文字契约、背景、导航/滚动/草稿、分组对齐和更新状态分类。

最终测试结果：`/Users/bot/Library/Developer/Xcode/DerivedData/Blocks-aqqjrhpitijmabasqqqrahnzyhvq/Logs/Test/Test-BlocksAppTests-2026.09.25_20-38-49-+0800.xcresult`。

`./script/test_ui_design_system.sh`、本地化JSON检查及`git diff --check`通过。仓库Run入口`./script/dev.sh run`已原地更新`~/Applications/Blocks.app`，保持`app.blocks.dev`和固定证书；未创建第二份日常安装。

本机macOS 26实测：

- 中文默认深色、英文浅色、日文深色；980pt默认窗口、820pt窄窗口，菜单文字和控件未见裁切。
- 冷启动、激活/失焦、主题切换；初次检查发现非选中图标缺失，增加图层支持后冷启动复验完整显示。
- 增强对比度（系统同时启用降低透明度）、单独降低透明度；发现选中文字过淡后改用系统选中文字色，复验清晰。
- 鼠标、方向键切换、`⌘[`/`⌘]`、诊断展开/收起正常。
- 剪贴板父页滚动至底部后进入隐私页；输入临时搜索词，后退恢复父页底部，前进恢复搜索词。随后清空测试查询，未变更任何捕获策略。
- 辅助功能树中的快捷键仍为完整`Control + Option + A`等名称，录入/恢复/开关名称保留。

## 证据与边界

改动前后同为中文、深色、激活状态、980pt窗口的截图，以及英文/日文/辅助显示截图，已在本次会话中展示。未保存独立截图文件，不以本文替代原始截图。

测试后已恢复语言和外观为“跟随系统”，窗口恢复原尺寸；增强对比度和降低透明度均恢复原先关闭状态。最终停留在快捷键设置页。

未进行VoiceOver实际语音听读、macOS 14/15实机运行或完整无障碍合规评估；辅助功能名称验证仅来自实际AX树与测试。没有修改权限、安全策略、数据格式或更新行为。用户原有AGENTS.md改动保留未动。
