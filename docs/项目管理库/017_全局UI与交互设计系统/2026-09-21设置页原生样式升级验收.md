# 设置页原生样式升级验收（2026-09-21）

## 范围

按用户提供的 macOS 系统设置截图升级设置窗口。保留既有路由、设置绑定、权限与数据语义；不新增全局搜索，不调整截图、剪贴板和翻译浮窗。

## 实现与证据

| 要求 | 实现及本轮证据 |
| --- | --- |
| 背景层级 | 设置专属画布与分组底色；浅色系统语义色同为白色时，画布增加轻微自适应明度差。实机深浅色预览均可区分分组。 |
| 侧栏 | 22pt 彩色圆角图标，28pt 行高，原生选中态；中英日文窄窗口未见截断。图标使用独立容器，避免 NSTableCellView 拉伸图标底色。 |
| 页面标题 | 一级页面图标、标题、简介；收藏保留紧凑标题和独立列表布局。水印、AI 配置、标签、插件详情和翻译来源的二级页保留返回入口，不重复一级大标题。 |
| 分组与密度 | 10pt 分组圆角，18pt 页内分组间距，40pt 最小行高；无反馈时不保留54pt空白区域。最新剪贴板预览已确认空白消失。 |
| 行对齐与文字 | 760pt 表单最大宽度、24pt 横向边距、160–300pt 控件列；说明采用 callout。980pt 和820pt宽度实机检查，长文案自然换行。 |
| 原生交互 | 未替换原生控件或导航状态。AI 配置文本框 Tab 到下一项、Shift+Tab 返回上一项均通过 AX 焦点确认；返回导航通过实际点击验证。 |
| 状态反馈 | 普通信息使用次级文本色，警告和错误保留语义色；权限未授予、更新测试环境禁用、空列表及输入校验提示均在预览中检查。没有发起授权、外部请求、删除或反馈提交。 |
| 通用页顺序 | 应用外观与语言、版本与更新、反馈、高级诊断；诊断默认折叠。实际通用页 AX 顺序确认。 |
| 特殊页面 | 收藏独立列表、插件独立滚动容器均保留，无新增嵌套 ScrollView；实机查看截图、剪贴板、翻译、收藏、快捷键、权限、AI、本地自动化、插件、数据审计、通用全部一级入口。 |
| 主题、语言及辅助功能 | 实机检查深浅色、中文、英文和日文代表性页面，以及820pt窄窗口；原生输入焦点通过。增强对比度通过语义边界及交互样式的组件测试验证，未修改本机系统辅助功能偏好，不声称已执行全系统增强对比度人工验收。 |

## 自动检查

- `script/test_ui_design_system.sh` 通过；未移除门禁，规范参数同步到新设计值。
- 定向测试覆盖 `SettingsWindowBackingTests`、`SettingsNavigationRowSurfaceTests`、`SettingsNavigationInteractionTests`、`AppAppearanceTests`：123通过，0失败，0跳过。
- 结果：`/Users/bot/Library/Developer/Xcode/DerivedData/Blocks-bfwdnuxaiqxpdyhajvbdfggzoedq/Logs/Test/Test-BlocksAppTests-2026.09.21_01-43-14-+0800.xcresult`。
- 包含浅色画布与白色分组的差异、设置样式环境默认关闭、标题栏背景覆盖、导航/焦点契约、空反馈不占位、增强对比度语义边界等回归。

## 验收环境与边界

实机为 Apple Silicon、macOS26.6.2。使用当前源码 DebugTesting 构建的隔离预览，独立 Bundle ID、偏好和数据目录；未替换用户已安装应用、未改变用户系统主题或语言、未重置权限。本轮未发布安装包，也未进行签名公证验收。

英文/日文检查证明本轮布局及新增标题简介适配，不代表历史插件元数据已全部翻译；现有内置插件名称和描述仍可能保留中文。没有将空数据预览当作所有真实业务数据组合的穷尽验收。
