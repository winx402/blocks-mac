# Step 4C P1 回写测试/质量轻量复审 v0

- 角色：测试/质量
- 日期：2026-07-06
- 结论：`p1-closed-with-notes`
- 范围：只确认上一轮测试/质量 P1 是否已在 PRD 层面回写关闭；不做实现验收，不运行会触发系统动作的验证。

## 1. 复审依据

已读取：

- `docs/项目管理库/003_架构升级/step_4/PRD-Step4C-剩余Feature收口-v0.md`
- `docs/项目管理库/003_架构升级/step_4/2026-07-06-测试质量Step4C方案复审-v0.md`

本轮未改代码、未创建分支、未提交 commit，未触发真实权限请求、系统设置、Show in Finder、restart 或其他系统动作。

## 2. 上一轮 P1 关闭情况

### P1-1 子批次 stop/go gate

状态：已关闭。

PRD 已明确：

- Step 4C 开发必须按子批次推进。
- 每个子批次有独立开发记录、测试/质量验收记录、必要角色复审和主 agent stop/go 记录。
- 前一子批次 P0/P1 未关闭前，不得启动后一子批次开发。
- 若同一开发线程连续承接多个子批次，必须按文件变更、命令、低敏证据和残余风险清楚分段，否则测试/质量可拒绝合并验收。

### P1-2 P11A/C/D/E 可执行检查清单

状态：已关闭。

PRD 已补齐：

- P11 通用 fail-closed 要求：脚本缺失、target membership 无法解析、路径缺失、旧事实源当阻断证据、检查静默跳过均失败。
- P11 输出要求：checked files、target membership source、规则命中 / 未命中摘要和失败原因。
- P11A ScreenshotStore 最低检查清单。
- P11C ShortcutStore 最低检查清单。
- P11D Settings shell split 最低检查清单。
- P11E Clipboard hardening 最低检查清单和 read model allowlist / denylist 摘要要求。

说明：这是 PRD 层面关闭；P11A/C/D/E 脚本实现质量仍需在对应子批次开发验收时独立验证。

### P1-3 旧 P3/P6/P7/P8/P9 当前事实源处理

状态：已关闭。

PRD 已明确历史门禁当前事实源规则：

- Step 4C 阻断矩阵中使用旧 P3/P6/P7/P8/P9 脚本时，开发前必须迁移到当前 Step 4C PRD、开发记录、验收记录或当前代码事实源。
- 仍读取旧归档、旧 story、旧 acceptance，或依赖旧 AppState / SettingsView 字符串形态的检查，只能作为 `baseline_reference`，不得参与 `ok` 判定。
- 已点名需处理的候选包括 `p7q_screenshot_window_fullscreen_checks.py`、`p8_clipboard_product_polish_checks.py`、P3D/P3E/P3F、P6A/P6B/P6C、P8G、P9B。

### P1-4 Clipboard hardening stop/go / Step 4D 拆分

状态：已关闭。

PRD 已调整为 conditional 4C-4：

- Step 4C core 为 ScreenshotStore、ShortcutStore、Settings shell split。
- Clipboard hardening 只有 go/no-go 通过时才留在 Step 4C。
- 进入 4C-4 前必须完成 go/no-go 评估。
- 若 P11E、payload allowlist / denylist、UX contract 或安全/质量门禁无法闭合，则拆为 Step 4D，并写 Step 4D handoff record。
- Step 4C 最终接受记录不得暗示拆出的 Clipboard hardening 已完成。

## 3. 新增 P0/P1

P0：未发现。

P1：未发现。

## 4. Notes

- 本轮只确认 PRD 回写是否关闭测试/质量 P1；不代表 P11A/C/D/E 脚本已经存在或已经通过实现验收。
- 历史门禁的实际迁移仍必须在对应子批次开发前完成，并在验收记录中证明旧事实源未参与 `ok` 判定。
- Clipboard hardening 若进入 Step 4C，仍需单独 go/no-go、P11E、UX、安全和测试证据闭合；否则应按 PRD 拆到 Step 4D。

## 5. 建议

建议主 agent 提交 PRD 阶段文档并进入开发准备。开发仍应严格按 4C-1 / 4C-2 / 4C-3 / conditional 4C-4 子批次 stop/go 执行，测试/质量不替主 agent 接受后续实现残余风险。
