# Step 4D Clipboard Hardening UI P1 修复补充复审

日期：2026-07-06
角色：UI/交互设计师
结论：`p1-closed`

## 1. 范围与限制

本轮只复审上一份 UI/交互实现复审中提出的 P1：

- `ClipboardController.filteredRecords` 仍把 historical pinned `displayName` 拼入 searchable text。

本轮不改业务代码，不创建 branch / commit，不运行真实 App，不触发真实剪贴板、TCC、provider、Keychain、系统设置、Show in Finder 或 restart。未做真实 UI 截图、VoiceOver、hover、copy、paste 或 translation preview 实测。

已读 / 已核对：

- `apps/Blocks/BlocksApp/Stores/ClipboardController.swift`
- `tools/verification/p11e_clipboard_hardening_checks.py`
- `docs/项目管理库/003_架构升级/step_4/开发记录-Step4D-ClipboardHardening-v0.md`
- `docs/项目管理库/003_架构升级/step_4/2026-07-06-UI交互Step4D实现复审-v0.md`

## 2. P1 关闭判断

上一轮 P1 已关闭。

事实依据：

- `ClipboardController.filteredRecords` 的 searchable text 已移除 `pinnedMetadata[record.id]?.displayName ?? ""`。
- 当前 searchable text 保留 `preview.searchableText`、`record.redactedSearchableText(...)` 和 pinboard name；默认 redacted title / search 不再直接信任 historical pinned displayName。
- `p11e_clipboard_hardening_checks.py` 已新增 `CLIPBOARD_CONTROLLER` 检查，并在 `filteredRecords` body 中发现 `displayName` 时 fail closed。
- 本轮复跑 `python3 tools/verification/p11e_clipboard_hardening_checks.py`，结果为 `ok=true`、`failures=[]`，且 `denied_default_sites.filtered_records_no_pinned_display_name=true`。
- 开发记录已补充说明：历史 pinned `displayName` 不再作为默认 redacted title、redacted searchable text 或 `ClipboardController.filteredRecords` 搜索输入。

## 3. P0 / P1 / P2

### P0

无。

### P1

无。UI/交互侧 P1 已清零。

### P2

以下 P2 仍建议进入最终接受记录或后续 polish，不阻断 Step 4D 接受：

1. Clipboard panel 在 repository unavailable 且 records 非空时，仍需要低敏实物证据证明用户能理解 storage degraded；必要时后续补 header / strip 级轻量提示。
2. Translation preview explicit read failure 当前反馈偏弱；建议最终验收覆盖失败态，或后续补 “content unavailable, preview uses metadata only” 类状态。
3. Settings hardening allowlist / redacted policy 文案清楚但较长，需要窄宽度、三语言、VoiceOver reading order 低敏实物验收。
4. `ClipboardHistoryView` 仍显示 `record.summary`，当前未发现主路由直接实例化；若未来复用，必须先改成 redacted preview 或删除。
5. Hover loading / unavailable 文案存在，payload cache 已按 `(recordID, purpose)` keyed；真实 hover 退出、panel close、repository reload、record delete 后残留体感仍需低敏实物验收。

## 4. 运行命令

本轮运行：

- `python3 /Users/bot/.codex/plugins/cache/openai-curated-remote/product-design/0.1.47/skills/user-context/scripts/user_context_preflight.py`
- `sed` / `rg` 静态读取相关 Swift、P11E、开发记录和上一份 UI 复审文档。
- `python3 tools/verification/p11e_clipboard_hardening_checks.py`
- `git diff --check`

结果：

- Product Design user context：missing，无保存上下文可用。
- P11E：`ok=true`，`failures=[]`，`filtered_records_no_pinned_display_name=true`。
- `git diff --check`：无输出。

未运行：

- 未运行真实 App。
- 未运行 P8 / P9B / build。
- 未触发真实剪贴板、TCC、provider、Keychain、系统设置、Show in Finder 或 restart。

## 5. 对主会建议

UI/交互侧建议将 Step 4D UI P1 标记为已关闭。若其他角色无 P0/P1，UI/交互侧不再阻断主会进入 Step 4D 最终接受判断。

最终接受记录仍应保留本轮未实测的低敏实物验收范围：redacted list、repository degraded、empty / unavailable / filtered / redacted、hover lazy read、copy / paste / translation feedback、Settings 文案、窄宽度、多语言和 VoiceOver。
