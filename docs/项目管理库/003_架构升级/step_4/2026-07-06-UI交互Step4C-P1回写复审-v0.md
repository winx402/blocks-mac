# Step 4C UI/交互 P1 回写轻量复审 v0

日期：2026-07-06
角色：UI/交互设计师
结论：p1-closed-with-notes

## 1. 复审范围

本次只核对上一轮 UI/交互方案复审提出的 P1 是否已在 `PRD-Step4C-剩余Feature收口-v0.md` 中回写关闭，并确认是否出现新增 P0/P1。

已读取：

- `docs/项目管理库/003_架构升级/step_4/PRD-Step4C-剩余Feature收口-v0.md`
- `docs/项目管理库/003_架构升级/step_4/2026-07-06-UI交互Step4C方案复审-v0.md`

未做：

- 未读取代码。
- 未运行 App。
- 未创建 branch / commit。
- 未触发真实权限请求、系统设置、截图、快捷键或剪贴板系统动作。

## 2. 上一轮 P1 关闭情况

### P1-1：Screenshot Result 最小体验验收不完整

状态：已关闭。

回写证据：

- PRD 第 3.1 节继续要求 `ScreenshotResultView` 的 copy / save / retake / close、outcome banner、blocked detail、no-upload / provider-not-called 文案不丢失。
- PRD 第 8 节 Screenshot 最小实物证据已补充：
  - copy success / copy failure
  - save success / save cancelled / save failure
  - retake 后状态更新
  - close 后主界面状态保持
  - recent captures 更新、空态 / 列表态
  - outcome banner 可见且不遮挡核心操作
  - AI route preview ready / blocked
  - localized blocked detail
  - provider profile
  - confirmation level
  - dependency note 或等价信息
  - no-upload / provider-not-called 用户可见承诺
  - 低敏桌面、低敏窗口标题和低敏图片证据要求

判断：覆盖了上一轮 P1 所要求的体验验收口径，可以关闭。

### P1-2：Clipboard hardening 缺少具体用户反馈契约

状态：已关闭。

回写证据：

- PRD 第 0 节已将 Clipboard hardening 改为 conditional 4C-4，并要求进入 4C-4 前完成 go/no-go；若 P11E、payload allowlist / denylist、UX contract 或安全/质量门禁无法闭合，则拆为 Step 4D。
- PRD 第 3.4 节新增 Clipboard hardening UX contract，覆盖：
  - `repositoryUnavailable` 在 Clipboard 面板和 Settings 至少一个位置解释 storage degraded / repository unavailable。
  - 区分无历史、存储暂不可用、历史不可写入、复制/粘贴仍可用但历史降级。
  - redacted list 只展示 metadata / redacted preview / kind / source / timestamp / pin state 等低敏信息。
  - redacted 占位文案必须让用户理解内容被保护，不是内容丢失。
  - paste、copy、hover detail、translation preview 作为 explicit payload read 路径。
  - empty / unavailable / filtered / redacted 四类状态必须可区分。
  - 如果不新增 Settings UI，必须说明 Clipboard 面板中的 degraded 反馈位置。
- PRD 第 8 节 Clipboard hardening 最小实物证据也补充了 repository unavailable、redacted list、empty / unavailable / filtered / redacted、payload allowlist 和 Step 4D handoff 条件。

判断：覆盖了上一轮 P1 所要求的用户反馈契约，可以关闭。

## 3. P2 回写观察

上一轮 P2 也基本被回写为开发 / 验收门槛：

- Shortcut invalid shortcut、Esc / cancel recording、切换行、切换 pane、关闭 Settings 后停止监听、restore default / restore all defaults 状态同步已进入第 8 节 Shortcut 最小证据。
- Settings 窄宽度、长文本、长路径、长 bundle ID、长 recommended action、中文 / 英文 / 日文长句和 VoiceOver label 已进入第 8 节 Settings 最小证据。
- 子批次 hard stop/go 已进入第 4 节和第 9 节。
- Clipboard hardening 可条件拆 Step 4D，并要求 handoff record，不再阻断 Step 4C core closeout。

这些回写足以消除上一轮 UI 复审中的主要执行歧义。

## 4. 新增 P0/P1

新增 P0：无。

新增 P1：无。

非阻断注意事项：

- 本次只做文档轻量复审，未验证 P11 脚本实际可执行性。
- 低敏截图、VoiceOver、长文本、多语言、快捷键录制和 Clipboard redacted / unavailable 状态仍需在开发与测试阶段提供实证。
- Clipboard hardening 是 conditional 4C-4；如果 go/no-go 后拆出 Step 4D，Step 4C 最终接受记录必须明确它未被接受。

## 5. 是否建议进入开发准备

建议：可以提交 PRD 阶段文档并进入 Step 4C 开发准备。

条件：

- 主 agent 先提交 PRD、复审文档和阶段状态更新。
- 开发必须按 4C-1 / 4C-2 / 4C-3 / conditional 4C-4 子批次推进。
- 前一子批次 P0/P1 未关闭前，不启动后一子批次。
- Clipboard hardening 在进入 4C-4 前必须完成 go/no-go；无法闭合时拆到 Step 4D。

最终结论：p1-closed-with-notes。
