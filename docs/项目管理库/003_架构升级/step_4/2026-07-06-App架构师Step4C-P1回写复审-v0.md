# 2026-07-06 App 架构师 Step4C P1 回写复审 v0

结论：`p1-closed-with-notes`

复审角色：App 架构师

复审范围：仅对照 `2026-07-06-App架构师Step4C方案复审-v0.md` 中提出的 App 架构 P1，确认 `PRD-Step4C-剩余Feature收口-v0.md` 的回写是否关闭 P1，以及是否引入新增 P0/P1。本次未改代码，未创建 branch / commit，未触发系统权限请求。

## 1. 总体结论

上一轮 App 架构复审提出的 3 项 P1 已关闭。未发现新增 P0/P1。

建议主 agent 可以提交 PRD 阶段文档，并进入开发准备；开发仍必须按 PRD 中的子批次 stop/go 机制推进，不应把 Step 4C 压成一次性实现。

## 2. P1 关闭情况

### P1-1：Step 4C 接受范围与 Clipboard hardening 可拆 Step 4D 的口径冲突

状态：已关闭。

事实依据：

- PRD 第 0 节已明确 `Step 4C core scope` 是 ScreenshotStore、ShortcutStore、Settings shell split。
- Clipboard hardening 已改为 `conditional 4C-4`，进入前需要 go/no-go。
- 若 P11E、payload allowlist / denylist、UX contract 或安全/质量门禁无法闭合，PRD 要求拆为 Step 4D 并写 handoff record，不阻断 Step 4C core closeout。
- PRD 第 9 节的可接受标准已同步：4C-4 若进入 Step 4C 才要求完成；若拆出 Step 4D，必须写 Step 4D handoff record，且 P11E 不得写成 Step 4C 已通过。

判断：接受范围与拆分条件已统一。

### P1-2：P11A/C/D/E 缺少每个门禁的具体职责矩阵

状态：已关闭。

事实依据：

- PRD 第 7.1 节已分别补充 P11A ScreenshotStore、P11C ShortcutStore、P11D Settings shell split、P11E Clipboard hardening 的最低检查清单。
- P11A 覆盖 target membership、AppState screenshot facts、objectWillChange、权限输入、result presenter / route resolver、provider/image upload/base64/log 禁止项、Screenshot Result 行为入口。
- P11C 覆盖 target membership、`shortcutRegistrationResults` 迁移、AppShell 声明动作闭包、action allowlist、禁止 shell / AppleScript / CGEvent / CLI / Accessibility 自动化、single recorder state。
- P11D 覆盖 Settings feature 文件、`SettingsView` wrapper 化、`SettingsViewMode` 全量保留、mode mapping、setting key、pane local state、敏感 token 禁止项。
- P11E 覆盖 repository unavailable degraded state、默认列表不读完整 payload、完整 payload allowlist、helper / App Group / CLI 禁止项、验证输出低敏和 read model allowlist / denylist 摘要。
- PRD 还补充了旧 P3/P6/P7/P8/P9 历史门禁的 current evidence path / baseline_reference 规则。

判断：已具备交给开发实现 P11A/C/D/E 的结构 contract。

### P1-3：Settings shell 必增 pane 列表与 mode 列表不一致

状态：已关闭。

事实依据：

- PRD 第 3.3 节必增文件列表已加入 `HooksSettingsPane.swift` 和 `DataAuditSettingsPane.swift`。
- PRD 明确 `hooks` 映射 `HooksSettingsPane`，`dataAudit` 映射 `DataAuditSettingsPane`。
- P11D 最低检查清单也要求检查这些文件存在、进入 target，并检查 mode mapping。

判断：pane 文件列表与 mode mapping 已闭合。

## 3. 新增 P0 / P1

新增 P0：无。

新增 P1：无。

本次回写没有引入新的 App 架构阻断项。

## 4. 非阻断备注

- P11A/C/D/E 的职责矩阵已经足够进入开发准备，但脚本实现阶段仍需由测试/质量确认 fail-closed 行为真实可执行，特别是旧门禁 current evidence path 与 baseline_reference 的处理。
- Clipboard hardening 已被正确降级为 conditional 4C-4；如果 go/no-go 判定为 no-go，主 agent 需要确保 Step 4D handoff record 先写入，再关闭 Step 4C core。
- Step 4C 涉及多个子批次，后续每个子批次完成后仍建议按影响范围触发 App 架构师复审，而不是只在 Step 4C 末尾统一复审。

## 5. 主 agent 建议

建议主 agent 提交 PRD 阶段文档，并进入开发准备。

建议进入开发准备的前提口径：

- Step 4C core：ScreenshotStore、ShortcutStore、Settings shell split。
- Clipboard hardening：conditional 4C-4，进入前必须 go/no-go；无法闭合则拆 Step 4D。
- 开发方式：按子批次 stop/go，不一次性实现四个切片。

阻断项：无。
