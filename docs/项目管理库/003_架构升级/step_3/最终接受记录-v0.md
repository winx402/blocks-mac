# Step 3 核心状态拆分与 Provider 契约最终接受记录 v0

状态：accepted
日期：2026-07-05
来源级别：main agent acceptance

## 1. 接受结论

主 agent 最终接受 Step 3。

接受范围限定为：Provider / Translation 状态迁入 feature stores、`AppState` 降级为 Provider / Translation facade 与 AppShell coordinator、`.llmBacked` route/runtime contract 统一，并由 P10A / P10B / P5O / P5P / P9B 等门禁覆盖。

本接受不表示 SettingsView、Screenshot、Permission、Shortcut、Clipboard hardening、真实 provider 集成、真实 OCR、图片外发、helper / App Group / CLI 完整 payload 能力已完成。

## 2. 接受依据

- 开发记录结论为 `DONE`。
- 测试/质量验收结论为 `accepted`。
- App 架构师最终实现复审结论为 `approve-for-acceptance`。
- App 架构师复审未发现 P0 / P1。
- 测试/质量记录显示指定验证命令均通过。
- App 架构师复跑 P10A、P10B、P9B、P5O 均通过。

## 3. 已接受成果

- `ProviderStore` 与 `TranslationStore` 已进入 feature boundary 和 Xcode target。
- `AppState` 不再直接声明 PRD FR-3 中 16 个 Provider / Translation `@Published` 事实源。
- `TranslationStore` 不强持有 `ProviderStore`，通过 `auditRecorder` 和 `readProviderSecret` 闭包协作。
- Provider audit 只有 `ProviderStore.providerAuditEvents` 一个事实源。
- `.llmBacked` 在 runtime configured + alias + external confirmed 下 route ready；missing configuration、missing secret、confirmation required 正确阻断。
- route check 不读 Keychain、不访问网络、不执行 CLI。
- gate false、missing alias、missing base URL、missing model、invalid URL、非 loopback HTTP 均为 0 Keychain read / 0 network call。

## 4. 转入 Step 4 的风险

- `AppState` 仍保留 Screenshot、Permission、Shortcut、Settings shell、clipboard panel / paste 等跨 feature 协调入口。
- `SettingsView` 仍是大型聚合 View。
- `providerRouteResolution` 分别存在于 ProviderStore 和 TranslationStore，AppState facade 做合并投射；Step 4 拆 Settings / Translation pane 时需要明确 UI 使用哪个 route state。
- Clipboard `repositoryUnavailable` 产品化、redacted list + lazy payload 仍未解决。
- helper / App Group / CLI 完整 payload 读取仍是未来能力，不能写成已具备。
- 真实 provider 兼容性、真实 API key 生命周期、真实网络失败形态不是 Step 3 目标，启用前需要单独验收。
- `FloatingPanelSupport.swift` main actor isolation build warnings 不阻断 Step 3，但建议在 UI / AppShell 阶段清理。

## 5. 下一步

进入 Step 4A rebaseline，不直接进入 Step 4 开发。Step 4A 需要基于 Step 3 accepted 后的真实代码和复审意见，决定 Step 4B 的最小可开发切片。
