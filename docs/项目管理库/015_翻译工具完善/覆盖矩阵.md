# 翻译工具覆盖矩阵

日期：2026-07-31

状态说明：

- `自动化通过`：最后工作树对应的定向测试或产物审计通过。
- `预最终`：证据早于最后源码改动，或只覆盖隔离场景，最终必须重跑。
- `代码存在，安装版待验`：实现与测试存在，但没有当前安装版真实证据。
- `未闭合`：测试失败、跳过、缺少权限/凭据/平台，或尚未执行。

## 功能覆盖

| 能力 | 当前代码事实 | 自动化/产物证据 | 安装版/外部证据 | 结论 |
| --- | --- | --- | --- | --- |
| 手动输入与自动翻译 | 统一 Panel Model、revision 防抖和 AppKit 输入通知桥接存在；delegate 被重置时会主动修复，并抑制重复发布 | 输入通知桥接、用户编辑防抖和迟到结果测试通过 | 最新安装版输入 `hello` 后字符计数更新，Apple/腾讯/MyMemory 均返回“你好” | 当前短文本主路径已闭合 |
| 0–4 服务并发与稳定顺序 | Registry/Run Coordinator 已实现；数值诊断统一使用类型安全 `%lld`/`Int64` 格式，移除会导致 `EXC_BAD_ACCESS` 的 `%@` 整数调用 | 定向格式与稳定顺序测试通过；P14-G 两段式完整 AppTests 为最终门禁 | 当前安装版配置有四个启用源；未用桌面控制通道伪造全局快捷键 | 崩溃根因已修复，外部硬件触发待验 |
| 统一翻译方向解析 | 会话启动前生成共享 `TranslationResolvedDirection`；短文本使用分级置信阈值和脚本/母语/关注语言回退，需要明确源语言的服务不再自行猜测 | 单字、双字、Han/Latin/Hangul、显式目标、唯一语言对与未知多候选定向测试通过 | 最新安装版 `hello` 的三个启用源使用同一方向并全部成功 | 短文本方向与多源一致性闭合 |
| 单卡流式、失败、取消、重试 | 独立结果状态与 retry revision 已实现 | 既有测试覆盖 | 无真实流式 Provider | 代码存在，安装版待验 |
| 结果卡操作栏与拖动排序 | Header 使用全局 28pt 紧凑按钮和固定五槽操作区；名称区使用 AppKit 指针排序生命周期，操作按钮不参与拖动；运行协调器可重排当前快照且不重启任务 | 单源重试隔离、迟到结果、运行中重排、持久化及首/中/尾目标解析测试通过 | 最新安装版真实鼠标已完成面板排序并同步设置，窗口未被带动；设置页反向排序也通过并恢复原顺序 | 主路径自动化和安装版闭合 |
| BCP-47 与动态语言能力 | Core 标签及服务能力交集已实现；Libre 按实例读取 `/languages` 有向图并保留 `zh/zt` 映射 | 动态能力、精确方向与缓存定向测试已增加；最终统一回归待执行 | 无真实服务/语言包矩阵 | 代码存在，最终回归与安装版待验 |
| 独立 Selection Helper 划词 | Direct/App Store 主 App 均不嵌入 Helper；协议 v2 完整传递鼠标坐标和 request ID，Helper 依次解析焦点、鼠标命中及有界 Document/WebArea/PDF/Text 候选；AX 失败后只允许按 App 显式授权的兼容取词 | 最新定向 120 项、1 跳过、0 失败；Helper 严格签名通过；IPv4/IPv6 loopback 真实监听 | 当前稳定 Helper 已安装、配对、TCC 就绪并显示中文；最后工作树的外部 App 真实硬件快捷键矩阵待验 | 本机连接与权限已闭合，真实选区矩阵和外部分发待验 |
| Option+D 面板、位置与输入焦点 | 触发瞬间冻结屏幕，全部入口在目标屏幕 `visibleFrame` 中央设置首帧；关闭首次显示动画，不再恢复位置或接受 Helper/OCR 二次移动；空选区静默聚焦输入，可恢复故障仍提示 | 居中、负坐标、触发屏幕冻结、显式截图屏幕优先、静默/可恢复失败分类定向通过 | 桌面控制通道不能产生系统全局快捷键，TextEdit/Chrome/Electron/PDF 最终矩阵待人工复核 | 几何与状态自动化闭合，硬件触发待验 |
| App Store 降级 | AppStoreRelease 与 Direct 统一不嵌入 Helper、LaunchAgent 或 selection Mach lookup；无 Helper 时保留手动/截图翻译 | 当前源码与工程依赖检查通过；最终 AppStoreRelease 产物审计待本轮构建 | App Store 安装包未运行；外部下载入口仍有审核风险 | 代码边界已收敛，渠道运行与审核待验 |
| 截图翻译捕获 | 独立捕获目的，无编辑器/历史/剪贴板；OCR 行数/置信度改为类型安全格式 | Screenshot Core 220/220，OCR 格式契约测试与静态门禁通过 | 多屏/混合 DPI/真实 Option+S 仍待人工触发 | 已修复真实格式崩溃，捕获实机矩阵待验 |
| Apple Vision OCR | 共享 OCR、取消和 revision 已实现 | 冷启动全量曾因 latin 初始化超过 8 秒失败；随后 warm 定向与最新完整 warm 全量通过 | 真实无文字/低置信度/重截未验 | 功能回归通过，冷启动性能边界未闭合 |
| Apple 本地翻译与语言包 | 可用性与 `session.isReady` 联合判定；公开 `TranslationError`、运行/准备阶段统一分类；只有真实缺包显示下载；设置显示两个方向 | 已安装、可下载、就绪/未就绪、不支持、识别失败、空文本、取消、内部错误和恢复操作定向通过 | 最新安装版 AX 显示 `简体中文 → 英语` 与 `英语 → 简体中文` 均已安装；真实下载确认/拒绝和 macOS 14 未验 | 当前已安装及错误分类闭合，下载与旧系统待验 |
| 母语与关注语言 | 有序关注语言、最近使用、自动目标解析和旧偏好迁移已实现 | 非母语→母语、母语→最近关注、无关注不臆测目标等定向测试通过 | 中英日设置 UI 和真实短文本识别待验 | 自动化通过，安装版待验 |
| 系统词典管理 | 设置页提供打开 Dictionary App 和系统翻译语言设置入口，不伪造系统字典状态 | 构建覆盖 | 当前安装版入口待验 | 代码存在，安装版待验 |
| OpenAI-compatible | 适配器、凭据和诊断存在 | 既有测试覆盖 | 无真实测试 API | 代码存在，真实服务待验 |
| DeepL API Free | Profile 模板、请求映射和 Free endpoint 存在 | 本地 Stub/请求契约测试存在 | 无专用 Auth Key | 代码存在，真实服务待验 |
| Microsoft Translator | Key/可选 Region、请求映射存在 | 本地 Stub/请求契约测试存在 | 无专用 Key/Region | 代码存在，真实服务待验 |
| Google Cloud Translation Basic | API Key 和请求映射存在 | 本地 Stub/请求契约测试存在 | 无专用 API Key | 代码存在，真实服务待验 |
| 阿里云机器翻译 | AK/SK、Region 和签名请求存在 | 本地 Stub/签名契约测试存在 | 无专用 AK/SK；计费路径未验 | 代码存在，真实服务待验 |
| LibreTranslate | Base URL、可选 Key、持久化前 URL 规范化/敏感组件拒绝、loopback HTTP、`/languages` 有向能力发现、确定性低敏验证和 Profile/revision 有界缓存存在；无有效快照时首次运行按需发现能力 | URL 持久化与端点策略、动态能力、方向拒绝、中文映射和无缓存运行测试通过 | 无真实公网/自托管实例 | 自动化边界已覆盖，真实服务待验 |
| 免配置社区网页源 | MyMemory、Google 网页、腾讯翻译君以 `.communityWeb` 稳定 ID 注册；默认关闭、当前窗口确认、白名单 HTTPS、有限响应/超时、无 Cookie/重定向、单源错误隔离；目录顺序不再受启用状态影响 | 协议 Stub、固定目录、独立结果顺序和 Google 连接测试定向通过 | 2026-07-29 最新安装版 Google 低敏测试真实成功，并显示正确“连接成功”反馈 | 三项可生产展示；社区协议仍可能限流或失效 |
| 翻译面板设置跳转 | 明确请求 `.translationSettings`；App 只保留单例 `Window(id: "main")`，移除重复 Settings Scene；路由提交后再关闭未钉住面板 | 导航 generation、单 Scene 和设置菜单门禁通过 | 最新安装版 AX 只发现一个 `main` 设置窗口且“翻译”选中；面板按钮真实点击仍待硬件链路触发 | 单窗口与锚定闭合，面板入口点击待验 |
| Profile v13 | `translation_service_profiles`、多实例、Apple 单例、敏感配置拒绝、损坏行隔离和数据库失败保留最后良好状态存在 | Migration/Profile 与损坏行恢复测试在最新完整 warm AppTests 中通过 | 真实用户升级未验 | 自动化边界已覆盖，安装升级待验 |
| 翻译源配置流程 | 主设置页只保留摘要入口；二级页固定分为已开启、免费待开启、需配置待开启。免费源直接启用，社区源先做风险确认，需配置源保存即启用；配置和验证状态分离 | 分组快照、无重复、最多四项、保存即启用和未验证可运行的定向测试通过；最终 AppTests 786 项、4 跳过、0 失败 | 最新安装版 AX/截图已确认三组布局和无重复；真实配置 Sheet 与启用路径待验 | 页面结构闭合，外部配置待验 |
| 翻译源结果顺序 | 结果卡及设置二级页共用 AppKit 指针排序、首/中/尾目标模型和同一 `enabledServiceIDs`；运行中排序不重启请求 | 原始事件与 SwiftUI 状态时序已移除；目标解析、持久化、运行中重排和门禁通过 | 最新安装版面板和设置页均完成真实鼠标排序，设置与持久化顺序同步 | 主路径闭合；全部边界投放组合仍可扩充证据 |
| 翻译结果复制到剪贴板历史 | 翻译面板和收藏共用 Clipboard Feature 的显式文本复制入口；系统 Pasteboard 写入后复用现有规范化、签名、去重、更新时间和置顶链路，自写入标记阻止监听器重复入库 | 新译文和重复译文定向测试通过；重复复制保持一条记录并提升时间 | 最新安装版复制后剪贴板第一条和筛选刷新待验 | 自动化通过，安装版待验 |
| 分组设置侧栏 | AppKit Source List，5 组、11 路由，`.settings` 显示“通用” | Source List 模型/选择测试存在 | 当前安装版截图/AX 未取得；旧 flat-sidebar 证据无效 | 代码存在，安装版待验 |
| 统一结果面板 | 原文、多卡、固定五槽操作区、复制、朗读、单源重试、连接详情、收藏、钉住已实现 | 翻译定向 161 项、1 条件跳过、0 失败；排除 30 项 Broker 宿主用例及 1 项非前台 Key Window 宿主断言后，AppTests 783 项、4 跳过、0 失败 | 当前真实三结果卡截图和 AX 已取得，服务名与操作区稳定 | 本轮结果卡结构已闭合 |
| 收藏整次会话 | v12 收藏、不可变快照、搜索、分页和重译存在 | 既有测试通过 | 只有旧版本空状态证据 | 代码存在，安装版待验 |
| Markdown/JSON 导出 | 联表读取和导出 UI 存在 | 既有测试覆盖 | `evidence/exports/` 未闭合 | 代码存在，安装版待验 |
| Manifest v2/v1 兼容 | v2 六种字段类型；v1 兼容清单；v2 Secret 声明与敏感字段一一对应；升级比较 schema、字段类型与规范化域名并撤销变化契约 | Secret 升级定向 9/9、Manager/Validator 49/49、相关夹具回归 5/5，最新完整 warm AppTests 通过 | 真实插件配置未验 | 自动化边界已覆盖，安装版待验 |
| 插件验证生命周期 | 默认禁用；当前包/配置/凭据验证成功后才能启用 | 43 个普通用例通过；签名 Runner 集成隔离复跑通过 | 真实安装/变更/禁用/卸载未验 | 预最终 |
| XPC JavaScriptCore 隔离 | 独立 Runner、超时、取消、网络代理和 Host 连接测试 | 签名 Runner 集成隔离复跑通过 | 安装版崩溃/重启 UI 未验 | 预最终 |
| Cookie/sessionCredential | 用户手工录入、精确域名、跨域剥离模型存在 | 定向测试覆盖 | 无真实 Cookie，按约束不读取浏览器 Cookie | 自动化通过，真实插件待验 |

## 回归与工程验证

下表使用最后工作树的当前证据。完整 AppTests 同时保留历史冷启动失败、warm 定向
通过和最新 704 项完整 warm 通过，不能选择性引用任一单次结果替代完整事实。

| 验证项 | 当前证据 | 结论 |
| --- | --- | --- |
| 两个 P1 修复后的冷启动完整 AppTests | `current_blocks_app_tests_704_cold_failure_after_p1.log` | 704 项、4 跳过、1 失败；唯一失败为 Vision OCR latin 冷初始化超过 8 秒 |
| Vision OCR P1 后 warm 定向 | `current_vision_ocr_warm_after_p1_pass.log` | 1/1 通过；latin 0.162 秒 |
| deadline/取消与 Vision 定向 | `current_apptests_deadline_and_vision_targeted_pass.log` | 3/3 通过 |
| 最新完整 AppTests warm | `current_blocks_app_tests_704_warm_pass.log` | 704 项、4 跳过、0 失败 |
| 插件 Secret 升级契约 | `current_plugin_secret_upgrade_targeted_9.log` | 9/9 通过 |
| 插件 Manager/Validator 类 | `current_plugin_secret_upgrade_classes_49.log` | 49/49 通过 |
| Secret 契约相关夹具回归 | `current_plugin_secret_contract_regressions_5.log` | 5/5 通过 |
| 完整 AppTests 前一轮 | `current_blocks_app_tests_698_pass.log` | 698 项、4 跳过、0 失败 |
| 完整 AppTests 冷启动轮次 | `current_blocks_app_tests_cold_start_failure.log` | 698 项、4 跳过、1 失败；Vision OCR latin 13.956 秒超过 8 秒阈值 |
| Vision OCR 冷/热复跑 | `current_vision_ocr_cold_rerun_failure.log`、`current_vision_ocr_warm_rerun_pass.log` | 第一次 8.231 秒失败；第二次 0.838 秒通过 |
| 完整 AppTests warm-verified（P1 修复前） | `current_blocks_app_tests_698_warm_verified.log` | 698 项、4 跳过、0 失败；早于两个 P1 修复 |
| Libre URL 安全定向 | `current_libre_url_security_targeted_3.log` | 3/3 通过 |
| Selection Agent 复审定向 | `current_selection_agent_review_targeted.txt` | 6/6 通过 |
| Selection 骨架与焦点定向 | `current_selection_agent_skeleton_focus_targeted.txt` | 4/4 通过 |
| Helper 签名/二进制 | `current_selection_agent_signing_audit.txt`、`current_selection_agent_binary_audit.txt` | 同 Team、Hardened Runtime、严格签名和最小静态依赖已确认 |
| Direct Release | `current_direct_release_build.log` | `BUILD SUCCEEDED` |
| AppStoreRelease | `current_appstore_release_build.log` | `BUILD SUCCEEDED` |
| Xcode Analyze | `current_xcode_analyze.log` | `ANALYZE SUCCEEDED` |
| Direct/App Store 产物审计 | `current_selection_agent_artifact_audit.log` | Direct 包含 Agent 边界；App Store 排除 Agent/LaunchAgent/selection lookup，审计通过 |
| Debug 构建/复制/启动校验 | `current_debug_install_artifact_audit.log`、`current_debug_install_finished_launching_failure.log`、`current_debug_install_process_sample.txt`、`current_debug_install_secinitd_sample.txt` | 构建、复制、严格签名、Agent/LaunchAgent 嵌入通过；PID 10862 `finishedLaunching=false`，新校验要求新 PID、精确 bundle/executable、finishedLaunching 与 Identifier/TeamIdentifier/CDHash 一致，因此正确失败 |
| 安装版首启阻塞最终 sample | `current_secinitd_display_sharing_consent_block.txt` | `secinitd` 正等待 macOS `displaySharingConsentPrompt` 响应；不是 App 代码死锁，但系统同意交互未完成 |
| 插件生命周期定向 | `current_plugin_lifecycle_targeted_with_runner_failure.txt` | 43 个普通用例通过，签名 Runner 集成在该宿主失败 |
| 签名 Runner 隔离复跑 | `current_plugin_runner_signed_integration_rerun.txt` | 1/1 通过 |
| Screenshot Core | `current_screenshot_core_tests_220.log` | 220/220 通过 |
| 58.5MP 资源 | `current_58_5mp_resource_tests_2.log` | 2/2 通过 |
| Blocks Core 独立测试入口 | `current_blocks_core_test_action_unavailable.log` | 不存在独立 `BlocksCoreTests` scheme；`BlocksCore` scheme 未配置 test action，不能声称通过 |
| 当前门禁 | `current_gate_p5q.log`、`current_gate_p7h.log`、`current_gate_p7m.log`、`current_gate_p10a.log`、`current_gate_p11c.log` | 均通过 |
| Diff check | `current_git_diff_check.log` | 通过 |
| 最新划词运行时定向 | `selection-runtime-targeted-tests-120.log` | 120 项执行、1 项真实 Helper 测试宿主环境用例跳过、0 失败 |
| 最新 P14-G / P3-C | `p14g-app-tests-selection-runtime.log`、`p3c-selection-runtime.log` | 正式两段式 AppTests 通过；P14-A～P14-G 全部通过 |
| 最新构建与分析 | `direct-release-selection-runtime.log`、`appstore-release-selection-runtime.log`、`helper-signed-build-localized.log`、`analyze-selection-runtime.log` | 全部成功 |
| 最新安装版 Helper / 翻译设置 | `selection-helper-zh-Hans-ready-20260728.jpeg`、`translation-settings-helper-ready-20260728.jpeg` | Helper 已配对/TCC 就绪/中文；主 App 显示连接正常和已安装语言对 |
| 面板定位与 Apple 错误分类定向 | `Test-BlocksAppTests-2026.07.28_20-51-48-+0800.xcresult` | 135 项执行、134 通过、1 条真实 Helper 宿主条件用例跳过、0 失败 |
| 本轮完整 AppTests / Screenshot Core | P14-G 两段式运行结果；`Test-BlocksScreenshotCoreTests-2026.07.28_20-44-03-+0800.xcresult` | P14-G 通过；Screenshot Core 220/220 |
| 本轮门禁、构建与分析 | P5-Q、P7-H、P7-M、P10-A、P11-C、Release build、Analyze | 全部通过 |
| 本轮最终安装 | `evidence/runtime/translation-panel-placement-error-classification-2026-07-28.md` | 合并后 PID `55083`，安装路径、Identifier、Team、CDHash 和严格签名均通过；Helper 保留 |
| 当前安装版 UI/AX/运行时 | 最新签名 Debug App 已完成启动；翻译设置 AX 与截图确认三个免配置社区源位于列表末尾且默认关闭 | `translation-settings-community-sources-20260728.jpeg`、`evidence/runtime/translation-centered-panel-free-sources-2026-07-28.md`；桌面控制通道不能生成系统全局 Option+D，面板硬件触发、帧序列和面板内设置跳转仍待人工复核 |
| 翻译崩溃、单设置窗口与 Google 连接 | 真实崩溃报告定位到 `%@` 数值格式；类型安全格式、HUD、单例设置 Scene、固定源目录和 Google 测试已落地 | 定向 91 项、P5-Q/P7-F/P7-M、P14-G 最终两段式结果、Debug/Release/Analyze | 最新 AX：单一 `main` 窗口、翻译页选中、固定目录；Google 真实请求成功；见 `evidence/runtime/translation-panel-crash-settings-stability-2026-07-29.md` | 当前源码与安装版已验证，Option+D/Option+S 硬件触发待验 |
| 真实 entitlements 测试宿主启动 | Direct 与不嵌入 Agent 的 App Store entitlements 宿主均卡在 libsecinit→secinitd 初始化，未进入 test main | 最终 sample 证明当前等待不是 App 代码死锁；系统同意交互仍未完成 |

## 平台、凭据与真实交互边界

| 项目 | 当前事实 | 后续验证 |
| --- | --- | --- |
| 独立 Helper 安装/配对 | 本机 Debug Helper 已签名安装、配对并同时监听 IPv4/IPv6 loopback；旧 SMAppService Agent 已删除 | 等正式 URL、Developer ID、公证；重启恢复仍需最终矩阵复核 |
| Helper 辅助功能 TCC | 当前真实 AX 状态为已就绪 | 后续覆盖撤销、恢复和版本升级 |
| 外部 App 选区 | 未执行当前分支真实路径 | TextEdit、Safari、Electron/Codex、Preview PDF |
| 主 App axserver 日志 | 架构上不直接调用 | 用统一日志确认主 App 无自身 sandbox 拒绝，AX 只发生在 Helper |
| 第三方翻译源 | 没有专用测试凭据 | 用户授权测试凭据后逐项验证；不得提交凭据 |
| Apple 语言包 | 没有真实系统状态证据 | 已安装、待下载、不支持、取消 |
| macOS 14 | 没有运行证据 | 验证 Apple 服务降级和外部服务引导 |
| 多屏/混合 DPI | 没有截图翻译真实证据 | 当前安装版跨屏圈选和像素对照 |
| 真实插件 | 没有用户确认的插件包 | 使用低敏包验证安装到卸载全生命周期 |
| 中英日 Source List | 只有旧扁平侧栏证据 | 重拍 5 组、11 路由、键盘/AX |
| 性能指标 | 自动化时长不等于产品 p95 | 安装版测量骨架、输入、滚动和主线程 |
| `secinitd` 启动状态 | Direct、App Store entitlements 及既有稳定 App 均复现 libsecinit 等待；最终 `secinitd` sample 显示 `app.blocks.app` 队列阻塞于 `displaySharingConsentPrompt → CFUserNotificationReceiveResponse` | 当前停滞不是 App 代码死锁；需完成或解除系统“共享同意”交互后重跑真实签名宿主 |
| Debug 安装真实性 | `~/Applications/BlocksDev/Debug/Blocks.app` 主二进制哈希为 `34b33caf…02515c`，严格签名和嵌入内容通过；真实 PID 的 `finishedLaunching=false` | 安装脚本已拒绝旧 PID、错误可执行路径、代码身份/CDHash 不一致、测试进程和仅存在进程假阳性；真实 AX/UI 仍待 App 完成启动后验证 |

## 当前验收结论

当前可以确认 v13 数据模型、损坏 Profile 隔离、Libre URL 持久化策略和实例能力
发现、官方翻译源适配器、独立 Selection Helper 协议、母语/关注语言模型、Apple
语言包状态机、分组设置侧栏和插件 v2/v1 Secret 生命周期已进入源码。独立 Helper
的本机双栈 loopback 与签名已取得运行证据。

当前不能确认安装版真实行为或冷启动性能闭环：最新完整 warm 全量为 704 项、
4 跳过、0 失败，但历史冷启动仍有 Vision OCR 初始化超时；真实 entitlements
测试宿主受 macOS“共享同意”系统交互阻断；划词 Helper 还未经过正式分发、
用户配对/TCC/外部 App；第三方服务没有测试凭据。项目继续保持
`implementation-in-progress`，不推进阶段。

## 2026-07-29 翻译运行链路与面板稳定性增量

| 能力 | 当前代码事实 | 自动化/产物证据 | 安装版/外部证据 | 结论 |
| --- | --- | --- | --- | --- |
| 外置通知 HUD | 通知使用非激活 HUD 子窗口；上方 8pt、下方回退、显示器安全区收敛；移动和缩放时跟随但不改变面板布局或焦点 | 翻译/通知定向 189 项，1 项条件跳过、0 失败；P5-Q 通过 | 最新签名安装版已运行；硬件 Option+D 触发的真实 HUD 帧序列仍待验 | 代码与窗口契约闭合，真实硬件触发待验 |
| 稳定原文区和划词空态 | 原文永久展开；删除折叠状态、折叠入口和“划词操作”菜单；空选区静默，运行故障只通过 HUD 说明 | 原文高度、通知窗口、静默空态、真实故障和迟到结果测试通过 | 当前桌面控制通道不能产生系统全局快捷键 | 自动化闭合，外部 App 硬件矩阵待验 |
| 会话、并发与错误链路复审 | 统一 session/revision/attempt generation；结果顺序来自配置；Apple、OCR、社区源、收藏和插件不接受迟到结果；数值格式继续走类型安全接口 | Screenshot Core 220/220；P5-Q 通过；排除两个 Broker 沙箱宿主套件后 AppTests 769 项、4 跳过、0 失败 | Google、MyMemory、腾讯安装版低敏连接均成功；未生成新 Blocks 崩溃报告 | 翻译回归通过 |
| 完整 AppTests 原始结果 | 翻译改动没有修改 Clipboard Broker entitlement 或进程边界 | 805 项、4 跳过、30 失败；全部集中于两个 Broker 进程套件，子进程在测试宿主继承沙箱时于 `_libsecinit_appsandbox` 退出；单独复跑一致 | 与安装版翻译链路无直接关系 | 测试宿主环境问题保留，不能写成完整套件通过 |
| 工程与安装 | Debug/Release/Analyze 通过；HUD 关闭回调已显式回到 MainActor | Release、Analyze、P5-Q、diff check 证据已持久化 | PID `37295`，Identifier `app.blocks.app`，Team `LOCAL_TEAM_ID_REDACTED`，CDHash `3378bd6e8f44748d9803cb9dc2312fe20ba27ed0`，TCC 已授权 | 合并到 `main` 后已重新构建、替换并运行最新 Debug App |
| 已知运行诊断 | 本轮未改写无证据的全 App 布局逻辑 | 安装日志后仍可见既有 `Invalid view geometry`；自动化还产生一次 `NSLocalWindowSharingWindow makeKeyWindow` 警告 | 未观察到新 crash report | 不归因、不冒充已修复，后续独立定位 |

## 2026-07-30 结果状态与翻译源配置增量

| 能力 | 当前代码事实 | 自动化/产物证据 | 安装版/外部证据 | 结论 |
| --- | --- | --- | --- | --- |
| 结果状态表达 | 成功只显示前置勾选，不重复显示“已完成”；失败前置状态位变为单源刷新按钮，右侧不重复重试 | Header 状态与操作布局测试通过；翻译定向 165 项、1 跳过、0 失败 | 当前桌面通道不能触发全局 Option+D，真实成功/失败卡待硬件入口复核 | 代码和自动化闭合，安装版结果态待验 |
| 紧凑操作与复制反馈 | 操作区 28pt、2pt 间距、最多四槽尾部对齐；复制成功原位切换绿色勾选 1.2 秒，不弹成功 HUD | 操作数组、几何宽度、反馈 revision 与取消逻辑测试通过；P5-P 通过 | 未通过当前控制通道取得 1.2 秒瞬态截图 | 自动化闭合，瞬态视觉待验 |
| 结果卡和设置页排序 | 仅标题区可发起 AppKit 指针排序；窗口仅顶部空白可移动；运行中排序不重启请求 | 首/中/尾解析、运行中重排、持久化与 AppKit 门禁通过 | 最新安装版面板和设置页均完成真实鼠标排序，面板窗口未移动，设置及持久化顺序同步 | 主路径自动化和安装版闭合 |
| 翻译源开关与配置 | 三组位置固定；所有源统一开关；配置按钮在开关前；配置缺失和四项上限为行内反馈；保存配置不隐式开启 | 启用策略、固定分组、配置完整性和设置页门禁通过 | 最新安装版截图确认四个已启用源、统一开关、配置按钮及无重复目录 | 当前可见结构闭合，行内错误真实触发待验 |
| OCR 与面板稳定布局 | OCR 状态位于“原文”标题后的固定槽；删除独立状态行和来源 App 名称 | OCR 固定宽度、面板背景拖动关闭和源码契约测试通过 | 全局 Option+S 无法由当前桌面控制通道触发 | 自动化闭合，硬件截图翻译待验 |
| 本轮完整回归 | 本轮没有修改 Clipboard Broker 进程/沙箱边界 | Screenshot Core 220/220；完整 AppTests 原始 825 项、4 跳过、30 个既有 Broker 宿主失败；排除两个已知宿主套件后 789 项、4 跳过、0 失败；Release/Analyze 通过 | 最新签名 Debug App 已安装运行 | 翻译改动无新增自动化回归；完整原始套件仍不能写成全绿 |

## 2026-07-30 面板、自定义源与 CLI 增量

| 能力 | 当前代码事实 | 自动化/产物证据 | 安装版/外部证据 | 结论 |
| --- | --- | --- | --- | --- |
| 固定标题栏和 micro 操作区 | 标题栏固定 50pt；窗口移动只允许标题空白；结果按钮 28pt 命中、22pt 左右可见底板、0–1pt 间距 | 几何、OCR 标题预算和状态命中定向通过 | 420pt、中英日及改变面板高度的安装版截图待本轮重拍 | 代码与自动化闭合，视觉证据待补 |
| 设置翻译源窄宽布局 | 删除固定 420pt 尾列；宽屏三列、窄屏状态下移，最多两行且尾部控件固定 | 宽/窄预算定向通过 | 安装版窄宽和长文案待验 | 代码闭合，安装版待验 |
| AppKit 排序 | 面板和设置共用 `TranslationServiceOrderDragCoordinator`；AppKit 指针生命周期维护真实 source/target、首中尾投放和 2pt 插入线；成功投放一次更新、一次持久化；设置补右键、键盘和 VoiceOver 上下移动 | payload、伪造/迟到拒绝、目标解析、原子持久化、运行中重排及最终 177 项定向测试通过 | 最新安装版真实鼠标已在面板完成 MyMemory 第二→第一，并同步设置；设置页也完成真实反向排序，随后恢复用户原顺序 | 主路径自动化和安装版闭合；全部边界组合仍待扩充证据 |
| OCR 产品展示 | 产品只显示识别行数；原始 observation confidence 仅供诊断，不再显示 50% 或统一阈值警告 | 中文、英文、日文、混排及无文字契约测试覆盖 | 真实 Option+S 仍受全局快捷键控制能力限制 | 无意义百分比已删除 |
| Manifest v3 | v3 声明 text/screenshot、上下文、显式源语言、动态状态及截图数据权限；v1/v2 text-only 兼容；权限变化撤销审批 | Manifest、Validator、升级、权限、状态预算和唯一终态测试通过 | 用户真实插件包待验 | 协议和安全边界闭合，真实包待验 |
| 截图附件 | 仅截图入口＋声明＋明确审批提供；会话内存态；20MB/40MP 输入，4096px/2.5MB 目标，必要时 2048px；缩放失败不回退超限原图 | 图片允许/拒绝、超限、编码、清理和 action payload 测试覆盖 | 真实自定义图片源待验 | 边界闭合，外部源待验 |
| CLI 管理 | UI/CLI 共用管理服务；CLI 不直接读写持久层；Secret 只接收 stdin；无任意 Shell；失败测试非零退出 | CLI build、低敏合同脚本、失败终态及管理服务测试通过 | Action Broker 安装版 CLI 生命周期待本轮实测 | 代码闭合，安装版命令链待验 |
| 性能与状态一致性 | 包物化/验证和 Broker 大载荷解码移出 MainActor；插件失败禁用时同步移出启用顺序；排序原子写入 | 安全定向 3/3 通过，最终增量编译通过 | 安装版长包/高频操作性能待测 | 已关闭审查 P1，性能实测待补 |

## 2026-07-31 最后工作树验证增量

| 验证项 | 最后工作树证据 | 当前结论 |
| --- | --- | --- |
| 翻译/插件/Helper/管理服务定向 | `translation-custom-source-targeted-post-review.log`：177 项执行、1 项真实签名 Helper 环境用例跳过、0 失败 | 定向回归通过；跳过项不能写成真实 Helper 已验 |
| 配置与快照 fail-closed | `translation-custom-source-fail-closed-corrected.log`：2/2 通过 | 无效配置不破坏已启用插件；Secret 变更后快照刷新失败会移除陈旧启用顺序 |
| 原生插件/XPC | `translation-custom-source-plugin-tests-final.log`：88/88 通过 | Manifest、配置、权限、Runner 和 XPC 自动化闭合；真实用户插件待验 |
| 排除已知 Broker 宿主套件的 AppTests | `translation-custom-source-filtered-app-tests-final.log`：817 项、4 跳过、0 失败 | 最后工作树其余 AppTests 通过，但不是原始完整套件全绿 |
| 原始完整 AppTests | `translation-custom-source-full-app-tests-final.log`：853 项、4 跳过、30 个失败断言；25 个失败测试方法全部属于 `ClipboardBrokerBehaviorTests` 与 `ClipboardBrokerProcessIntegrationTests` | 测试宿主 Broker 环境失败保留；翻译/插件测试类无失败，不能把原始套件写成通过 |
| Screenshot Core | `translation-custom-source-screenshot-core-post-review.log`：220/220 通过 | 通过 |
| CLI | `translation-custom-source-cli-build-post-review.log`：`BUILD SUCCEEDED`；`translation-custom-source-cli-contract-post-review.log`：合同脚本通过 | CLI 构建与命令边界闭合；安装版 Action Broker 命令链待验 |
| 翻译门禁 | 最终长文件名的 P5-P、P5-Q、P7-C/D/E/H/M、P10-A JSON 均为 `ok: true` | 通过；同目录较早 `p5p-final`、`p7m-final` 是旧断言失败，不是最终结果 |
| Debug 构建 | `translation-custom-source-debug-build-final.log`：`BUILD SUCCEEDED` | 通过 |
| Release/AppStore/Analyze | `translation-custom-source-build-Release-final.log`、`translation-custom-source-build-AppStoreRelease-final.log`：`BUILD SUCCEEDED`；`translation-custom-source-analyze-final.log`：`ANALYZE SUCCEEDED` | 通过 |
| 最终指针排序定向 | `translation-custom-source-pointer-final.log`：177 项、1 项 Helper 环境跳过、0 失败 | 通过；跳过项不能写成真实 Helper 已验 |
| 安装版 UI 与真实拖动 | PID `56244`，安装路径 `/Users/bot/Applications/BlocksDev/Debug/Blocks.app`，Identifier `app.blocks.app`，Team `LOCAL_TEAM_ID_REDACTED`，CDHash `a66abf71ef761822514d53e04ceb1ae4da117831`；固定标题栏及 micro 操作区截图已形成；真实鼠标面板排序后设置同步，设置页反向排序也通过并恢复原顺序 | 标题栏和排序主路径闭合；Option+S、自定义图片源和 CLI 安装版全链仍待验 |

### 当前质量结论

- 面板固定高度、micro 操作区、AppKit 结构化排序、OCR 行数展示、Manifest v3、
  截图附件权限和 CLI 管理边界已有最后工作树自动化证据。
- 原始完整 AppTests 仍存在两个 Clipboard Broker 宿主套件的环境失败；本轮没有修改
  Broker 安全边界来掩盖失败，也没有把排除后的 817 项结果写成完整套件通过。
- 当前最主要的翻译残余风险是安装版真实交互：截图翻译、用户插件、Action Broker
  CLI 生命周期、浅深色与窄宽视觉。上述证据形成前，项目继续保持
  `implementation-in-progress`。

## 2026-07-31 翻译体验与主题最终收口矩阵

| 能力 | 当前实现事实 | 自动化证据 | 安装版／外部证据 | 当前结论 |
| --- | --- | --- | --- | --- |
| 原文先收缩、结果独立滚动 | 标题固定；手动／划词原文默认 72pt、截图 OCR 96pt、最小 48pt；语言栏固定；原文编辑器内部滚动不驱动外层 | 滚动协调器、收缩／恢复、内部滚动隔离及 Reduce Motion 定向覆盖；最终 P14-G 通过 | 真实滚动验证 `72 → 48 → 72pt`；达到 48pt 后仅结果移动；12 行、734 字符原文内部滚动时外层仍为 72pt | 主路径闭合 |
| 结果卡分片更新与共享紧凑操作 | 单源状态独立观察；操作组、语义颜色、高对比度及失活状态共用全局实现 | 结果状态、按钮几何、主题语义及 AppTests 覆盖；最终 P14-G 通过 | 四源同时成功；显式浅／深、跟随系统、降低透明度和提高对比度均完成安装版检查 | 主要路径闭合；完整语言／错误态像素矩阵待补 |
| 翻译源排序 | AppKit 结构化载荷；一次更新 `enabledServiceIDs`；运行中排序不重启请求；设置同步 | 拖放载荷、首中尾目标、非法载荷、取消、持久化已有覆盖；最终 P14-G 待重跑 | 真实鼠标证据 `evidence/screenshots/translation-panel-pointer-drag-reordered-20260731.jpeg`；本轮 AX 键盘排序同步到设置并恢复原顺序 | 鼠标主路径有既有真实证据；本轮键盘与设置同步闭合 |
| 四源运行 | 同一 session/revision 下稳定按配置顺序归并，迟到结果不覆盖 | 多源、失败隔离、重试和取消已有覆盖；最终 P14-G 通过 | 腾讯翻译君、MyMemory、Google 社区源、Apple 本地对低敏合成文本均成功 | 本机当次请求通过；不承诺外部协议长期稳定 |
| 译文复制与剪贴板历史 | 写系统 Pasteboard 后按隐私／历史状态决定去重、持久化与置顶 | 复制历史、暂停、禁用和隐私分支已有 AppTests 覆盖；最终 P14-G 通过 | 合成译文复制后立即位于剪贴板第一条；验收后删除合成记录并恢复系统剪贴板 | 主路径闭合 |
| 收藏 FTS v14 | 收藏迁移到 `unicode61` FTS，替代大数据线性扫描 | 数据库迁移、Unicode token／prefix 查询和 Repository 测试覆盖 | 无大规模真人收藏库性能样本 | 自动化闭合；`unicode61` 不提供任意 CJK 子串语义 |
| OCR／图片附件 revision | OCR 和文字源不等待图片编码；仅声明图片输入的服务等待附件；编码失败为单源错误 | Screenshot Core 220/220；取消测试修前 30 轮失败 2 次，均为结果发布后 registry 清理前读到 `count = 1`；只修测试等待边界后 `translation-finalization-ocr-cancellation-100x-20260731.log` 100/100 通过 | 真实硬件 Option+S 受控制通道限制 | 产品迟到结果覆盖未复现；测试同步竞态已闭合，硬件链路待验 |
| Action／CLI、Profile 与插件配置 | request ID 任务所有权、真实取消、Apple 确认关闭保护、精确回滚、可选枚举“未设置”及 CLI 发现入口 | P14-G、P14-E、CLI 构建与合同脚本均通过 | Apple 系统确认与签名安装版 CLI 全链未完整实测 | 自动化主路径闭合，平台交互待验 |
| 主题矩阵 | 保留跟随系统／浅色／深色；颜色来自语义 Token；无自定义 Accent | 主题切换、失活、高对比度、降低透明度的状态和几何测试纳入 AppTests | 显式浅／深、跟随系统、降低透明度、提高对比度真实检查；验收后系统选项恢复关闭 | 主要主题状态闭合；中英日全部子界面像素矩阵待补 |
| 完整 Screenshot Core | 本轮共享组件变更未修改截图输出语义 | `evidence/automation/translation-finalization-screenshot-core-20260731.log`：220/220 | 不适用 | 通过 |
| 完整 P14-G | 正式签名宿主与隔离 Broker 两段式 AppTests | `evidence/automation/translation-finalization-p14g-after-ocr-sync-20260731.log`：测试等待边界调整后重新执行通过 | 不替代安装版体验 | 通过 |
| P14-E | 截图 Action／CLI 合同门禁依赖已按最新工作树修正 | `evidence/automation/translation-finalization-p14e-20260731.log`：真实重跑通过 | 不适用 | 通过 |
| P3-C | 首轮失败为测试同步竞态，不是产品 OCR 状态覆盖；修复只调整测试等待 registry 清理边界 | 修前 30 轮失败 2 次，均在结果发布后清理前读到 `count = 1`；定向修后 100/100；`translation-finalization-p3c-final-20260731.log`：P14-A～G 全部通过、聚合 `ok: true` | 不适用 | 通过 |
| main 合并与最终安装 | 提交 `6fa7dd0e` 已快进合并到 `main`；功能分支已删除 | Debug App/CLI 构建通过，严格签名和 CLI 合同复核通过；证据 `translation-finalization-main-install-20260731.txt` | PID `26178`，路径 `/Users/bot/Applications/BlocksDev/Debug/Blocks.app`，CDHash `8b50893ae057f775373707052b3a08c61d580411` | 通过 |
| 硬件入口和系统交互 | Option+D、Option+S、Apple 系统语言确认必须由真实系统入口触发 | 自动化不能替代 | 当前控制通道不能等价生成硬件全局快捷键，也不能稳定制造 Apple 系统确认 | 待验证 |
| 四源连续输出性能 | 目标为可见反馈 p95 ≤ 33ms、主线程无 >100ms 无响应 | 尚无有效最终样本 | 本轮未取得可声明的安装版 p95 | 待验证 |

### 最新结论

- Screenshot Core 220/220、P14-E、P14-G、P3-C 及 OCR 取消定向 100/100 均有
  最后工作树通过证据。
- 安装版已验证原文滚动几何、四源基本运行、复制历史、排序同步和主要主题状态。
- 真实硬件 Option+D／Option+S、Apple 系统确认及四源 p95 尚未在本矩阵中判定
  通过。项目继续保持 `implementation-in-progress`。

## 2026-07-31 语言阻断项与示例源矩阵

| 能力 | 当前实现事实 | 自动化证据 | 安装版／外部证据 | 结论 |
| --- | --- | --- | --- | --- |
| 有方向语言能力 | Adapter 可覆盖 source→target 判断；腾讯按实测非对称能力限制；Libre 使用实例能力图 | 支持／拒绝语言对、单源失败隔离和无错误网络请求测试通过 | 腾讯对多种冷门目标真实回退中文；Google/MyMemory 返回目标文字 | 已关闭假成功根因 |
| 返回语种核对 | 腾讯核对响应 target；Microsoft 核对 `to`；不以短译文语言猜测器误杀合法结果 | 正确目标、目标不匹配和不可重试分类覆盖 | 腾讯响应 `target=zh` 与请求不符已复现 | 主要接口闭合 |
| 常用语言优先 | 母语→最近关注→其余关注去重；全部语言本地化排序；当前选择保留 | 菜单分组、去重、顺序和当前值测试通过 | 当前翻译设置母语简中、关注英语的 AX 状态已确认 | 数据与 UI 模型闭合，实际下拉瞬态待补图 |
| 收藏确认 | 全局紧凑确认样式，星标和单行文字整体居中，2 秒关闭 | 浅深色、失活、降低透明度／高对比度和几何测试覆盖 | 硬件翻译入口受当前控制通道限制 | 自动化闭合，瞬态图待补 |
| Input Echo 文字 | Manifest v3，无网络／Secret，动态状态后回显请求与授权上下文 | 合同、唯一终态、超限和 SHA 测试通过 | 安装版 CLI/XPC 文字测试成功 | 闭合 |
| Input Echo 截图附件 | 仅回显 ID、格式、尺寸、字节数和 SHA-256，不回显 Base64 | 编码、权限和摘要测试通过 | AppIcon PNG 安装版 CLI/XPC 测试成功 | 闭合 |
| Input Echo 生命周期 | 校验、安装、测试、启用、排序、脱敏导出、禁用，共用 UI/CLI 管理服务 | PackageWorker fatal 回归与 Runner 身份边界测试通过 | 最终已安装、已验证、关闭；原四源顺序恢复 | 闭合 |
| 全量回归与产物 | 本轮未改数据库和用户语言／源顺序 | 翻译定向 345 项 1 跳过 0 失败；P14-G、P3-C、Screenshot Core 220/220；Release/AppStore/Analyze 通过 | 最新签名 Debug App 已运行 | 可自动验证项通过 |
| 硬件入口与平台状态 | 不用自动化代替真实 Option+D/S 或系统下载确认 | 单元与集成覆盖存在 | 当前控制通道不能等价触发；本机无可安全制造的未安装语言对 | 待验证 |
| 四源产品性能 | 目标可见反馈 p95≤33ms、主线程无>100ms无响应 | 未以测试总时长替代产品测量 | 本轮无有效最终样本 | 待验证 |
