# Step 1 验收审计（2026-07-12）

状态：code-blockers-closed-platform-evidence-pending
审计提交：`97d3ea8e`
来源级别：project acceptance audit

## 1. 结论

- Step 1 的代码与契约阻塞项已经关闭；当前仍不把缺少真实硬件证据的平台矩阵写成最终 PASS。
- Step 2 可以进入需求、交互和架构规划；开发实施仍需完成 Step 2 PRD 确认。
- 当前单屏主流程成立：设置入口启动、首次拖动区域、快速编辑、切换完整编辑、关闭和图片剪贴板均实测可用。
- 多屏、混合 DPI、VoiceOver、完整工具、Broker/CLI 成功链路和外部读取矩阵仍缺真实证据，不能用静态门禁替代。

## 2. 本轮证据

- `BlocksScreenshotCoreTests`：88 项通过。
- P14-A 至 P14-G、P3C：通过。
- P007 App Icon：通过。
- 工作树和 `git diff --check`：干净。
- 安装包：`/Users/bot/Applications/BlocksDev/Debug/Blocks.app`，进程正常运行。
- 真实低敏区域截图：从设置页启动，首个 down/drag/up 进入快速编辑；切换完整编辑后内容可见；关闭返回设置页。
- 系统剪贴板包含 PNG、TIFF 和系统派生图片类型。
- 合法 CLI 请求在 Broker 未启用时返回结构化 `broker_unavailable`；非法 interactive kind 返回 `interactive_requires_smart`。

## 3. 已关闭验收阻塞项

### P1 代码与交互

1. `completeForReplacement()` 已进入异步 revision gate；先提交样式事务，再等待最新缓存，生产代码不保留同步 renderer 旁路。
2. 快速编辑和完整编辑改用同一主操作按钮组件，文字 label 自身持有最小 `36pt` 高度；实机复查主按钮视觉与完整矩形命中恢复正常。
3. 工具配置中的普通 tool chip 已暴露为 AX 按钮，固定选择/范围工具继续保持不可操作语义；拖入、拖出、排序和键盘路径保留。
4. ready/window 提示已展示 `Space`、`Tab`、`F`、`Shift+F`，区域拖动后隐藏已失效提示。
5. CLI `--dry-run` 已实现为纯参数验证路径，不连接 Broker、不截图、不创建输出文件；合法请求返回结构化 `completed + dry_run`。

### P2 代码与规格

1. 方向键、删除和 AX 对象调整会先提交当前样式事务，再执行普通 document mutation。
2. 选择工具条拖动柄实际命中区已改为 `36×36pt`，grip 视觉尺寸不变。
3. Quartz alpha 为零的窗口已从候选和遮挡列表排除；非零透明或异形窗口仍采用保守矩形近似，列入平台残余风险。
4. 放大镜实现已统一为 UED 记录的 `96×96pt`。
5. 旧 `screenshot.selection.hint.region` 和“Return/双击确认”文案已删除。

## 4. 已收口产品口径

- 默认快编要求可扩展冻结上下文；构建失败时终止本次截图且不写剪贴板，并提供重新截图/取消。默认完整编辑允许缺少上下文，但不提供扩大范围。
- “固定捕获设置”开启时，会话内修改只影响本次；关闭时，截图工具条最后一次修改值写回设置并供下次沿用。
- 快编和完整编辑共享工具主顺序与逐工具样式，分别保存显隐集合；选择和调整范围固定在最前，不参与隐藏或拖动。
- 隐藏的完整编辑工具仍允许从“更多”菜单临时调用，该调用不改变设置中的显隐状态。

## 5. 尚缺真实证据

- 双/三屏、上下/负坐标、1x/2x 混合 DPI、跨屏区域和全部屏幕输出。
- 窗口阴影边界、透明/点击穿透窗口候选。
- 11 个工具、属性修改、对象二次调整和 quick/full 往返。
- 工具拖入、拖出、排序和 2pt 插入线。
- VoiceOver、完整键盘路径、浅色、Reduce Transparency、自定义 accent。
- Finder、Preview、浏览器、常见输入框读取 PNG/JPEG。
- Broker 启停、App 冷启动、XPC 成功往返和 CLI 真实捕获。

## 6. Step 2 架构准备结论

- 可复用：Apple Vision OCR、方向与中英日语言配置、阅读顺序、revision guard、用户编辑锁、FTS、分页、清理策略和 pasteboard 自写入抑制。
- 必须新增：`screenshot` built-in tag、截图历史开关、截图会话到历史记录的桥接、图片 payload 原子替换、OCR/search 重置和最终图重复合并。
- 当前 `ClipboardRepository.insert` 会按 signature 返回旧记录 ID，不满足“保留当前截图 ID、合并旧标签/收藏并删除旧重复项”的需求。
- 当前没有通用图片 payload 替换接口；详情编辑只支持修改图片 OCR 文本。
- Step 2 实施前必须先确认记录创建时点、取消编辑后的记录行为、OCR 启动时点、重复合并语义、系统标签展示和 CLI 隐私边界。
