# 按序处理开放 Issue 与 Beta 8

## 旧项复核

按编号复核 #34、37、38、40、41、42、44–49：已有补丁未被回退；仍缺旧TCC、缺权限、双屏、macOS27/Xcode27或原交互矩阵等证据，未强行关闭。Beta7 后测试修正提交 `93a3a33` 的云端完整CI已通过（35387835002）。

## 新项实现

| Issue | 改动与边界 |
| --- | --- |
| #50 | 源码安装检查正在运行进程及注册job，无法确认安全则拒绝覆盖；显式关闭总CLI集成复用认证idle proof和异步注销。修正等待批准注册、取消恢复及quit/update交错。未知连接失败不伪称权限关闭。无法握手的旧坏状态不自动强杀，故仅部分修复。 |
| #51 | 主App真实启动时一次性恢复已配对可信Helper；密钥读取失败保持明确状态，未配对/测试runtime不启动，通知观察仍尊重用户退出。不重置配对、不扩大信任。 |
| #52 | 修正@Published willSet订阅重新读取旧状态的明确风险，拖动/系统交互/固定状态使用回调新值。列表以serviceID保持视图身份；陈旧拖动完成有generation保护。此机制修正不等价于原macOS27拖动失灵已完整复现。 |
| #53 | 产物/目标名称blocksHelper，复用主App资产；保留模块名以兼容内部测试，保持Bundle ID/Keychain/IPC身份。发行脚本、审计与本地peer路径同步；旧独立路径仅兼容，不扩大沙盒权限。 |
| #54 | prune事务内聚合自定义标签、分组metadata、pinned标记保护；收藏沿用原开关，普通预算为max(0,上限-保护数)，手动删除不受影响。 |
| #55 | 共享图标按钮新增可选无边框外观，只用于收藏；已收藏黄色填充星形，未收藏次级色空心星形，热区/帮助保留。 |
| #56 | Core共享palette，repository在创建事务内选色；UI传递未指定颜色，CLI/import一致。已存在/显式色不改，无存量迁移。 |

## 验证与发布

本机日常安装版未替换；测试使用隔离数据，不撤销用户权限、不读取用户剪贴板内容。

- 主App受控构建通过。第二次全量执行2108项：2090通过、17跳过、1项新增内存清理夹具失败。夹具直接赋records却未加载tag snapshot，导致测试标签被清走；补正常加载步骤和标签存在断言后，最终相关45项定向全部通过。前一轮Helper夹具“未运行也返回healthy”的问题也已修正，不改变生产启动策略。没有把失败的全量结果写成全绿。
- 最终定向结果：`Test-BlocksAppTests-2026.09.20_16-06-49-+0800.xcresult`；全量结果：`Test-BlocksAppTests-2026.09.20_16-00-37-+0800.xcresult`。
- Helper改名后构建通过，35项测试全部通过；测试描述从旧format1等价转成format2后使用相同受控runner执行，未绕过隔离。其显示名为blocksHelper，Bundle ID仍是app.blocks.selection-helper；与主App的AppIcon.icns SHA-256均为 `0206bcd906e8db69ca17aad26b9a569f256ee1b58984d62ec283ab52916bd43d`。
- CLI优化执行23例、跨模块list输出7例、开发安装事务与本地信任策略检查、UI设计系统门禁通过。发行P20静态检查通过；旧独立Helper沙盒访问例外保持原路径，不因改名扩大权限。
- #55 使用当前生产UI构建的隔离副本和3条合成记录实测：收藏后黄色裸星形无边框，坐标点击可取消收藏。副本禁用后台采集/Helper并已退出，不等同日常安装版升级验收。
- #50仍是保守的部分修复；#51/#52原macOS27恢复/拖动场景、#53更名后原TCC/配对保留未完整实机验收。#54/#56的仓库事务、内存fallback与导入/tag-add检查有明确证据。

## 正式发布完成

已发布 [0.1.0-beta.8](https://github.com/winx402/blocks-mac/releases/tag/v0.1.0-beta.8)，包源提交 `a240bbd88d46579f25c4f697fe91fe05bc83ffe2`，build8、arm64、macOS14+。

- 主App与Helper签名、嵌套角色及沙盒审计通过。Helper更名前后与beta7的designated requirement完全一致；这不等同原TCC/配对迁移实测通过。
- Apple公证 `aef5a78c-dbff-439d-9820-a77cca97e76d` Accepted；DMG已stapled且Gatekeeper通过。
- DMG 24,653,537 bytes；SHA-256 `94a0b34827657efb2533e842ffb77db81e568d112e7aa98842743b54365d08f0`。
- 草稿与公开附件长度/摘要/Sparkle签名复核后，Beta源更新build8，最终状态published_feed_updated；匿名下载HTTP200、在线appcast首项匹配。
- 实际签名发行CLI `list` 在无可用官方Broker时返回结构化broker_unavailable/exit1，没有错误宣称explicit_enable_required，也无SIGTRAP。
- 云端首轮仅因配置测试仍按旧Helper目标名查找而失败；测试脚本已在 `fda51a0` 修正并本机通过，未变更发行生产代码或重指向标签。[修正后云端完整检查](https://github.com/winx402/blocks-mac/actions/runs/35499042055) 截至发布记录时仍在运行，不能计作已通过。
- #54/#55/#56基于上述证据关闭；#50部分修复、#51–#53及旧项原场景仍有未验证范围，继续开放。
