# Issues 22–26 实施与验收

状态：本轮实现与相关验证已完成；受控外部 App／全屏截图验收仍保留待验证。未发布版本，Issue 当前状态以 GitHub 为准。

## 批准范围

- #22：本地开发版安全自动关联 Helper；保留手动配对与官方沙盒边界；权限引导使用实际 Helper 的身份和认证健康状态。
- #23/#24：设置侧栏实际禁止水平滚动，移除品牌头，统一顶部/详情背景。
- #25：退出隐藏 UI、最多 5 秒清理后无条件强退；同时处理恢复等待、迟到回调和更新意图交错。新增默认关闭的异常反馈与独立 CLI。
- #26：翻译提示位于源标题行，不创建独立 HUD；保留稳定布局、详情访问、动作及关闭；扩大安全拖动区。

## 已取得的局部证据

- 反馈隔离测试：17 个单测及 14 项 CLI fixture 检查通过，没有实际 GitHub 提交。
- 本地 Helper 关联：6 项纯 fixture 通过；本地 Swift 6 与官方配置 Core typecheck 通过；尚未验证实际签名双方或钥匙串授权。
- 存储/升级安全独立检查通过。它不代表 5 秒硬退出、安装版或所有 Issue 验收通过。
- 集成编译曾发现新增侧栏测试使用了不可传入的 `let` 默认成员参数，已改为可注入属性；中途构建还读取过尚未完成的权限目标方法。最终必须重新构建固定后的工作区。

## 审查发现与处理

- 更新准备/恢复与退出交错：加入不可逆退出标记、准入及安装回调检查。
- 迟到的 Broker 认证回复：发送生命周期 RPC 前检查取消与退出，不在普通退出注销共享服务。
- 强退登记锁竞争：改为不可变快照发布和无锁读取，避免锁竞争导致整个清单遗漏。
- 后台反馈晚于退出、关闭自动开关后的发送竞态：加入发送 gate，取消仅限当前服务实例拥有的进程。
- 未知提交过期后可能再次创建：保留无正文防重发标记。

## 未通过前不得关项

完整 App/Helper 构建、固定签名安装、系统权限目标、真实交互/拖动/布局与无障碍、多语言及浅深色、独立进程硬截止、原有相关门禁与遗留 Issue 的针对性验收仍需完成。

## 后续集成证据

- App、CLI、Helper 的 LocalDevelopment 构建通过；首次新包经固定证书安装，旧包有备份。新旧包的 designated requirement 一致，未重置 TCC。
- 完整 XCTest 首轮：1956 passed / 4 failed / 16 skipped（1976 total）。之后定向 146 项：145 passed / 1 failed。最后对侧栏两个回归及独立主线程阻塞 watchdog 用例复跑：3 passed / 0 failed。没有把这些分轮结果冒充最终整套全绿。
- 两个新侧栏用例证明：`constrainBoundsRect` 不足以拦住直接 `scroll(to:)`，且原生 Source List 仍会把 224pt 视口对应文档扩宽到 256pt。已在实际滚动/边界入口锁定 x=0，并让 outline 的最终 frame 宽度遵循视口；不放宽断言。
- 权限新用例此前把已结束、清空 session 的成功路径误判为失败，改验结束回调；既有翻译源取消用例定向复跑通过，未改其产品代码或放宽等待。
- 真实临时进程 fixture：严格 timer 下 startup-to-exit=5.014s、request-to-force=5.000s；退出码77证明走强退，已登记私有 Broker 消失，未登记 `/bin/sleep` 存活后由 fixture 自行清理。
- UI design-system 门禁通过。对新的精简协作说明，门禁验证 AGENTS → docs/index → UI 规范完整导航链，未移除规范存在性检查。
- 安装版 CLI 在主 App/Helper 均未运行时成功输出 `feedback --help` JSON；尚未执行真实公开提交。

## 21:21 实机检查点（等待用户系统确认）

- 当前包仍位于 `~/Applications/Blocks.app`，最近一次备份为 `DevelopmentInstall.noindex/.BlocksDev-install-7c01zq6d/previous.app`。未提交本轮代码、未关闭 Issue、未发布版本。
- 新安装版权限页实际显示录屏、主程序辅助功能、输入监控均已授权，无 TCC 重置。Helper 的前一轮界面也显示辅助功能已就绪。
- 实际 Cmd+Q：主 App 与剪贴板 Broker 消失，独立 Helper 保留。本地反馈记录包含全部 participant_completed 和 graceful，终态耗时 234ms；CLI 预览成功，无外传。
- 手动反馈实际完成合成文字编辑→预览→取消；公开提交按钮独立存在，未点击。自动开关保持关闭。
- 实机首次自动反馈确认页丢失账号快照，已改为 sheet(item:) 持有冻结账号，最新包已安装；仍需复验该确认页。
- 实机设置标题栏与详情存在双层 within-window material 色差；已移除详情的重复背景，最新包已安装；仍需复验浅深色。
- Helper 自动关联发现确定协议错误：UUID 授权码不能生成现有六位数字协议的 HMAC proof。已修为兼容码，新增完整双向 HMAC/ECDH/加密命令 fixture，当前 7/7 通过，不放宽身份或密钥策略。
- 修正后实际翻译设置处于“正在检测助手连接”；系统出现 SecurityAgent 进程。CUA 访问 com.apple.SecurityAgent 被安全规则明确拒绝，因此无法确认窗口内容，也没有用其他方式绕过或读取密码。等待用户查看 Blocks/Blocks Helper 的系统钥匙串提示并自行完成；不把关联视为成功。
- 下一步：用户确认后复验真实自动关联及重启保留；确认页账号、背景、翻译内联/拖动、遗留截图/剪贴板交互矩阵尚未全部完成。不能把已有局部通过当作所有 Issue 已关闭。

## 用户完成钥匙串确认后的实机复验

- 主 App 显示“连接和辅助功能权限正常 · 助手 0.1.0”，Helper 显示“已配对／已就绪”。双方均正常退出、确认进程消失后重启，认证连接恢复；未输入配对码、未重置或替换已有密钥。
- 自动报告确认页首次打开正确显示当前账号与固定公开仓库。Escape 能取消，开关保持关闭；没有开启自动提交或创建测试 Issue。
- #24 的旧全窗背景已经覆盖标题栏，真正的色差来自 macOS 26 ScrollView 顶部 scroll-edge 合成。采用公开 toolbar/scroll-edge 隐藏 API 后，安装版浅色、深色的标题栏和详情连续一致；侧栏仍为原生 sidebar，外角未出现透明缺口。
- 翻译实际处理合成英文，Apple 本地译文出现。临时移除唯一关注语言，中文输入触发“请先添加至少一种关注语言”错误：错误位于原文后、输入框上方，输入框仍在原位置；关闭错误不改变输入。英语关注语言已恢复。
- 拖动日志确认事件到达正确窗口和 DragView，但系统 performDrag 在后续 dragged 事件到达前立即返回。专用标题区改为 down/drag/up 坐标跟随后，实机窗口由 `(640,295,640,520)` 移到 `(750,365,640,520)`；输入框内拖动只选中文字，窗口位置不变。逐鼠标事件的临时诊断已移除。
- 剪贴板安装版已记录合成 TextEdit 文本；搜索限定到该记录并切换富文本筛选后，顶栏标签、清除按钮无重叠，恢复入口和无障碍名称存在。

### 仍然不冒充通过的边界

外部 App 的全局快捷键／自动粘贴及受控全屏截图还不能据此关项。CUA 可以直接向 TextEdit 控件投递事件，但只读 `NSWorkspace.frontmostApplication` 仍为 Blocks；点击、AX Raise 均未把 TextEdit 变为系统前台，Dock 访问超时。这不满足自动粘贴“原外部 App、原窗口在前台”的前提。没有降低目标校验，也没有用其他技术绕过 CUA 控制边界。需要用户把受控文稿真正置于前台后确认；不把 AX 的 focused 字段当作系统前台事实，不捕获含 Codex 的非受控全屏作为证据。

## 交付验证汇总

- 完整基线：1964 passed / 0 failed / 16 skipped（1980 total）；构建使用既有受控 ibtoold 清理后一次增量重试，不称为首次构建即通过。
- 随后的扩拖动区域定向：27 passed / 1 failed，失败明确指出空输入走 BlocksStateView 而非兜底 Color.clear，已给这个无交互引导分支补齐同一拖动区域；未放宽断言。最终 TranslationInlineFeedbackTests 5 passed / 0 failed；其余 27 个定向用例对应代码未再变更。
- 最终安装版的头部 padding 点 `(100,35)` 可拖动，窗口由 `(640,295)` 到 `(700,335)`；空输入引导区 `(320,325)` 再拖动，窗口到 `(750,365)`。头部非交互区域从 28pt 扩为完整 50pt，动作组与编辑／结果区域保持独立。
- 设置、翻译、剪贴板三类窗口均检查了安装版浅深色外角；未见矩形材质穿出原生圆角。剪贴板在浅深色、搜索和活跃富文本筛选下检查了顶栏与清除按钮。
- App、CLI、Helper 的最终 LocalDevelopment 构建和固定证书安装通过；旧包继续备份。最终位置不变，最新备份为 `DevelopmentInstall.noindex/.BlocksDev-install-4g88nfx2/previous.app`。
- UI design-system 门禁通过，临时逐鼠标事件诊断已移除。自动反馈仍关闭；应用外观、语言均跟随系统，英语关注语言已恢复。
- 尚未验证／不得宣称完成：#13/#14/#15 的受控全屏截图安装版验收、外部前台下的自动粘贴与全局快捷键、真实公开反馈 POST、所有真实系统辅助显示选项与 macOS 14 运行矩阵。没有发布 Beta 或绕过既有发布门禁。
