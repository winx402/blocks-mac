# Issues #44/#45 与 Beta 3

## 修复

- #44：将标题纳入 FTS；索引 revision v3 驱动旧记录增量重建。改名同步移除旧标题，保留 OCR、标签及记录 ID，隐私排除投影保持为空。
- 修复重建批次被无 FTS 的正常 redacted 记录反复占满的问题，仍清理残留私密 FTS；revision 变化时将未锁定 running OCR 恢复 pending，拒绝旧回调但不遗留无人负责的 running 状态。
- #45：固定证书配置允许省略/空 Team ID，实际签名必须满足精确叶证书指纹；指定 Team ID 时继续匹配。升级校验旧新双方证书与不变的 designated requirement；不开放静默换证书，不改官方发行策略。

## 验证与边界

- 最终仓储及剪贴板 CLI XCTest 107/107 通过，无跳过；覆盖标题首次索引、改名、旧版迁移、96 条隐私记录后的批次收敛、旧 OCR 任务迁移，以及已有 CLI 授权/事务测试。
- 开发签名、安装失败路径、本地身份安全、CLI 参数/文件 I/O、发布流水线、xcconfig、安装入口和 P20 分发检查通过。
- 现有 Developer ID 安装的实际叶证书与 DR 检查通过，错误证书指纹被系统拒绝。不是原无 OU 机器的升级验证。
- 无 OU 临时自签尝试因合成证书未受信任而被系统拒绝：CSSMERR_TP_NOT_TRUSTED / no identity found。没有修改用户信任设置或使用真实私钥；临时库已删除，搜索列表和默认钥匙串无残留变更。不能把模拟测试或现有 Developer ID 校验写成无 OU 实机通过。
- 本轮未开启真实历史 CLI 访问进行 #44 安装版复验；该项与 #45 原环境升级继续保留为未验证，不因发版自动关闭 issue。
- Beta 3 同时包含上一轮已验收的 #43 导入、管理与导出能力。不包含第三方私有数据库转换器。

## 发布

已发布 [0.1.0-beta.3](https://github.com/winx402/blocks-mac/releases/tag/v0.1.0-beta.3)，源提交 `6e291d4d134d708a2be9bda7ecf3dcbcc3932916`。使用独立临时发布检出保留用户工作区的既有 AGENTS.md 修改；只打包已提交代码。

- 主 App 与 Helper 的 marketing version 均为 0.1.0，build 3，release name 0.1.0-beta.3；Apple Silicon、macOS 14+。
- Developer ID 签名与嵌套代码审核通过，Apple 公证 `fb71f903-4171-4636-998f-037d1a844fb7` 为 Accepted，issues=null；DMG已stapled，挂载App的Gatekeeper结果为 Notarized Developer ID。
- DMG 22,392,556 bytes；SHA-256 `aa4601972cab1db98f636e5b465c859786ba6a36572767d7437a98f785a77327`。草稿和公开附件重新下载并验证长度、SHA-256、Sparkle Ed25519签名；匿名下载HTTP200且长度一致。
- 发布状态 published_feed_updated；公开 Beta appcast 首项为 build3，保留build1/2，不改变稳定频道。
- GitHub完整Source verification在交付核对时仍运行，不能将本地107项和分发门禁写成云端完整测试已通过。
