# 发布准备状态：2026-09-06

状态：准备中，尚无冻结候选，不放行公开发布。

## 当前用户范围

先修复顶部截图超时提示无法关闭及不协调的占位，再核对遗留验收模块并准备发布。不进行整体 UI 重设计。此前用户要求跳过的后续人工验收继续记为“跳过／未验证”，不再索取实体按键或其他人工配合，不转换为通过。

## 本次通知问题

- 源码链路已确认：ScreenshotStore 的超时错误创建 standard/error 通知，默认 manual dismiss；BlocksNotificationPanelPresenter 原先只看有没有额外 action，没有 action 就忽略全部鼠标事件。因此标准卡片本来存在的关闭按钮也无法点击，且此类错误不会自动消失。
- 原回归 `testIndependentNotificationHUDPassesMouseOnlyForActionableDescriptor` 也错误要求手动通知忽略鼠标。本轮改为验证标准卡片关闭控件可点击，同时保留 compact 无控件确认的鼠标穿透。
- 已修正为依据实际被接受的当前通知决定鼠标策略，避免低优先级 compact 消息被拒绝后仍使旧错误卡片不可点击。使用既有非激活面板类型，保持不成为 key/main。
- 去掉全局通知外层固定 150pt 高度与空白 Spacer，按现有卡片实际内容测量高度；保持原配色、字体、图标与 Surface，不做装饰性重设计。
- 初轮定向 `/private/tmp/blocks-notification-close-targeted-20260906.xcresult`：113 total / 113 pass / 0 skip / 0 fail；runner 0，无超时/进程清理。覆盖 AppAppearanceTests 与通知面板生命周期、新关闭/高度/优先级检查。
- 最终完整结果 `/private/tmp/blocks-notification-final-full-20260906.xcresult`：1865 total / 1849 pass / 16 skip / 0 fail，逐用例核对 1865 个唯一用例；start `1788626797.56`，finish `1788627035.853`。报告同名 `.json` runner 0、无超时/清理，用例树为同名前缀 `-tests.json`。16 skip 类型保持不变，P17-A 源码外观检查也通过。
- 两轮构建分别记于 `/private/tmp/blocks-notification-close-build-20260906.json`、`blocks-notification-final-build-20260906.json`，均保留首次编译子进程 0、ibtoold 残留受控清理后 runner 70，再增量 clean return 0 的事实。诊断 `/private/tmp/blocks-notification-final-diagnostics-20260906` 的 App 标准输出指定 CA/NSCGS、view-update、geometry、AttributeGraph、crash、TSan/Main Thread Checker 扫描无命中，未关闭历史间歇性 CA 告警。
- 通知图像附件已导出到 `/private/tmp/blocks-notification-final-attachments-20260906/5B9A3FBA-6FB2-4BE4-A878-EFF65CA9B07F.png`，实际查看是空白/透明图，已拒绝作为视觉证据。测试通过仅证明它记录了附件，不证明渲染正确；后续应修正渲染取证方式，不能用这张图交付布局验收。
- 当前 Blocks 主窗口只观察到快捷键与截图设置页，没有捕获到用户原始超时浮层；Chronicle 未运行，不读取历史录制。Product Design audit 的原提示截图证据缺失，不能宣称完成安装版布局审查。组件的自动化渲染与真实原提示应分开记录。

### 后续真实通知观察与关闭验证

- 首帧等待 300ms 后再缓存 NSHostingView，`blocks-notification-render-20260906.xcresult` 单项通过，但附件仍为空白。这不是有效视觉证据，未据此放行；该等待和无效导出随后移除，没有添加到产品逻辑。
- 改用隔离 XCTest 宿主直接显示生产 `BlocksNotificationPanelPresenter` / `BlocksNotificationCard`，仅临时延长测试窗口存活，使用 CUA 获取实际窗口截图。观察到约 300×65 的紧凑卡片，标题“截图超时”、说明及右侧关闭按钮均完整可见，没有固定 150pt 容器形成的大块占位。沿用现有图标、颜色、字体和 Surface，没有额外重设计。该实图在本任务 CUA 输出中，不冒称已导出独立 PNG 或完成原始用户现场的前后对照。
- 最终 `blocks-notification-live-observe-20260906.xcresult` 的临时场景中，CUA 在新鲜 AX 树定位“关闭”按钮并点击；随后的 AX 树明确移除通知节点 0–3，返回主窗口。测试宿主额外存活 8 秒用于观察关闭状态，诊断 `/private/tmp/blocks-notification-live-observe-diagnostics-20260906` 的标准输出记录 `NOTIFICATION_LIVE_FIXTURE_END visible=false`。不能把此前宿主太快退出造成的观察错误当作点击失败；此轮取得了关闭后的独立证据。
- 该临时场景结果 1/1，范围仅为现场观察，不替代正常回归的后续优先级断言。所有临时标题、等待、提前 return、日志探针和空白附件导出均已撤下；永久两项通知测试恢复完整断言。
- 撤下后的构建 `/private/tmp/blocks-notification-clean-test-build-20260906.json` 保留首次清理 70 和单次增量 clean 0；永久回归 `/private/tmp/blocks-notification-clean-test-20260906.xcresult`：108 total / 108 pass / 0 skip / 0 fail，覆盖整个 AppAppearanceTests 与两个通知面板回归，runner 0、无超时/清理。没有因仅调整测试取证代码而冒称新跑完整 1865 项。
- 最终测试源 SHA-256 `271fccdbf3c1be6d15db4b7bff6f68714de46a8c2de776c542db023546ad8039`。产品通知源码保持 `8b77c1d51737c8a1448f96a132b84ba3bf4702cc191e2c0e7d28bc9b9135cda4`，实际安装主二进制保持 `0c1051501f1b3d0e0f9154e5962696fb3be03c4f38bf0139a685027275130460`。主 App PID 33038 和 Helper PID 1463 未重启；只读 CUA 复核真实安装版的冻结开关仍为 on，未把隔离测试宿主的设置状态混作用户状态。

### 已形成的发布准备交付

[候选文件盘点](候选文件盘点-2026-09-06.json) 是精确变更路径与内容哈希的审核快照，不是冻结提交或公开发布清单。排除盘点文件自身后共 322 个变更/未跟踪文件：App 与检查/脚本 269、私有文档 32、明确排除的内部截图 3、独立网站 18；无未分类路径。真实 Signing.local / ReleaseIdentity.local 配置仍被 Git 忽略，未读取或收录其凭据。暂存区为空，没有提交或上传。

此快照仅包含工作树变更，不覆盖未变化的受跟踪依赖、忽略的本机配置，也不等于敏感内容审计已全部完成。准备范围及遗留门槛已可供候选决策使用；正式冻结/分发另按下节条件执行。

## 安装更新

- 已按此前批准的开发签名主 App 更新范围执行，不涉及公开分发、独立 Helper 或权限变更。原安装包先备份到 `/Users/bot/Applications/BlocksDev/Backups/pre-notification-fix-20260906.JznKt6/Blocks.app` 并严格签名校验通过。
- 标准安装入口 `BLOCKS_REQUIRE_STABLE_SIGNING=1 BLOCKS_USE_STABLE_SIGNING=1 ./script/build_and_run.sh --verify` exit 0；日志 `/private/tmp/blocks-notification-install-20260906.log`，主 App/CLI 构建成功，新主 App PID 33038、CDHash `86073df2be47b1ddf66813780f2bf98bafe2bf82`。旧 PID 63359 已退出。
- 稳定路径和构建产物主二进制 SHA-256 一致：`0c1051501f1b3d0e0f9154e5962696fb3be03c4f38bf0139a685027275130460`，`codesign --verify --deep --strict` 通过。该安装包含之前焦点缓存、结构拆分与搜索修复以及本次通知修复；开发签名安装不等于发布候选。
- 独立 Helper 仍是 PID 1463、SHA-256 `2a341479b4b931e13ecee08e01cfbbb1b2b2c7b1b1aca0bd965a8ac5eee11bc2`，未停止或替换。更新后 CUA 实查主窗口正常打开截图设置，原设置值保留，当前窗口没有超时卡片；未将“重启后未见”当作真实再现关闭操作的证明。
- 验证来源 SHA-256：BlocksNotification `8b77c1d51737c8a1448f96a132b84ba3bf4702cc191e2c0e7d28bc9b9135cda4`；ScreenshotAppStateTests `fd96730fad210b88fe68a519f66fb815a15c81e30104456999522eb5e4304660`。安装与最终测试后未再改产品实现；diff-check 通过。

## 模块与剩余验收

| 模块 | 已有证据及范围 | 未关闭项目 |
|---|---|---|
| 剪贴板 | 搜索截断修复 5/5；完整 XCTest 覆盖采集、历史、复制、筛选、详情、策略等自动化场景；修复已进入开发安装版 | 外部 App 粘贴/快捷键矩阵；用户跳过的人工部分仍未验证 |
| 截图、滚动截图、钉图 | 核心及 App 测试、独立大图资源专项有各自记录；用户确认过进入选择层和编辑器；通知修复已安装，生产组件实图及实际关闭已验证 | 原始现场前后对照/完整体验矩阵；历史 CA/NSCGS 告警根因；完整外部操作矩阵 |
| OCR | warm/质量/大图等专项记录存在 | system-cold 历史约 14 秒，仍超过严格 8 秒门槛；人工冷态配合已跳过，不能靠预热关项 |
| 翻译与 Selection Helper | 完整 App 测试覆盖翻译自动化；Helper loopback 有独立历史专项 | 当前签名 Helper 配对握手、断连/冲突的真实安装版证据；旧 Helper 不随主 App 更新 |
| 设置、权限、快捷键 | 注册/存储与自动化检查；旧安装版主要 4、快速粘贴 9、失败 0 的记录 | 实体输入/权限撤销恢复、VoiceOver、多屏/系统矩阵中未验部分已跳过 |
| Provider、插件、CLI、审计 | 存储、网络安全、渠道、Hook/权限等自动化及静态检查 | 公网 TLS 成功证据、正式渠道安装验证；CLI 集成保持用户原启用状态 |
| 分发与官网 | 渠道配置、发布脚本、unsigned 结构与网站草案存在 | 干净冻结来源、实签产物、公证/DMG/Gatekeeper、Store profile/Archive/上传、公开材料与真实链接 |

上一轮完整结果 `/private/tmp/blocks-search-batches-full-20260905.xcresult` 实查为 1864 total / 1848 pass / 16 skip / 0 fail。16 skip 是公网 TLS 1、资源 opt-in 2、Helper 13，不是“16 个功能都未测试”，也不能记成通过。此结果证明当时来源，不自动覆盖本次通知代码。工作树是否 modified 不能单独证明某文件晚于测试；覆盖关系必须核对内容哈希与时间。

## 发布准备顺序与边界

1. 通知修复、主 App 安装和实际组件显示/关闭检查已完成，原包回退副本保留。空白附件仍然拒绝，只采用后续 CUA 实图及关闭后状态作为本次视觉/点击证据。独立 Helper 和安全权限不变。
2. 已重新生成精确候选文件快照及内部截图排除范围；冻结前须核对快照哈希是否变化，并完成内容审核。旧 8 月冻结清单的数量失效，不执行 `git add -A`。
3. 确认候选范围/版本后才形成不可变来源并重新跑冻结门禁。准备发布不等于默认暂存、提交、上传或公开源码。
4. Developer ID / App Store 的正式签名与外部账号操作按对应门禁处理。已获授权的 Development profile 只支撑开发安装，不替代 Store distribution profile 或公证。
5. 公网 TLS、冷态 OCR、签名 Helper 及明确跳过的体验风险单列；如准备带已知风险发布，需要明确的范围/例外决定，不能由自动化绿色自行推导放行。

本次只读盘点子任务未执行构建、账户操作或写入。协作接口未暴露实际模型/推理元数据，未把请求角色当作实测模型事实。
