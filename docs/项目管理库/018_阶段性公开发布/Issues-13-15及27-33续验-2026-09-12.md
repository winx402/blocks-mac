# 截图续验与新增 Issue 修复方案

基线：`b70137f`。本文件是执行中记录，不代表 Issue 已关闭或版本已发布。

## 顺序与依赖

用户要求继续验收并关闭 #13–#15，随后处理新增 Issue。新增 #30（工具属性不刷新）和 #33（切换全屏 Space）直接覆盖旧截图验收范围，因此先修复这两个阻塞，再对最终安装版复验，不能先关闭旧问题。

## 方案

| Issue | 范围与方案 | 验证重点 |
| --- | --- | --- |
| #27 | 保留原生 titled 窗口圆角；仅让前景使用顶部安全区，保留非激活、缩放与既有 50pt 拖动区域。初版移除 titled 的方角回归已在实机发现并撤回。 | 安装版标题空白、首个控件、拖动与输入选择；浅深色。 |
| #28 | 独立呈现连接丢失 -1005、超时和 HTTP 429；提示网络扩展/代理可能影响，不宣称特定安全组件为已证明根因。保留原传输与默认源。 | 注入 transport 错误及取消，最终连接测试展示；不关闭安全保护。 |
| #29 | 使用紧凑的统一返回行，消除局部容器的叠加间距；不重复窗口标题，不全局缩小一级页间距，不采用负 padding 补丁。 | 翻译、标签、水印、Provider、插件二级页及返回位置/焦点。 |
| #30 | 属性子视图显式观察既有 ObservableObject；先修实证的迟到/缺失刷新，不混入尚未证明的零宽测量推测。 | 真实 NSHostingView 与安装版逐工具属性出现、数值可编辑。 |
| #31 | 基于现行同步实测布局建立稳定的上下工具区对齐基准；分开只读尺寸、操作与元素选择，不恢复异步估宽，不以固定大块空白替代协调。 | 元素增删、裁剪数值变化、窄屏及长语言时布局稳定；须与 #13/#14 回归。 |
| #32 | 剪贴板专用浮层提升至 statusBar 层级，物理底边 frame 算法不变；不采用 screenSaver 层级。 | 常显 Dock 下可见可点、物理底边、全屏/多屏及焦点。 |
| #33 | 编辑器从初始化起使用 nonactivatingPanel，禁止成为 main；交接只获取 key/first responder，不无条件激活 Blocks。 | 外部 App 与主 App 全屏、不切 Space、文字输入/菜单/弹窗及退出恢复。 |

## 当前证据

- 安装包代码签名检查有固定证书签名与 Team Identifier；`dev.sh doctor` 确认仍使用既有固定身份，无 profile 开发配置。未重置权限。
- 受控 TextEdit 合成文稿通过 CUA 进入全屏，NSWorkspace 只读确认真正前台；通过菜单完成真实全屏截图。
- #30 再次实机复现：矩形按钮 selected，属性行仍显示“选择对象后可调整实际属性。”；截图与 AX 一致。并非仅凭静态代码推断。
- #33 已做非激活补丁，P14-C 检查通过；尚未以该补丁安装版证明全屏 Space 保持，不把静态通过写成实机通过。
- 上次 #13–#15 浅深色显示、尺寸、圆角、撤销证据保留；完整语言/读屏、绘制及全屏 Space 问题必须对最终修改后版本继续复验。

## 安装版交互补证

- 固定证书整包安装两轮，均备份旧包。录屏、辅助功能、输入监控授权继续保留，未清理数据库或配对。
- 中文：矩形工具从空提示立即切换为描边、线宽、填充、圆角、不透明度控件。线宽 3→8 后实际绘制矩形；文字工具经真实输入框写入 `Blocks 验收 日本語 Test`，提交后图像中出现该标注。箭头、高亮、像素化的属性分别正确切换。最后撤销合成标注，矩形线宽恢复 3。
- 英文、日文：实际重启切换 App 语言并捕获受控文稿，工具条、尺寸/圆角文字和无障碍名称/值完整；日文尺寸弹窗存在可操作预设、宽高字段。语言和外观最终恢复跟随系统。这里验证的是 AX 名称和值，不声称进行了 VoiceOver 语音朗读全流程。
- #31：上下工具区使用主操作条的实际宽度作为共同边界，元素增删时外框没有随内容重新收缩/居中。顶部显示实际裁剪像素尺寸。属性内容在主操作条宽度内滚动，不撑大其外框。
- #33：外部 TextEdit 全屏进入编辑器后，NSWorkspace 真正前台仍为 TextEdit；实际绘制、文字输入均可用。退出后原合成文稿仍为全屏。Blocks 主窗口全屏路径也退出回同一全屏窗口。
- 主 App 自捕获补充发现：原全 App 排除策略产生黑图。改为入口冻结普通主窗口 ID 后，实机全屏图像包含 Blocks 设置页，仍排除选择遮罩。独立审查进一步指出智能候选穿透问题，已同步把同一冻结 ID 传入候选与遮挡分类，其余自身浮层仍排除，nil 会话保留旧行为。
- #32：临时设置底部常显 Dock。系统真实窗口列表中，剪贴板主面板层级为 25，Dock 为 20；主面板 `(0,794,1920,286)` 仍到物理屏幕底部 1080。底部合成记录可触发预览，预览同属层级 25。之后恢复 Dock 为 right/autohide=1，恢复剪贴板搜索。
- #29：实际日文水印二级页返回入口与正文可见，返回行采用 compact token，不重复系统窗口标题。一级页仍保留原顶部间距。其他二级页沿用统一容器，但不能把该水印样本写成五个页面都已逐一实机检查。

## 测试证据与边界

首轮 1967 passed / 3 failed / 16 skipped；其中两项为旧窗口样式/层级断言，另一项是新增属性测试错误依赖 NSView/AX 节点读取。第二轮 1973 passed / 1 failed / 16 skipped，唯一剩余是同一测试读取盲区。将测试改为直接宿主真实属性行（不重建宿主，切换工具后真实控件 fitting width 必须改变）后，相关三类定向测试 17/17 通过；随后捕获身份/过滤与窗口合同定向 7/7 通过。这些分轮结果不冒充一次完整全绿。

P14-B 的两条旧基线失败在 HEAD 纯读取复跑同样存在：它只接受旧无 owner 的会话调用与旧 completion 捕获列表，且不理解两个 defer/独立输出 helper。已更新为验证现行 owner 对称 begin/end、特定恢复 defer 和输出 helper，未删除恢复/会话合同。P14-B/C 和 UI design-system 门禁通过。

本机没有完成真实多显示器、所有系统辅助显示选项及 VoiceOver 语音导航全矩阵；保持发行验收的未验证标记。#28 只修复诊断分类与提示，不声称本机网络过滤或上游限流已经消失，也没有切换传输栈、关闭安全保护或停用默认源。

## 最终定向复验

- 最新定向 XCTest 为 **39 passed / 0 failed / 0 skipped**（`Test-BlocksAppTests-2026.09.12_17-09-45-+0800.xcresult`），覆盖最终属性行、候选与遮挡、选择层优先级、会话冻结身份、翻译顶部拖动区域、网络错误、设置二级路由、Dock 层级与几何。
- 最终安装保留固定身份及原路径，旧包备份为 `DevelopmentInstall.noindex/.BlocksDev-install-1m3apuwh/previous.app`。
- #27 最终实机：标题上方空带消失，四角仍为原生圆角；标题真实拖动使 640×520 窗口从 `(640,295)` 到 `(700,330)`，反向拖动恢复。没有把初版方角样本记为成功。
- 独立审查的自捕获候选穿透问题已修正，新增完整/局部遮挡及 nil 原行为测试通过。尚未进行“两个普通窗口部分重叠”的冻结/非冻结双实机矩阵，不把纯测试写成实机。
- 本次按问题复现路径交付 #13/#14 的完整显示及 #15 的全屏呈现修复，语言、实际工具操作与外部/主 App 全屏证据见上；更广的发行硬件/辅助功能矩阵仍保留，关闭具体缺陷不代表整个发行验收完成。
- #28 保留开放：诊断已改善，但外部网络过滤/服务限流是否恢复未验证。#29 保留开放：统一紧凑返回区补丁和水印样本已通过，余下二级页尚待逐页验收。
