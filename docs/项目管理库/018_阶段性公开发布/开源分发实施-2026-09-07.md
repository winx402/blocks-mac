# 开源与独立分发实施

状态（2026-09-08）：实施中；仓库已公开并采用 MIT，未发行安装包。

用户确认：MIT，GitHub 公开，不再上架 App Store；Apple Silicon / macOS 14+
发行包；无证书完整 LocalDevelopment 可非沙盒，官方发行版保持沙盒；Helper
随主 App 打包按需启用；本机签名、公证、准备草稿后发布，CI 不持有发布密钥。

当前正式工作区为 `/Users/bot/Documents/blocks-mac`，正常跟踪 GitHub。原工作区
保留，不重置、不导入未经审查的历史，不再以长期手工快照同步作为发布流程。

交付门槛：完整历史/资源/Issue/日志公开审计 → 无证书开发版全功能与隔离验证 →
签名官方首装 → Sparkle 跨版本升级、数据与 Helper 迁移验证 → 首个 Beta。
不得用仅编译通过代替这些验收。尚未完成的入口必须明确失败，不能伪造可安装发行版。

## 已确认事实

- 公开前审查了原始四个提交、1199 个 Git blob、Issue/评论和媒体资源；公开准备提交为 `371d2d0`。
- `winx402/blocks-mac` 已变更为 public；GitHub 已识别 MIT。
- 新增 LocalDevelopment 配置、隔离运行身份、开发/安装/发布脚本及 Sparkle 接入正在整体验证；尚未提交的实现不能视为已交付。
- 安装入口隔离测试通过；数据库在线备份、迁移回滚、未来版本拒绝、更新 admission 与取消恢复的隔离测试通过。以上均不等于真实首装或升级通过。
- 本机存在有效 Developer ID Application 身份；目前定位到的 provisioning profile 是开发用途，未确认主 App/Helper 的 Developer ID 分发 profile、公证配置及更新签名配置。
- 已用 Sparkle 2.9.6 官方工具创建独立钥匙串 account `app.blocks.app.sparkle`。公钥为 `ghXr46h3DqsfqMwzwHDrsAwh4M4aTVxD9rcsndtFrt0=`；私钥未导出。官方配置已内置公钥，本地开发配置仍禁用官方更新。公证配置与分发 profile 仍未完成。

## 验证进展与剩余门禁

- LocalDevelopment 主 App、CLI、Helper 完整构建通过；最终 DebugTesting 的实际 XCTest 结果为 1902 通过、0 失败、16 跳过（总数 1918），不能把跳过项算通过。一次测试构建本身返回 0，但 ibtoold 遗留触发监督门禁失败并被清理；之后干净增量复跑通过，未将该失败当作通过。
- 已通过 `dev.sh run` 首次安装并启动 `~/Applications/BlocksDev/Local/Blocks Dev.app`：ad-hoc、`app.blocks.dev`、无 Team ID、主 App 空 entitlements；严格全包签名校验通过，身份清单 mode 0600。设置页实际显示开发版禁用官方更新、内置 Helper 已发现但未配对。
- 不带证书/profile/本地覆盖的完整开发体验，以及权限、钥匙串重启读取、CLI、插件和 Helper 配对。
- 干净账户 DMG/脚本首装、跨版本与 Debug 迁移、故障场景及 #6/#7 真实体验复验。
- Beta 发行和后续版本升级演练。未创建发行包或 Release。`gh-pages` 的独立根提交 `20edd04` 仅包含 `.nojekyll` 和空 stable/beta appcast，GitHub Pages 已构建成功；两个 HTTPS XML 地址实读成功，未改动 `site/` 或现有官网。

开发版实际权限状态：辅助功能和屏幕录制未授权；已请求用户确认本次验收授权，尚未把截图/自动粘贴标为通过。
源码身份与清单读取的隔离安全测试为正式分支 33 项、本地分支 52 项；开发安装事务 12 项通过，
包含首装/升级在 manifest 写入前后中断、错误角色/Hash、私有权限和并发空目标保护。
发布测试新增真实临时 Git 仓库冻结、整个 prepare 互斥、单调构建号和 Developer ID profile 内容校验；
这些隔离测试均未调用真实公证或上传。

Gitleaks 8.30.1 已扫描工作区和 main 的五个已有提交；四个初始命中均为测试常量/类型表达式，
经逐行确认后配置文件+完整行的交集例外，复扫未发现未处理命中。不排除整目录或整条检测规则。

更新签名真实自测：官方 `sign_update --account app.blocks.app.sparkle` 对两个合成测试文件签名成功，
输出经内置公钥 Ed25519 验签通过；归档模式输出已通过发布脚本解析并核对字节数。
测试 `.dmg` 只是明确标记的非安装合成字节，不是可挂载发行镜像，不计为签名/公证发行包验收。
主 App 的公开 Team/证书指纹策略已写入 `script/release/install-release-policy.json`；本机忽略的
ReleaseIdentity 只指定 Team，不共享或猜测主 App/Helper 的 provisioning profile。
