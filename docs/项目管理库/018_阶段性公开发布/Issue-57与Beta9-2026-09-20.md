# Issue #57 与 Beta 9

## 方向与边界

复核开放项后，本轮新增需求为 #57，关联 #50。其余旧项没有新的原环境证据，保留既有状态。上一版修正后完整 CI（35499042055）已通过。

选择安装器编排而非重写业务传输。命名 NSXPCListener 仍需 launchd 宣告服务，直接移动到普通 GUI App 不能解决发现路径；已注册 SMAppService 再 register 也不保证刷新二进制锚定。依据：[Apple listener](https://developer.apple.com/documentation/foundation/nsxpclistener/init%28machservicename%3A%29)、[Apple register](https://developer.apple.com/documentation/servicemanagement/smappservice/register%28%29?language=objc)。

新增仅源码版启用的固定私有 AF_UNIX 管理端点，复用已有 audit-token/CDHash/路径验证，App 只接受已登记旧 CLI，CLI 验证旧 App。probe/prepare/commit/cancel 同连接绑定 session+token，业务动作仍走原 Broker。管理请求不占业务 admission lease；App 完整更新事务证明闲置后才准备成功，commit 回执完整发送后才正常退出。

安装器先完成新包验证；旧包与旧 peers.json 保持不变直至认证准备结束。CLI 回执还须结合旧进程/job 缺失证据，才允许原子替换。失败沿用原恢复材料；仅原安装身份与清单仍匹配时请求重开恢复。不执行 launchctl bootout 或强杀。

beta.8 缺少此管理端点，不能靠新 CLI 绕过旧身份；首次迁移保留一次兼容流程。正式发行构建不启用此通道。原坏状态 #50 与实际跨版本成功交易仍需单独核验，不把 socket/状态机测试等同全部实机通过。

## 验证与发布

- LocalDevelopment 完整工程构建通过（仅构建，未安装）；DebugTesting 受控构建与相关58项XCTest通过。
- 最终全量2129项：2112通过、17跳过、0失败。结果：`Test-BlocksAppTests-2026.09.20_22-40-36-+0800.xcresult`。
- 独立传输检查：Local 9/9、Official 3/3，两个模式的优化CLI fixture通过；涵盖token/status、截断/超限帧、绝对deadline、同UID不等于可信、真实隔离目录的bind/lock/stop与正式版unsupported。
- 安装管理11个隔离情景通过，包括旧版本兜底、错误身份、错误回执、超时、后来出现的job、App/manifest变化，以及真实临时文件事务中“准备在换包前”和“换包失败恢复并请求重开旧App”。签名/管理应答在这些安装fixture中模拟，不伪称真实已安装App握手。
- 既有安装事务、release xcconfig、CLI优化输出23例、应用更新安全、退出watchdog与UI设计系统检查通过。
- 独立审查发现并修复：取消Task污染恢复、poll首轮15秒提前结束、ACK与本地timer竞争，以及断连先于排队prepare的顺序风险。新增回归覆盖这些情况。
- 尚未在用户真实已配对/已授权的安装上完成“新协议版本→下一次源码重装”的签名成功交易与数据/权限保留实机验收；beta.8首次迁移也保留明确兼容提示。因此本项不因单测通过直接关闭。

## 发布完成

已发布 [0.1.0-beta.9](https://github.com/winx402/blocks-mac/releases/tag/v0.1.0-beta.9)，源提交 `7ce7ea5ec0fe61bab4e7e62a4058e82f12739f93`，build9、arm64、macOS14+。

- 主App/内置Helper签名、角色、沙盒及P20发行审计通过；未增加正式版权限或管理通道。
- Apple公证 `95c95f6a-b11d-4b33-a386-424d07d1487c` Accepted；DMG已stapled并通过Gatekeeper。
- DMG 24,683,006 bytes，SHA-256 `76eccf1f4938fbeac260271485d739761bb2d268539927777eebfa131a9d647c`。
- 草稿/公开附件长度、摘要、Sparkle签名均复核后更新Beta源，状态published_feed_updated；公开下载HTTP200，GitHub Pages部署后在线首项确认为build9。
- 实际签名发行CLI执行 `source-upgrade --json` 返回unsupported/exit1，确认正式构建不启用管理通道。
- [该提交的云端完整CI](https://github.com/winx402/blocks-mac/actions/runs/35517590753) 截至记录时仍在运行；本机全量结果单独见上节，不混为一项。
- #57实现与兼容限制已回填，真实已安装App/CLI成功握手与下一次源码重装仍待验收；保留开放。#50旧坏状态及其他原环境验收不在本次假定通过。本机日常安装版未替换，用户现有AGENTS修改保留。
