# Issue #46：Xcode 27 实机工具链复验

## 环境与结论

- macOS 27.0（26A428）、Apple Silicon。
- Xcode 27.0（27A266a），Apple Swift 6.4（swiftlang-6.4.0.34.1）。
- 验证源码：`6768fcf1bcdf174fd6be89cd7fa3dc011edabe41`，包含此前规避提交 `a45bf4f`。
- 本次未重现原 SimplifyCFG 崩溃。之前已实现的结构规避通过原报告版本工具链验证，无需新增业务代码或关闭优化。

## 安装边界

使用用户从 Apple 官方下载的 `Xcode_27.xip`；归档检查为 signed Apple Software，展开后 codesign 深度严格校验及 Gatekeeper 检查通过（Apple System）。独立安装在 `/Applications/Xcode-27.app`，用户自行完成首次启动协议。Xcode 26.6保留在 `/Applications/Xcode.app`，全局 `xcode-select` 未更改。构建通过每条命令的 `DEVELOPER_DIR` 指定27工具链。

## 验证

1. 独立目录 `~/Library/Caches/BlocksDev/Issue46-Xcode27.noindex` 完整构建 BlocksCLI，LocalDevelopment，arm64，显式 `SWIFT_OPTIMIZATION_LEVEL=-O`。构建退出0；日志中的实际 SwiftDriver 命令为 Xcode-27 路径并带 `-O`，不是复用26工具链产物。
2. `DEVELOPER_DIR=/Applications/Xcode-27.app/Contents/Developer ./script/dev.sh build` 完成 Blocks、BlocksCLI、BlocksSelectionHelper 三个 scheme，退出0；未安装替换用户日常 Blocks。
3. `cli_execution_output_self_test.py`：23项 `swiftc -O` 隔离执行/输出测试通过。仍为假Broker测试，不替代真实XPC。
4. 新构建真实CLI的9个 `--dry-run` 通过：capture、history.query、history.search、ocr.status、ocr.retry、history.export、scrolling.status、scrolling.finish、scrolling.cancel。均返回completed及dry_run=true；未生成输出文件、未请求真实截图或读取用户剪贴板。

本机原始日志：`/private/tmp/blocks-issue46-xcode27-cli.log`、`/private/tmp/blocks-issue46-xcode27-full.log`。临时日志不承诺长期保存；复现命令和关键结果保留在本文。

## 未涵盖

没有重新构建崩溃前的旧提交，因此不声称确定编译器内部根因；没有验证Xcode27其他版本、官方公证发布、所有运行时功能或其他开放Issue。构建中存在既有弃用/API并发警告，但没有编译器崩溃或构建失败。用户原有AGENTS.md修改保留。
