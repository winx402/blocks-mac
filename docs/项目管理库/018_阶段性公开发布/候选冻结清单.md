# 候选冻结清单

状态：待冻结／不得发布

盘点日期：2026-08-16（以下为历史盘点，数量已失效；2026-09-06 的精确路径/哈希快照见[候选文件盘点](候选文件盘点-2026-09-06.json)，审核状态见[发布准备状态](发布准备状态-2026-09-06.md)。新快照仍非冻结提交，暂存前必须核对内容与哈希。）

基线提交：`d8982e398db4ecd90a20c4790224eab33051ac03`

本清单把当前 285 个工作树文件条目分成互斥范围（折叠口径 243=221+22，展开口径 285=221+64），防止一次性 `git add -A` 把内部证据、官网草案和 App 候选混成同一提交。当前暂存区为空、无冲突；任何文件数量变化都必须重新盘点，不能沿用本清单的计数。

## A. App 与门禁候选：234 个折叠条目（展开 242 个文件）

建议作为同一可重建 App 候选范围复核，但本清单不授权自动暂存或提交：

- 已跟踪：`.gitignore` 1 个、`apps/Blocks/**` 163 个、`tools/verification/**` 51 个。
- 未跟踪 `apps/Blocks/**` 15 个：
  - `Blocks.xcodeproj/xcshareddata/xcschemes/BlocksAppTestsNetwork.xcscheme`
  - `Blocks.xcodeproj/xcshareddata/xcschemes/BlocksSelectionHelper.xcscheme`
  - `BlocksApp/App/DistributionChannel.swift`
  - `BlocksApp/Blocks-Development.entitlements`
  - `BlocksApp/Blocks-Testing.entitlements`
  - `BlocksApp/Features/Clipboard/ClipboardFeatureCoordinator+CopyActions.swift`
  - `BlocksApp/Features/Clipboard/ClipboardFeatureCoordinator+PanelPresentation.swift`
  - `BlocksApp/Features/Translation/TranslationFeatureCoordinator+PluginActions.swift`
  - `BlocksApp/Resources/InfoPlist.xcstrings`
  - `BlocksApp/Services/DiagnosticsExportService.swift`
  - `BlocksSelectionHelper/BlocksSelectionHelper.entitlements`
  - `BlocksSelectionHelperTests/SelectionHelperServerTests.swift`
  - `Config/Distribution.AppStoreBeta.xcconfig`
  - `Config/Distribution.DirectBeta.xcconfig`
  - `Config/ReleaseIdentity.local.example.xcconfig`
- 未跟踪发布脚本 9 个（折叠为 1 个 `script/release/` 条目）。
- 未跟踪门禁 3 个：`tools/verification/p14h_selection_helper_loopback_gate.py`、`p20_release_distribution_checks.py`、`verification_build_helpers_self_test.py`。

两份 Distribution xcconfig 是正式发布脚本通过 `-xcconfig` 加载的渠道契约，不是孤立模板；遗漏它们会使 Direct／Store 构建不可重现。真实 `Signing.local.xcconfig` 与 `ReleaseIdentity.local.xcconfig` 继续由 `.gitignore` 排除，禁止强制加入。

当前新增行与未跟踪文本的非回显扫描未发现私钥块、OpenAI／GitHub／AWS／Slack token 前缀或本机 `/Users` 路径。两处带用户名密码形式的 URL 均位于测试文件并使用保留的 `example.invalid`／`example.test` 域名，是拒绝输入的 fixture；冻结复核仍须检查实际暂存内容，不能只依赖当前工作树扫描。

## B. 项目私有文档候选：7 个折叠条目（展开 14 个文件）

建议与 App 代码分开提交，并保持为项目私有资料：

- 已跟踪修改的 `docs/**`：6 个。
- 未跟踪 Markdown：`018_阶段性公开发布` 下 8 个（含本清单，折叠为 1 个目录条目）。
- 安装版阶段截图：`01`、`02`、`06`、`07`、`08`、`09`、`10`、`11` 共 8 张。

`门禁与验收记录.md` 含内部调试轨迹、临时结果位置和身份／发布过程信息，不是对外发布说明。其余八张截图目前只有既有目视记录支持“未见凭据或个人信息”；若要进入公开材料，仍须重新逐张审核。

## C. 明确排除：1 个折叠条目（展开 11 个文件）

以下内部证据图含 Codex／额度测试文字和 UUID 型测试内容，不得原样进入冻结提交或公开材料：

- `docs/项目管理库/017_全局UI与交互设计系统/evidence/installed-app-2026-08-12-prelaunch/03-clipboard-bottom.jpeg`
- `docs/项目管理库/017_全局UI与交互设计系统/evidence/installed-app-2026-08-12-prelaunch/04-clipboard-left.jpeg`
- `docs/项目管理库/017_全局UI与交互设计系统/evidence/installed-app-2026-08-12-prelaunch/05-clipboard-right.jpeg`

如需保留证据链，应迁入不会随候选提交发布的内部存档；如需入库，应先用纯合成内容重新采集。

## D. 官网候选：1 个折叠条目（展开 18 个文件），单独冻结

`site/**` 当前 18 个未跟踪源码、配置、测试和静态资产不与 App 候选混提。源码实现支持静态渲染、`noindex`、无表单、无运行时遥测和无外部脚本；`node_modules`、`.next`、`dist` 等生成物已由 `site/.gitignore` 排除。

- `settings.jpeg`、`translation.jpeg` 已逐张目视并复核常见来源／定位元数据，未见凭据、个人信息或内部调试内容。
- `clipboard.jpeg` 没有敏感信息，但仍显示 `Synthetic clipboard/performance record` 压测文案，且最右卡片在画面边界截断；正式公开前必须从脱敏、真实产品示例重截，不能用生成图替代真实 App。

官网冻结前还须在干净依赖环境执行 `npm ci`、lint、静态渲染测试，并检查生成站点继续无客户端脚本、表单、可用下载和索引入口。网站文案中的“App 无自动遥测”等产品声明必须由 App 独立证据支持，不能由静态站测试自证。

## 冻结顺序与停止条件

1. 先确认当前工作树仍恰好为 A 234 个折叠条目（展开 242）、B 7 个折叠条目（展开 14）、C 1 个折叠条目（展开 11）、D 1 个折叠条目（展开 18）；数量或路径变化即停止并重盘。
2. 对 A 执行 `git diff --check`、Swift／Python／Shell 静态检查及敏感内容非回显扫描；只按精确路径暂存，并复核 `git diff --cached --check` 与 `git diff --cached --name-only`。
3. 从 A 的不可变提交重跑 Debug／Release／AppStoreRelease、Analyze、完整 `BlocksAppTests`、`BlocksScreenshotCoreTests`、Selection Helper、P5-F、P14-F、P14-H、P18、P19-A 与 P20。P19-A 是独立的性能治理门禁，不由 Xcode test scheme 代跑；现有可变工作树结果不能继承为冻结结果。
4. B 使用独立私有文档提交；C 始终排除；D 在真实 clipboard 素材重截并通过站点测试后独立提交。
5. 任一 P0／P1、测试失败、意外跳过、签名身份不匹配、敏感文件进入暂存区或 P18 cold `>= 8,000 ms`，都必须停止放行。

即使上述冻结完成，真实 Developer ID／Apple Distribution 分层签名、公证、DMG、Gatekeeper、Store Archive／上传、已安装签名 Helper、外部 App 物理快捷键、VoiceOver、多显示器和干净机矩阵仍是独立上线门禁。
