# 上线前检查清单

状态：持续维护；当前发布候选：未指定
适用：Blocks Direct Beta、TestFlight / App Store Beta 与 Mac App Store 正式提交

> 本清单是发布门禁，不是已通过结论。当前脏工作树、unsigned 结构构建和本机 Debug 安装版均不得标记为发布候选。每次候选须重新填写版本、提交、构建时间、渠道和证据链接；空白即未通过。

## 当前执行约束（2026-09-05）

用户明确要求跳过后续人工验收。需要用户实体按键、选区/外部 App 操作、VoiceOver 朗读反馈、人工多屏/多系统/冷态配合或其他人工操作的未完成项目，本轮统一记为“用户要求跳过 / 未验证”，不再反复向用户索要验收操作。已获得的用户确认与自动化证据保持各自范围，不把跳过项勾选为通过；自主可执行的审查、测试和修复继续进行。此执行调整不是上线放行、修改系统权限、更新独立 Helper、重启系统或外部发布授权。

## 候选身份与证据抬头

- [ ] 候选版本、build number、渠道、架构、最低系统已冻结：`______`。
- [ ] 候选提交为干净、可复建的 commit：`______`；`git status --short` 无发布范围外改动。
- [ ] 构建机、Xcode/macOS、构建时间、执行人已记录：`______`。
- [ ] 每项证据可追溯（命令输出、测试报告、截图、日志、工单或审核记录）：`______`。

## 必须通过（任何一项未通过即不得生成、上传或公开候选包）

### 版本、构建与产物

- [ ] 版本、build number、Git commit、DMG 名称、更新说明和官网不可变 URL 完全一致；旧包不覆盖。证据：`______`。
- [ ] 从冻结提交全新构建 Direct Beta / Store Archive，且未使用 `--unsigned`；重建产物哈希一致或差异已说明。证据：`______`。
- [ ] arm64、macOS 14+、渠道编译开关和包内容符合[渠道能力矩阵](渠道能力矩阵.md)。证据：`______`。
- [ ] 测试门禁与 scheme 清单已复核；项目历史上不存在独立 `BlocksCoreTests` target／scheme，批准以完整 `BlocksAppTests` 中 12 个直接导入 `BlocksCore` 的测试源承载插件、Clipboard、Selection 与翻译核心契约，并以 `BlocksScreenshotCoreTests` 承载独立截图纯逻辑门禁。该替代不宣称覆盖 `BlocksCore` 全部 public API；两套门禁都必须从冻结提交重新执行。证据：`______`。

### 核心功能与权限

- [ ] 剪贴板：文本、URL、图片、文件、富文本的采集、搜索、详情、复制、粘贴、删除、恢复和历史持久化通过；已线性化的物理写入即使随后取消也会收敛并记录 recency，但失效请求不得再发 Cmd+V、HUD／通知或插件终态；辅助功能重试不得重写 Pasteboard，失败 recency 必须重试或明确反馈；不会泄露或意外覆盖剪贴板。证据：`______`。
- [ ] 截图：捕获、编辑、裁剪、OCR、翻译、钉图、保存/复制、取消和重试通过；鼠标打开“更多工具”不抢当前第一响应者，后台／定时插件不能伪装成用户动作执行 mutation／copy／save／complete；大图资源限制、失败路径和临时文件清理已验证。证据：`______`。
- [ ] 钉图 OCR close 生命周期：UI 关闭后显式 copy 行为、controller ownership 释放及累积 AppKit 状态下的回归均通过。证据：`______`。
- [ ] 翻译、Selection Helper 与语言包：手动/选中文本入口、Helper 配对/冲突/断连、四源并发、复制结果、离线/超时错误及中英日回退通过。Store 渠道确认 Helper 入口不存在。证据：`______`。
- [ ] 设置、快捷键和权限：重启后设置与快捷键一致；默认 Control + Option + D / Control + Option + V / Control + Option + A 在真实全局输入链路及 TextEdit、Safari、Preview、Electron 焦点中通过；若候选安装版存在用户自定义或禁用，以“设置 > 快捷键”显示的当前绑定为准；拒绝、授予和撤销辅助功能、屏幕录制、通知等权限均可恢复。证据：`______`。
- [ ] 插件与 Hook：内置/外部插件渠道边界、签名/哈希/权限披露、安全模式、安装/禁用/卸载、Hook 失败隔离和图片 payload 保留均通过；用户发起的 will Hook 不能把其请求的 host action 升级为 `explicitUser`，只有直接 UI action 可携带该来源；Store 包无外部代码入口。证据：`______`。
- [ ] Provider、CLI、本地自动化与数据审计：密钥仅经 Keychain 管理，替换/删除/失败回滚正确；CLI、ActionBroker、LaunchAgent 仅在允许渠道可用；本地自动化显式授权；审计与诊断导出脱敏且不含内容、路径、凭据或标识。证据：`______`。

### 体验、可访问性与可靠性

- [ ] UI 在浅/深色、窗口尺寸、三种语言与减少动态效果下无截断、抖动、横向溢出或无意动画；主题切换后状态正确。证据：`______`。
- [ ] 鼠标、键盘导航、焦点、Esc、菜单和 VoiceOver 的名称、角色、值、顺序与动态反馈可用；关键流程不依赖鼠标。证据：`______`。
- [ ] 关键流程性能基线已记录；冷启动、剪贴板打开、截图编辑、翻译、插件 Hook、设置切换无可复现卡顿。证据：`______`。
- [ ] OCR 的 system-cold 用户首次真实识别（候选安装版、受控重启／冷态、无并发构建，从点击到结果）与 warm corpus 均重测；单项严格 `< 8,000 ms`。corpus 进程首次、预热完成后的用户首次与 system-cold 必须分栏记录，预热或已加热的 Vision 结果不得冒充冷态修复；同时记录环境敏感风险和主线程响应。证据：`______`。
- [ ] 在定义的重复场景完成 100 轮（含启动/退出、剪贴板、截图、翻译、权限/错误恢复）；0 崩溃、0 数据损坏、无 P0/P1。证据：`______`。

### 数据生命周期与安全

- [ ] 从上一正式/Beta 版本覆盖升级：数据库、设置、快捷键、权限身份、插件和历史数据迁移正确；失败可恢复。证据：`______`。
- [ ] 备份、导出、清除数据、卸载重装、降级/回滚的影响、步骤和可恢复范围已实测并写入发布说明。证据：`______`。
- [ ] Secret、Token、订阅链接、个人数据不进入仓库、包、崩溃日志、诊断、控制台或官网；发布前密钥与构建配置复查完成。证据：`______`。
- [ ] 日志最小化且脱敏；沙箱、TCC、文件选择、Keychain 访问和 IPC/XPC 边界符合渠道要求。证据：`______`。
- [ ] 网络仅使用 TLS，证书/重定向/超时/取消校验有效；Provider、插件与更新网络路径已审计，SSRF、内网/loopback 和 fake-IP 策略为 fail-closed。证据：`______`。

### 签名、分发与干净机

- [ ] Developer ID 候选：主 App、Helper、嵌入代码和 DMG 均以正确身份分层签名、Hardened Runtime、安全时间戳；`codesign --verify --deep --strict` 通过。证据：`______`。
- [ ] DMG 文件大小、SHA-256、公证 Accepted/notary log、staple、`stapler validate` 与 Gatekeeper assessment 均通过。证据：`______`。
- [ ] App Store 候选：Explicit App ID、profile、Apple Distribution、Archive、最终 entitlements、嵌入代码及上传前审计均通过；不得以 unsigned Archive 代替。证据：`______`。
- [ ] 在 Apple Silicon macOS 14、15、26 的干净账户/机器完成浏览器下载、隔离属性、安装、首启、权限、更新和卸载验证；Store 与 Direct 渠道分别记录。证据：`______`。

### 对外材料、合规与支持

- [ ] 官网、下载页、校验值、最低系统、渠道差异、已知问题、更新与回滚说明与候选完全一致；HTTPS、下载和支持链接真实可用。证据：`______`。
- [ ] 品牌、域名、Bundle ID、商店名称、截图、描述、类别、年龄分级、审核说明、出口合规、隐私标签与实际行为一致。证据：`______`。
- [ ] 隐私政策、条款、安全披露、第三方许可证和支持/隐私/安全邮箱已审阅并完成真实收发验证。证据：`______`。

## Beta 门禁（适用于对外 Beta；未完成不得扩大测试或进入下一渠道）

- [ ] Beta 1 仅按批准范围低调公开；下载对象、SHA-256、反馈路径和已知问题可用。证据：`______`。
- [ ] 灰度名单、上限、停止条件、撤包联系人与回滚版本已预设；无自动放量。证据：`______`。
- [ ] 每个 P0/P1 有分级、负责人、响应时限和用户恢复方案；崩溃、数据丢失、权限绕过、Secret 泄露和远程代码执行视为停止条件。证据：`______`。
- [ ] Beta 1 → Beta 2 手动覆盖升级在干净机通过，且无数据/权限/快捷键回归。证据：`______`。
- [ ] 连续 7 天无未解决 P0/P1，核心路径、崩溃和支持反馈已复盘，才可进入 TestFlight 外部测试。证据：`______`。
- [ ] Beta 3 的更新机制（如 Sparkle）单独完成签名、降级防护、失败恢复和干净机更新验证；未验证前保持手动更新。证据：`______`。
- [ ] 连续 14 天无未解决 P0/P1，渠道能力、商店材料和审核路径复核后，才可提交 Mac App Store。证据：`______`。

## 上线后观察（发布不等于通过；按候选版本持续记录）

- [ ] 发布后 0–24 小时：下载/安装/Gatekeeper、首启、权限、崩溃、性能与支持邮箱按渠道巡检；异常与处理：`______`。
- [ ] 第 7 天：P0/P1、崩溃、卡顿、数据迁移、插件/Provider 网络、无障碍与三语反馈复盘；结论：`______`。
- [ ] 第 14 天：重复第 7 天指标，核对灰度扩大或商店提交的门禁；结论：`______`。
- [ ] 所有遥测仅在用户明确同意且隐私材料已更新后启用；当前无自动遥测。未启用时以脱敏诊断、支持工单和人工验证记录为证据。证据：`______`。
- [ ] 发现停止条件时：立即停止放量/下架下载入口，保留证据，发布恢复或回滚指引，并记录复发防护。证据：`______`。

## 本次放行结论

- 候选：`______`
- 渠道：`______`
- 必须通过：`通过 / 不通过（未验证）`
- Beta 门禁：`通过 / 不通过（未验证）`
- 放行决定、日期与负责人：`______`
- 未解决风险与例外批准（无批准即不放行）：`______`
