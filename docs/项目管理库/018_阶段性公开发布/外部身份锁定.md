# 积木工具外部身份锁定

状态：local-identity-implemented / main-app-id-registered / development-profile-imported / signed-development-app-installed
锁定日期：2026-08-08

## 品牌

| 场景 | 最终名称 |
|---|---|
| 简体中文产品名 | `积木工具` |
| 英文产品名 | `Blocks for Mac` |
| 日文产品名 | `Blocks for Mac` |
| 工程技术名 | `Blocks` |
| CLI | `blocks` |

“积木 AI 工具箱”只作为产品描述，不作为正式产品名。`Orange Forge` 不作为开发者品牌、产品品牌或版权主体文案；公开检索已存在同名 Apple 生态软件工作室，但这只证明命名碰撞，不构成商标结论：<https://orangeforge.agency/>。

## 域名与公开地址

域名 `orangeforge.top` 继续由阿里云作为注册商。2026-08-08 只读核验时域名状态正常、自动续费开启、到期时间为 2027-05-09，权威 DNS 除 NS/SOA 外没有 A、AAAA、CNAME、MX、TXT 或 CAA 业务记录。

| 用途 | 锁定地址 |
|---|---|
| 官网 | `https://blocks.orangeforge.top/` |
| 下载 | `https://downloads.orangeforge.top/` |
| 版本记录 | `https://blocks.orangeforge.top/releases/` |
| 隐私 | `https://blocks.orangeforge.top/privacy/` |
| 支持 | `https://blocks.orangeforge.top/support/` |
| 安全反馈 | `https://blocks.orangeforge.top/security/` |
| Beta appcast | `https://downloads.orangeforge.top/appcast/beta.xml` |

`orangeforge.top` 与 `www.orangeforge.top` 后续统一跳转到官网。预留 `support@orangeforge.top`、`privacy@orangeforge.top`、`security@orangeforge.top`；邮箱未完成收发验证前不得在公开页面宣称已启用。

## Apple 与本地身份

主 App 的 Direct Beta、TestFlight 和 Mac App Store 构建统一使用 `app.blocks.app`。2026-08-08 已在 Apple Developer 注册表单中通过实时校验并进入确认页，但没有点击 Register，因此 Apple 后台仍未创建该 App ID。

2026-09-05 更新：用户明确批准创建主 App ID 和开发签名配置文件。现已在当前登录的 Apple Developer 账号注册 Explicit App ID `app.blocks.app`，描述 `Blocks for Mac`，注册后列表已显示该条目。未额外启用可选 capability，未创建商店记录。macOS App Development profile 尚未生成：选择该 App ID 与已有开发证书后，页面显示没有可用测试设备；本机 UDID 提交/设备注册仍待用户确认。

同日后续：用户明确批准读取并向 Apple 提交本机 UDID。本机已注册为 macOS 开发测试设备；随后官方页面确认主 App 的 Development profile 已生成，包含 1 张现有开发证书、1 台测试设备，有效期至 2027-09-05。设备标识符与签名文件内容不写入仓库。当前浏览器仅返回下载事件，尚未取得可校验的本地文件，故未导入、未验证 entitlement 授权或重新构建安装。

同日文件交付后：用户提供下载文件，已核对其 App ID、本机设备、现有开发证书、有效期及所需 Keychain group 授权，并导入 Xcode。主 App 已完成当前工作树的开发签名构建、稳定路径安装和启动验证；这不构成 Developer ID/Store 分发或完整体验验收。独立 Helper 保持原样。

| 组件 | 标识 |
|---|---|
| 主 App | `app.blocks.app` |
| Core | `app.blocks.core` |
| ScreenshotCore | `app.blocks.screenshotcore` |
| ActionBroker | `app.blocks.action-broker` |
| ClipboardBroker | `app.blocks.clipboard-broker` |
| PluginRunner | `app.blocks.plugin-runner` |
| Selection Helper | `app.blocks.selection-helper` |
| LaunchAgent | `app.blocks.action-broker` |
| Mach service | `app.blocks.action-broker.xpc` |
| Helper URL Scheme | `blocks-selection-helper` |

现有 Keychain service、`~/Library/Application Support/Blocks/Data` 逻辑路径和 `Blocks.sqlite` 数据库名保持不变。本轮不创建额外 App ID；只有后续 Archive/provisioning 证据明确要求时，才评估独立辅助组件的注册需求。

## 本轮边界

- 已实施：三语 Finder/系统显示名、三语屏幕录制用途说明、官网品牌文案、发布配置 URL、静态门禁和产物审计。
- 截至 2026-09-05，主 App ID、单台开发测试设备、Development profile 本地导入校验及签名开发版安装已完成。DNS/Cloudflare、邮箱、Store profile、App Store Connect 记录、签名发布包、公证、上传或公开发布仍未实施。
- App Store Connect 首次建档只尝试 `Blocks for Mac`；如果不可用，停止建档并重新确认，不自动换名。
