# Blocks 名称与固定签名迁移

用户确认保留当前源码版数据，旧版本只备份，不合并两套数据库或钥匙串。

## 固定契约

- 对外名称与主 executable：Blocks；安装位置：`~/Applications/Blocks.app`。
- Bundle ID 仍为 `app.blocks.dev`。原有 `Application Support/Blocks Dev`、Keychain、IPC 和 Helper 配对命名空间保持不变。
- Helper 对外名称 Blocks Helper，包内路径/内部 ID 保持兼容。
- 本机固定 Developer ID 证书，私钥保留系统钥匙串。公开指纹/Team 配置只在机器本地，0600，不提交仓库。
- 没有本机签名配置的贡献者仍可 ad-hoc 构建；明确提示该方式重编译可能要求授权。已证书签名安装不允许自动降级、换身份或静默回退。

## 已验证

- 名称、固定路径与 trust manifest 对齐；local/production 安全自测分别 52/33 项通过。
- 安装事务的旧路径迁移、失败回滚、权限/锁/竞态自测通过；旧包与原 manifest 可恢复。
- 所有已识别嵌套代码（包含 Sparkle Updater.app/Autoupdate）按深度优先重签并核对证书 Team；首次安装后实际核对 17 个嵌套组件通过。
- 两个内容不同的原生可执行文件使用同一证书签名，designated requirement 相同且交叉验证通过。
- 首次统一安装成功；旧开发版进入安装恢复目录，数据目录未迁移或清空。
- 旧 Debug App 与独立 Helper 正常退出，旧 Helper 登录启动已关闭；两个旧包移入应用数据目录下的 `.noindex` 备份。旧配对/钥匙串没有清空。
- 从临时签名切换到证书后，仅定向重置 `app.blocks.dev` 的 ScreenCapture/Accessibility 旧记录，并通过系统设置恢复权限；App 已确认两项授权有效。

## 跨安装验收

首次证书安装的 CDHash 为 `c9244a7de5fcf9772ec8777956dae827b000af59`。
每次安装写入独立的本机构建标识，使包的签名哈希不同；验证不能靠重复安装相同哈希假装升级。
第二次安装必须保留原 designated requirement，并在不改动任何 TCC 设置的条件下确认授权仍有效。
第二次安装最初因 App/Helper 仍运行而安全停止。只读日志确认主 App 主动取消退出；单独正常退出未配对的 Helper 后，主 App 正常退出，未使用 kill 或跳过安全参与者。

随后第二次覆盖安装成功，CDHash 为 `fc73b690b3117119bcb9b6e4852bac000c564767`，与首次不同；路径、Bundle ID、证书 Team 和 designated requirement 保持相同。
第二次安装期间没有重置/授予权限。新进程的应用权限页确认录屏、辅助功能仍已授权，完成两次不同签名哈希包之间的授权保留验证。
这是固定证书源码包的覆盖安装验证，不等同于尚未完成的官方 Sparkle 跨版本升级验收。

## #9 后续修复

远端 e5a536b 的结果提供了准确几何：1x，visible `(0,60,1024,677)`，预期 origin `(262,188.5)`，实际 `(262,188)`，尺寸均为 `500x420`。
因此只把原测试的位置断言改为最多半个物理像素（`0.5 / backingScaleFactor`），尺寸仍精确匹配，不改生产居中逻辑。
本机完整测试 1933 passed / 0 failed / 16 skipped；44b797b 的远端完整 CI（run 34267012402）也已成功，#9 据此关闭。

## 后续实机与发布检查

- 中文、英文浅色、日文深色的翻译面板原文标题/字数行已在安装版检查，测试文本经 Apple 本地翻译完成，顶部透明带没有再出现。没有改动 macOS 全局外观；最终恢复 App 外观/语言跟随系统。
- #20 使用空关注语言触发真实配置错误：独立通知窗口为 300×80、屏幕坐标 (810,30)，翻译面板为 640×520、(640,295)，错误未插入面板布局。自动化拖动未产生可验证的位移，因此不声称移动/缩放后不随动验收完成。测试后英语关注语言已恢复。
- 用户追加要求提交并发布新版本。代码已提交推送为 44b797b。实际执行 `./script/release.sh prepare 0.1.0-beta.1` 在身份预检停止：`BLOCKS_NOTARY_KEYCHAIN_PROFILE is missing`。
- 本机发布配置的主 App 和 Helper 分发 profile UUID/path 均未设置；现有 profile 不是 `ProvisionsAllDevices=true` 的 Developer ID 分发 profile，不能替代。
- 没有创建 GitHub Release、发布未公证包或切换 appcast；本机固定证书源码版不冒充官方发行版。仍需补齐发布凭据及未完成实机门禁后再发布。
- 补跑官方安装脚本测试时，发现它会读取主机真实应用目录，导致本机已有 Blocks 时的目录选择断言失败。新增仅供测试替换的安装根目录 seam，生产默认目录不变；全部官方安装自测、发布流水线自测和应用更新安全自测通过。开发测试入口同时增加编译/执行阶段提示，避免长时间没有可区分的进度。
