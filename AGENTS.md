# AGENTS.md

## 基本边界

- 严谨思考，拒绝迎合；不把猜测、推断或计划写成已确认事实。
- 不确定时明确区分事实、推断、建议和待验证项。涉及价格、版本、政策、接口或系统限制时，实施前重新核验。
- 不保存敏感凭据。密码、API key、验证码、私钥、Cookie、完整订阅链接和支付信息不得写入仓库、日志或长期记忆。
- 保持当前任务范围；不得把未确认需求、无关重构、权限扩张或外部服务混入实现。

## 计划与实施

- 计划阶段负责明确范围、非目标、方案、风险与可验证的完成条件。未经用户明确授权，不进入生产实现。
- 实施阶段按已确认方案完成代码、测试、真实体验验收和交付；自动化通过不能替代用户明确要求的安装版或外部 App 证据。
- 发现计划依据错误时先报告事实与影响，再修正规划；不得为了维持原结论绕过真实问题。

## 角色入口

- 角色职责位于 `agents/`。只有任务确实需要专业分工或用户明确要求时才创建子 agent。
- 项目负责人负责范围、取舍、验收和最终交付；子 agent 不替代项目负责人或用户作产品决策。

## UI 与交互（强制）

- 任何 UI 或交互改动开始前，必须先阅读 [`docs/产品知识库/UI与交互规范/index.md`](docs/产品知识库/UI与交互规范/index.md) 以及当前任务对应的专题文档。
- 业务模块必须复用全局 Token、组件、状态和动效入口；不得新增模块私有的结构颜色、任意圆角、尾列补丁、布尔控件样式或无边界隐式动画。
- 设置页必须遵循分区标题与设置行标题同左基线、值控件同右边缘、持久二态设置统一使用 Switch 的契约。
- 体验改动必须同时检查稳定几何、鼠标与键盘、VoiceOver、浅深色、多语言、窗口尺寸和减少动态效果。

## 文档归属

- 长期产品与体验规则写入 `docs/产品知识库/`。
- 架构、接口和运行边界写入 `docs/技术知识库/` 或 `docs/决策记录库/`。
- 项目阶段、计划、进度、证据和验收记录写入 `docs/项目管理库/` 对应项目目录。
- 本文件只保存长期协作边界和强制入口，不保存项目进度、临时任务清单或重复的角色细节。
